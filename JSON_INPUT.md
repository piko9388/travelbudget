# 출장 계획 JSON 입력 형식

사내 LLM 에게 **이 문서의 «형식» 부분을 그대로** 주고, 받은 JSON 을
화면 **엑셀 일괄 등록** 칸에 붙여넣으면 됩니다. 엑셀 붙여넣기와 같은 미리보기·같은
등록 버튼을 씁니다(경로가 둘로 갈리지 않습니다).

화면에서도 같은 글을 볼 수 있습니다 — **엑셀 일괄 등록 → [JSON 형식 보기] → [형식 복사]**.
그 버튼은 **지금 등록된 CCG팀 목록까지 채워서** 복사해 주므로, 문서보다 그쪽이 정확합니다.

---

## 형식

```
출장 계획을 아래 JSON 형식으로만 출력하세요. 설명·주석·코드펜스 없이 JSON 배열만 출력합니다.

[
  {
    "plan_type": "계획",                 // 계획 | 변경 | 긴급  (기본 계획)
    "city": "청주",                       // 필수 — 출장 도시
    "org": "원익머트리얼즈",               // 필수 — 출장 기관&업체
    "purpose": "NF3 순도 관리 정기 Audit", // 필수 — 출장 목적&사유 (소재명 포함 권장)
    "kind": "정기 Audit",                 // 기술교류(Live Demo, Data 분석) | 실사&사양 개선,협의 |
                                          //  정기 Audit | 비정기 Audit(Issue/Theme) | 기타
    "dep_dt": "2026-08-04",               // 필수 — 출발일자 YYYY-MM-DD
    "ret_dt": "2026-08-05",               // 복귀일자. 없으면 출발일과 같은 날(당일)
    "car": "미사용",                       // 미사용 | 자차사용
    "tags": ["CMP", "정기 Audit"],         // 선택 — 분류·검색용. "CMP, 정기 Audit" 처럼 한 줄도 됩니다
    "remark": "",                          // 비고 (긴급 출장 실적 입력 시 필수)
    "travelers": [                         // 필수 — 1명 이상. 같이 가면 여기에 사람을 늘립니다
      {
        "name": "박영희",                  // 필수 — 성명
        "emp_no": "20140508",              // 필수 — 사번 (한 출장 안에서 중복 불가)
        "rank": "TL",                      // TL | 팀장
        "ccg_nm": "EDTW소재기술",           // 필수 — CCG팀명(정확히) 또는 CCG 코드 8자리
        "p_trans": 120000,                 // 계획 교통비 (숫자, 원)
        "p_lodg": 100000,                  // 계획 숙박비
        "p_meal": 70000,                   // 계획 식대&잡비
        "p_etc": 0                         // 계획 기타
      }
    ]
  }
]

규칙
· 한 번의 출장에 여러 명이 가면 travelers 에 사람을 추가합니다(출장 1건 = 객체 1개).
· 서로 다른 출장은 배열에 객체를 늘립니다.
· 금액은 숫자만 씁니다(콤마·"원" 없이). 모르면 0 을 넣습니다.
· 실적 금액은 넣지 않습니다 — 다녀온 뒤 화면에서 입력합니다.
· CCG팀명은 아래 목록의 이름을 정확히 씁니다: (화면의 [형식 복사]가 현재 목록을 채워 줍니다)
· 날짜는 반드시 YYYY-MM-DD 입니다.
```

---

## 이렇게 씁니다

1. 사내 LLM 에 위 «형식» 을 붙여넣고, 이어서 출장 내용을 자연어로 적습니다.
   > 8월 4~5일 청주 원익머트리얼즈에 NF3 정기 Audit 으로 박영희 팀장(20140508, EDTW소재기술)과
   > 이정훈 TL(2071478, 소재전략)이 갑니다. 1인당 교통 7만, 숙박 9.5만, 식대 6.5만입니다.
2. LLM 이 뱉은 JSON 을 **엑셀 일괄 등록** 칸에 붙여넣습니다.
3. 미리보기에서 건수·인원·금액을 확인하고 **등록** 을 누릅니다.
   실제로 가는 출장이면 아래 **바로 확정(예산 반영)** 을 함께 체크합니다.

## 관대하게 받는 것

| 넣은 값 | 처리 |
|---|---|
| 배열이 아니라 객체 하나 | 1건으로 봅니다 |
| `{"groups":[ ... ]}` 로 감싼 것 | `groups` 안을 읽습니다 |
| `2026.8.4` · `26-08-04` | `2026-08-04` 로 고쳐 읽습니다 |
| `"120,000"` · `"120000원"` | 숫자 120000 으로 읽습니다 |
| `ccg_nm` 에 CCG 코드(8자리) | 코드로 팀을 찾습니다 |
| `kind` 가 목록에 없는 말 | 가장 가까운 것, 못 찾으면 **기타** |
| `tags` 에 `#CMP` · `"CMP, 정기 Audit"` | `["CMP","정기 Audit"]` 로 정리 (최대 8개·각 20자) |

## 막는 것 (등록 전에 화면에 이유가 뜹니다)

- `city` · `org` · `purpose` · `dep_dt` 가 없는 건
- `travelers` 가 비었거나, `name`/`emp_no` 가 없는 사람
- CCG팀을 알 수 없는 사람 (팀명이 목록에 없거나, `Patterning소재` 처럼 두 팀에 걸리는 말)

> 붙여넣기만으로는 아무것도 저장되지 않습니다. **미리보기를 보고 등록을 눌러야** 원장에 들어갑니다.
