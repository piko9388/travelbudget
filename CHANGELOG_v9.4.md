# CHANGELOG v9.4

- 기준일: 2026-07-27
- 대상: v9.3 → v9.4 (v9.3 패키지는 그대로 보존, v9.4 는 별도 zip)
- 범위: **배포 안전성 · 지표 정확성 · 실제 인폼 흐름 · 데스크톱 사용성**
  기능 확대 없음. 승인·통제 기능 추가 없음.

---

## 0. 검토 의견 판정 결과

받은 지적을 코드로 하나씩 재현해 판정했습니다. **맞는 지적은 모두 고쳤고,
사실과 달랐던 항목은 고치지 않고 근거를 남깁니다.**

### 사실 — 수정함

| # | 지적 | 재현 결과 |
|---|---|---|
| A1 | `webmain.py` import 오류 | 저장소에서는 `servera/__init__.py` 재노출 덕에 동작했으나, **사내 `servera/__init__.py` 는 이 프로젝트 소유가 아님** → 실서버에서 실패. 지적대로 수정 |
| A2 | `test_api.py` 가 운영 데이터를 훼손 | **가장 심각.** `shutil.rmtree('servera/travelbudget/data_json')` + 운영 `TB_DATA_DIR` 상속. DEPLOY.md 가 서버에서 실행하라고 안내까지 하고 있었음 |
| A3 | 신규 설치 시 예시 데이터 자동 생성 | 첫 실행에 출장 8건·예산 2건이 생성됨 |
| B5 | CCG별 건수 중복 집계 | 2개 팀이 함께 간 출장 1건이 합계에서 2건으로 — CCG행 합 11 vs 실제 9 |
| B7 | 잘못된 입력이 500 | `Infinity` → `OverflowError`, `/api/audit?n=abc` → `ValueError` |
| C8 | 계획 수정 UI 없음 | PUT API 만 있고 화면 진입점 없음 |
| C11 | 인증 모달의 ID 입력란 | 서버는 `X-Admin-PW` 만 검증 — ID 칸은 장식 |
| C13 | 인폼 기본 수신자 | 4명(이정훈·김은정·김건영·천지웅)이 기본값 |
| D | SAP 반쪽 기능 | 입력 UI 없이 목록 열·검색 필터·API 만 존재 |
| 문서 | API 개수 불일치 | 문서 18/19개 vs 실제 **22개** |
| 문서 | 없는 도구의 통과 주장 | E2E 스크립트가 저장소에 없는데 통과 수치를 주장 |
| 문서 | Qwen 출력이 순수 JSON 아님 | `[확인필요]` 를 JSON 뒤에 붙이게 되어 있어 `json.load` 실패 |
| 문서 | 실적만 보고 처리 완료 추론 | 프롬프트 기본 규칙이 그렇게 되어 있었음 |

### 사실과 다름 — 수정하지 않음 (근거)

| 지적 | 실제 |
|---|---|
| 확정 확보액·실적액 **이중 차감** | 이미 정상. 실적 입력 시 확정 확보 820,000 → 620,000 으로 **해제**되고 실적액만 처리중으로 이동. 가용은 차액만큼만 변동 (테스트 §21 로 고정) |
| SAP 전표가 **처리 완료 조건** | 조건으로 쓰인 적 없음. 전표 없이도 완료 가능했고 지금도 그렇습니다 |
| 이관 메일 수신자가 **고정** | 이미 빈 값(`to=""`)으로 담당자가 직접 지정. 다만 화면에 그 사실이 안 보여 **안내 문구만 추가** |
| 예산 부족 시 **차단** | 차단한 적 없음. 경고만 표시. v9.4 에서 확인창을 추가했을 뿐 여전히 차단하지 않음 |
| D-7 게이트 / ±10% 사유 강제 | 둘 다 없음 (정보 표시만) |

### 범위 밖으로 유지 (요청대로 추가하지 않음)

승인·반려 큐, 예산 인터락, 출장자·BP·담당자 마스터, 팀별 예산, SQLite 전환,
페이지네이션. **모바일 대응은 이미 되어 있어 제거하지 않았습니다** — 유지 비용이 0 이고
빼는 것이 오히려 작업이라 그대로 둡니다(추가 투자도 하지 않음).

---

## 1. 수정 파일과 사유

### `test_api.py` — 운영 데이터 훼손 차단 (P0)

**전:** 실행 시 `servera/travelbudget/data_json` 을 통째로 삭제하고,
운영 `TB_DATA_DIR` 이 설정돼 있으면 **그 경로에 그대로 씀**.

**후:** import 보다 먼저 `TB_DATA_DIR` 을 임시 폴더로 강제 교체하고, 종료 시 삭제.
격리 실패 시 `assert` 로 즉시 중단. 운영 경로가 있으면 화면에 "건드리지 않습니다" 표시.
시드가 없어졌으므로 테스트는 `store.example_data()` 를 픽스처로 직접 깝니다.

```python
_PROD = os.environ.get('TB_DATA_DIR')
_TMP = tempfile.mkdtemp(prefix='tb_test_')
os.environ['TB_DATA_DIR'] = _TMP
...
assert str(_store.DATA_DIR) == _TMP, f'격리 실패: {_store.DATA_DIR}'
```

### `smoke_test.py` — 신규. 배포 확인용 읽기 전용

GET 만 호출하고, 쓰기 라우트는 "무인증이면 401" 인지만 확인합니다(401 = 데이터 변경 없음).
기동·계산식 정합·내보내기·권한 경계·예외 입력 **19개 항목**.
`python3 smoke_test.py [URL]` — 실행 전후 `data.json` md5 동일함을 확인했습니다.

### `webmain.py` — 진입점

`from servera import travelbudget` → `from servera.travelbudget import travelbudget`.
사내 `servera/__init__.py` 의 재노출에 기대지 않습니다(DEPLOY.md 안내와 동일).
`processes=1` 명시 — `data.json` 단일 파일이라 다중 프로세스 금지.

### `servera/travelbudget/store.py` — 빈 원장 시작

`default_data()` 는 **설정만** 있는 빈 원장을 반환. 예시 8건은 `example_data()` 로 분리했고
`data.example.json` 생성과 테스트 픽스처에만 씁니다.
`admin_id` 설정 제거(검증에 쓰이지 않음), 기본 수신자 4명 → **이정훈·김은정 2명**.

### `servera/travelbudget/core.py` — 계산·예외

- `num()`: `NaN`·`±Infinity`·1e15 초과를 예외 없이 0 처리 (`Infinity` → 500 이던 것)
- `dash()`: **`nTrips`** 추가 — 취소 제외·중복 없는 실제 출장 건수.
  CCG행의 건수는 '참여' 기준(두 팀이 함께 간 1건은 양쪽에 표시)이라 합계에 쓰면 안 됩니다.
- `make_transfer_mail()`: `to_hint` 추가 — "수신자: 직접 지정"

### `servera/travelbudget/routes.py` — 권한·감사·예외

- `_qint()`: 쿼리 정수 파라미터를 안전 파싱 (`?n=abc` → 500 이던 것)
- `update_group()`: **실적 입력 후에는 예산 담당자 모드에서만 수정** (계획·확정 단계는 자유)
- `_diff_fields()`: 감사 로그에 **무엇이 바뀌었는지** 기록
  (`출장도시: 수정전→수정후 / 계획액: 100,000→150,000`)
- **계획 수정이 기존 실적을 지우던 버그 수정** — 수정 화면은 계획액만 보내므로
  실적 금액(`a_*`)을 사번 기준으로 이식해 보존합니다. (v9.4 에서 새로 발견)

### `servera/travelbudget/static/app.js` — 화면

- **출장 계획 수정 UI** — 목록의 `수정` 버튼 → 계획 화면이 수정 모드로 열림.
  실적이 있으면 담당자 모드를 먼저 요구하고, 상태·확정 체크박스는 건드리지 않습니다.
- **예산 부족 확인창** — 확정 시 가용 잔여가 음수가 되면
  "확정 후 가용 잔여가 −120,000원입니다. 예산 부족을 인지한 상태로 계속 확정하시겠습니까?"
  **계속 확정하면 정상 저장됩니다. 차단하지 않습니다.**
- **문구** — `관리자 인증` → `예산 담당자 모드`, 좌측 메뉴 `잠금` → `담당자`,
  `확정 예정` → **`출장 확정 · 예산 반영`**
- **인증 모달** — 검증하지 않는 ID 입력란 제거, `label[for]` 연결, 비밀번호 칸에 포커스
- **SAP 제거** — 목록 열·검색 필터에서 삭제
- **이관 메일** — 수신자가 비어 있으면 "수신자: 직접 지정"을 빨간 글씨로 표시

> ⚠ `확정 예정` 은 **화면 표기만** 바꿨습니다. `data.json`·CSV·센터 제출 양식의
> **저장값은 `확정 예정` 그대로**입니다. 저장값까지 바꾸면 기존 데이터와 센터 양식이
> 깨지고 Qwen 변환 규칙도 어긋납니다. 표기는 `ST_LABEL` 한 곳에서만 치환합니다.

### `QWEN_PROMPT.md` — 순수 JSON·추론 금지

- 출력 전체가 `json.load()` 로 읽히는 JSON 하나여야 함을 명시. 코드펜스·설명문 금지.
- `[확인필요]` 를 JSON 밖에 붙이던 것을 **JSON 안 `_confirm_needed` 배열**로 이동
  (시스템이 무시하는 키임을 확인했습니다)
- **실적이 있다는 이유만으로 `처리 완료` 추론 금지.** 근거가 없으면 `실적 입력·인폼` 으로 두고
  `_confirm_needed` 에 기록. (처리 완료는 돈이 나갔다는 뜻이라 예산 잔액이 틀어짐)
- settings 예시에서 `admin_id` 제거, 수신자 2명

### `tools/e2e/` — 없던 도구를 저장소에 넣음

통과 수치만 주장하고 스크립트가 없던 문제를 해결했습니다.
샌드박스 전용 경로를 걷어내고 `TB_REPO`·`PLAYWRIGHT_PATH`·임시 폴더로 이식 가능하게 만들었습니다.
E2E 도 저장소 `data_json` 을 지우던 코드를 제거하고 임시 폴더 + 픽스처로 동작합니다.

### `README.md` / `DEPLOY.md`

- API 개수 22개로 정정
- **실제로 실행한 명령과 결과만** 표로 기록, 각 명령이 운영 데이터를 건드리는지 명시
- DEPLOY 에 **1 worker 고정** 절(gunicorn `-w 1`, uWSGI `--processes 1`, waitress) 추가
- 서버 점검은 `smoke_test.py` 로 안내 (`test_api.py` 는 서버에서 불필요)
- 점검표에 "첫 접속 시 예시 출장이 **없어야** 정상" 추가

---

## 2. 실제 수행한 테스트와 결과

모두 이 저장소에서 실행한 결과입니다.

```
$ python3 test_api.py
  [격리] 임시 데이터 디렉터리 /tmp/tb_test_xxxxxxxx
  ...
  API 통합  252 passed / 0 failed

$ python3 smoke_test.py http://127.0.0.1:5000
  스모크 테스트  19 passed / 0 failed
  (운영 데이터는 변경되지 않았습니다)
  → 실행 전후 data.json md5 동일 확인

$ python3 tools/e2e/fuzz.py
  퍼징 1,302회 → 500/예외 0건

$ PLAYWRIGHT_PATH=... node tools/e2e/e2e.mjs
  브라우저 E2E  82 passed / 0 failed

$ PLAYWRIGHT_PATH=... node tools/e2e/e2e_pages.mjs
  Pages 정적 E2E  19 passed / 0 failed

$ PLAYWRIGHT_PATH=... node tools/e2e/design_audit.mjs
  대비 검사 조합 30개 / 미달 0개 · 폰트 스택 1종 · 1920~390px 오버플로 0px
```

신규 테스트(§20~22, 64개)가 검증하는 것:

- 신규 설치 빈 원장 / 예시는 `example_data()` 에만 / `admin_id` 없음 / 수신자 2명
- 테스트가 임시 폴더에서만 동작하고 운영 `TB_DATA_DIR` 을 쓰지 않음
- `nTrips` 가 실제 건수와 일치, 건수 KPI 합 = `nTrips`, CCG행은 참여 기준
- `NaN`·`Infinity`·`-Infinity` → 400, `?n=abc` → 500 아님, 잘못된 분기 → 400
- 실적 전 수정은 인증 없이, 실적 후는 401 → 담당자 모드로 200
- **계획 수정이 기존 실적을 지우지 않음**
- 감사 로그에 변경 항목과 시각 기록
- 예산 초과여도 확정·실적·처리 완료 모두 성공, `short` 플래그만 켜짐
- 확정 확보액↔실적액 이중 차감 없음 (해제 → 이동 → 차액만 변동)
- 이관 메일 `to` 가 비어 있고 직접 지정 안내가 붙음
- 화면에 `sap` 문자열이 남아 있지 않음 / 데이터 필드는 호환 유지
- 문서의 API 개수가 실제와 일치, 주장한 도구가 저장소에 존재

---

## 3. 완료 판정 대조

| 기준 | 결과 |
|---|---|
| 독립 실행 진입점으로 서버 정상 시작 | ✅ `python3 webmain.py` 기동 후 smoke 19/19 |
| 기존 API 테스트 + 신규 테스트 통과 | ✅ 252/252 (기존 188 + 신규 64) |
| 테스트 전후 운영 `TB_DATA_DIR` 불변 | ✅ 임시 폴더 강제 + `assert` 로 고정, md5 확인 |
| 신규 설치 빈 원장 | ✅ 출장 0건 / 예산 0건 |
| 예산 부족해도 확정·실적·완료 가능 | ✅ 확인창만, 저장은 정상 |
| 확정 확보액·실적액 이중 차감 없음 | ✅ 원래 정상 — 테스트로 고정 |
| 이정훈·김은정 외 고정 수신자 없음 | ✅ 2명 |
| 이관 메일 수신자 직접 지정 | ✅ 빈 값 + 화면 안내 |
| 화면에 SAP 반쪽 기능 미노출 | ✅ 제거 (데이터 필드만 호환 유지) |
| 라벨·대비·키보드 이동 명확 | ✅ 대비 미달 0건, 폰트 1종, `label[for]` 연결, 탭·엔터 이동 |

---

## 4. 남은 판단 사항 (임의로 하지 않은 것)

1. **`확정 예정` 저장값** — 화면만 `출장 확정 · 예산 반영` 으로 바꿨습니다.
   저장값까지 바꾸길 원하시면 기존 `data.json` 일괄 변환 + 센터 양식 재확인이 필요합니다.
2. **정적 GitHub Pages 판(`docs/index.html`)의 시드** — 브라우저 localStorage 데모라
   예시 8건을 유지했습니다. 사내 Flask 원장과는 별개입니다. 빈 화면으로 바꾸길 원하시면 말씀해 주세요.
3. **일반 사용자 인증** — 여전히 없습니다(금액을 움직이는 행위만 담당자 모드).
   출장자끼리 서로의 잠정 계획을 지울 수 있는 상태이며, 이번 범위에 포함하지 않았습니다.
