# '26 SK hynix AI Summit — 제출서

---

## (1) 사례 개요

| | |
|---|---|
| **이름** | 이정훈 (소재전략) |
| **사례명** | 출장비 업무를 "AI가 실행할 수 있는 형태"로 다시 설계 |
| **시스템** | 소재 국내 출장비 관리 · v10.28 |
| **사내 링크** | http://material.skhynix.com/travelbudget |
| **운영 상태** | 실운영 중 (2026-07-27 개발 착수 → 현재 운영) |

---

## (2) AI 활용 방식

| 방식 | 해당 | 무엇을 |
|---|---|---|
| AI Agent | | |
| Skill | | |
| Plugin | | |
| MCP | | |
| **Prompt** | **O** | 자연어 출장 계획 → 구조화 JSON 변환 규격을 사내 LLM에 제공 (`JSON_INPUT.md`) |
| **Harness** | **O** | 개발·검증·배포 전 과정을 AI 에이전트(Claude Code)와 수행. 배포 전 **자동 검증 1,261건**을 AI가 실행 |
| **기타** | **O** | **업무 재설계** — 런타임에 LLM이 필요 없도록 업무 구조 자체를 바꿈 |

**한줄 설명**
> AI로 무엇을 만들까가 아니라, **AI가 안전하게 실행할 수 있도록 업무를 어떤 구조로 바꿔야 하는가**를 먼저 풀었습니다.
> 그 결과 런타임에는 LLM이 필요 없어졌고, AI는 **만드는 쪽(Harness)** 과 **입력 규격(Prompt)** 에서 일합니다.

> **명시**: 이 시스템의 **런타임에는 LLM이 들어 있지 않습니다.** 문답형 입력·이름 매칭·
> 인폼/정산서 생성은 모두 결정적 로직입니다. 예산과 상태는 오답이 0이어야 하는 계산이라
> 의도적으로 AI를 쓰지 않았습니다. (심사 항목 *AI 활용 적절성* 에 대한 저희의 답입니다)

---

## (2) 해결하려는 업무 문제

### 대상 업무
소재 조직의 **국내 출장 계획 수립 → 예산 관리 → 실적 정산 이관** 전 과정. 분기당 수십 건.

### 기존 업무 방식
- 개인 엑셀로 계획을 받아 담당자가 취합
- 예산 잔여는 담당자가 수식으로 계산
- 실적은 다시 엑셀로 받아 **비용 이관 메일을 손으로 작성**
- 사내 결재(국내 출장 정산서)에 **같은 내용을 다시 타이핑**

### 주요 불편 / 병목

| 병목 | 내용 |
|---|---|
| **반복 작업** | 같은 출장 정보를 **엑셀 → 시스템 → 결재 창** 세 번 입력 |
| **다중 시스템 조회** | 사번·CCG는 조직도에서, 카드 사용액은 SAP에서 각각 확인 |
| **품질 편차** | 사람마다 엑셀 양식·계정과목 분류가 달라 센터 제출 시 합계 불일치 |
| **오류** | 잔여 예산 수식이 파일마다 달라짐. 확정 전/후 금액 기준이 섞임 |
| **지연** | 실적 입력이 늦어 정산 이관이 밀림 |
| **숙련자 의존** | 예산 잔여 공식과 이관 절차를 담당자만 정확히 알고 있음 |

### 이 업무에 AI가 필요했던 이유
**두 가지를 구분했습니다.**

1. **런타임** — 상태 6개와 예산 공식은 **틀리면 안 되는 계산**입니다. 여기에 LLM을 넣으면
   정확도가 100%에서 내려갑니다. 필요했던 것은 AI가 아니라 **업무 구조의 재설계**였습니다.
2. **만드는 과정** — 실운영 시스템을 6주 만에 29개 버전으로 반복하며, 매 배포마다
   1,261건을 검증하는 일은 사람 혼자 못 합니다. **여기에 AI가 필요했습니다.**

---

## (3) 제작 내용

### 주요 기능 (3개)

**① 문답형 입력 — 묻는 대로 답하면 등록까지 끝남**
한 화면에 질문 하나, 고르면 바로 다음. 계획 **5문**, 실적 **2문**.
고를 수 있는 값(출장구분·자차·직책·CCG·태그)은 전부 눌러서 고르고, 날짜는 달력을 씁니다.
**질문이 답이 들어갈 칸을 정해 주므로 자연어 해석이 필요 없습니다.**

**② 이력 기반 이름 → 사번·CCG 매칭 (조직도 연동 대체)**
조직도 연동이 불가하여, **지금까지 출장 간 사람의 이력을 명부로** 씁니다.
이름만 치면 사번·직책·CCG가 따라오고, **동명이인이면 둘 다 사번을 붙여 띄워 고르게** 합니다.

**③ 산출물 자동 생성 — 인폼 메일 · 센터 제출 양식 · 결재 정산서 도우미**
실적을 저장하면 비용 이관 메일이 자동 생성됩니다.
센터 제출 27필드 양식은 Excel/CSV로 추출되고, 사내 결재 정산서는 **결재 창의 칸 순서 그대로**
정리해 복사 버튼을 제공합니다.

### 작동 흐름

```
입력                     처리                                결과
─────────────────────────────────────────────────────────────────────
[계획] 5문 응답      →  검증(필수·중복사번·금액상한)      →  원장 등록
       (대부분 클릭)     예산 자동 계산                      확정 시 예산 확보

[실적] 이름 클릭     →  내가 간 출장만 필터                →  인폼 메일 자동 생성
       + '계획대로'      계획액 → 실적액 전환                 정산서 도우미(복사)
                                                             센터 제출 양식(Excel)
```

### 최종 산출물
- 사내 Flask 서버 운영 시스템 (http://material.skhynix.com/travelbudget)
- 비용 이관 인폼 메일 (Outlook 붙여넣기용 HTML 표)
- 센터 제출 정산 대장 (27필드 Excel/CSV)
- 국내 출장 정산서 작성 도우미 (결재 창 칸 순서 · 탭 구분 복사)
- 문서: 시스템 기준서 `SPEC.md` · AX 로드맵 `AX_ROADMAP.md` · 배포/업그레이드/스키마

### 사용자 검토 / 승인 절차
**있음.** 그리고 이것이 이 시스템의 핵심 설계 원칙입니다.

- 문답은 **양식을 채워 주기까지만** 하고, 지나온 답을 표로 보여 준 뒤 **사람이 [등록]을 누릅니다.**
- 저장은 기존 검증·권한 게이트를 그대로 지나갑니다. **자동화가 새 쓰기 경로가 되지 않습니다.**
- 이관·처리 완료·예산 리비전은 **관리자 인증 필수**. 모든 변경은 감사 로그에 남습니다.
- 예산이 모자란 확정은 **막지 않고 확인만** 받습니다 (업무 판단은 사람 몫).

---

## (4) 업무 변화 및 효과

### 적용 전 / 후

| 구분 | 적용 전 | 적용 후 |
|---|---|---|
| **업무 절차** | 개인 엑셀 작성 → 담당자 취합 → 시스템 입력 → 결재 창 재입력 (**4단계, 3회 입력**) | 대시보드에서 문답 → 등록 (**1단계, 1회 입력**) |
| **소요 시간** | 계획 1건: 엑셀 작성 + 취합 + 입력<br>실적 1건: 엑셀 + 메일 표 수작성 | 계획 1건: 문답 5문<br>실적 1건: 이름 클릭 + '계획대로' 1클릭 |
| **반복 작업** | 계획 등록 시 **입력 지점 26개**(2인 기준)<br>사번·CCG를 매번 조직도에서 확인<br>인폼 메일 표를 매번 손으로 작성 | 문답 **5문**, 사번은 **치지 않음**(이름 매칭)<br>금액은 **1인당 1회**(인원 늘어도 그대로)<br>인폼 메일 **자동 생성** |
| **오류 / 품질** | 파일마다 잔여 예산 수식이 다름<br>확정 전/후 금액 기준 혼용<br>센터 제출 합계 불일치 | 예산 공식 **1개**(가용 잔여 = 총예산 − 완료 − 처리중 − 확정예정)<br>계획액/실적액 전환 **자동**<br>배포 전 **자동 검증 1,261건** |
| **숙련자 의존** | 예산 공식·이관 절차를 담당자만 앎 | 화면이 다음에 할 일을 말함. 안내 문서 3종 |
| **데이터 정본** | 개인 엑셀 다수 | **원장 1개** + 자동 백업 30개 + 감사 로그 |

### 가장 크게 개선된 부분
**"같은 내용을 세 번 입력하던 것"이 한 번으로 줄었습니다.**
그리고 **사번을 외우거나 조직도를 열 필요가 없어졌습니다** — 이력이 명부 역할을 합니다.

### 정량 효과

| 항목 | 전 | 후 | 근거 |
|---|---|---|---|
| 계획 등록 입력 지점 (2인) | **26개** | **약 10개** (대부분 클릭) | 화면 필드 실측 |
| 계획 등록 시 타이핑 항목 | 13개 | **1~3개** (도시·업체·목적) | 나머지는 선택/달력/이력 자동 |
| 사번 입력 | 인원수만큼 | **0회** | 이름 매칭 |
| 금액 입력 | 인원 × 4칸 | **4칸** (1인당 공통) | 인원 증가에 불변 |
| 실적 입력 | 화면 이동 + 출장 검색 + 인원×4칸 | **이름 클릭 + 1클릭** | '계획대로' 버튼 |
| 인폼 메일 작성 | 수작성 | **자동 생성** | 실적 저장 시 |
| 배포 전 검증 | 없음 (수동 확인) | **1,261건 자동** | API 790 / 브라우저 249 / 윈도우크롬 135 / 정적판 33 / 설정보존 22 / 디자인 13 / 스모크 19 |

> **소요 시간(분)은 기재하지 않았습니다.** 적용 전 실측치가 없어 추정하면 근거 없는 숫자가 됩니다.
> 대신 **입력 지점·타이핑 횟수·클릭 수** 등 셀 수 있는 값으로 적었습니다.
> 4분기 계획 수집을 기준선으로 삼아 **사전 신청률·실적 입력 지연**을 측정할 예정입니다 (AX 로드맵 Phase 0).

### 체감 효과
- 출장 가시는 분이 **행동하는 순간은 두 번뿐** (계획 · 실적). 나머지는 시스템과 담당자가 처리
- 예산 잔여를 물어보지 않아도 화면에 늘 보임
- 결재 정산서를 쓸 때 시스템 화면을 오가며 옮겨 적지 않아도 됨

### 기타 의견
**"AI를 넣는 것"과 "AI가 일할 수 있게 만드는 것"은 다른 일이었습니다.**
업무를 상태 6개·규칙·단일 원장으로 정리하고 나니, 정작 런타임에는 LLM이 필요 없어졌습니다.
대신 그 구조 덕분에 **다음 단계(계획 제안·이상 감지)를 안전하게 얹을 수 있는 자리**가 생겼습니다.
현재 위치와 다음 4단계는 `AX_ROADMAP.md` 에 정리했습니다.

---

## (5) 사용법

### 주의 사항
- **웹 서버는 프로세스 1개 + 스레드로 기동합니다.** 원장이 파일 1개라 여러 프로세스가 쓰면 서로 덮어씁니다
- `TB_DATA_DIR` 로 데이터 폴더를 앱 밖에 지정하세요 (배포로 폴더를 덮어써도 데이터 보존)
- 업그레이드 시 **`data.json` 은 건드리지 않습니다.** 다만 새 기본값(결재 주소 등)은 자동으로 안 들어가므로 화면 [시스템 설정]에서 한 번 넣어야 합니다
- 관리자 비밀번호는 **공용 1개**입니다 (의도된 설계, 화면에 안내됨)

### 사용 방법
1. `servera/travelbudget/` 폴더를 사내 서버 `servera/` 아래에 복사
2. `webmain.py` 에 블루프린트 2줄 등록 → 프로세스 1개로 기동
3. `smoke_test.py` 로 확인 (읽기 전용, 운영 데이터 변경 없음)
4. 브라우저에서 http://material.skhynix.com/travelbudget

### 사용 예시 — 4분기 출장 계획 수집
1. 각자 대시보드에서 **[출장 계획 넣기]** → 5문 응답
   `어디로` → `언제(달력)` → `무슨 일로` → `같이 가시는 분` → `1인당 얼마`
2. 수집 단계에서는 **[실제로 가는 출장입니다] 를 체크하지 않고** 잠정으로 둠
3. 예산 배정 후 **출장 내역에서 잠정 건을 골라 일괄 확정** (한 번에 200건)
4. 다녀온 뒤 **[다녀와서 실적 넣기]** → 이름 클릭 → **[계획대로 썼습니다]**
5. 저장 시 인폼 메일 자동 생성 → 담당자가 이관

### API Key 필요 여부
**필요 없습니다.** 외부 API를 호출하지 않습니다.

### 추가 의존성
**`Flask` 하나뿐입니다.**
외부 CDN · 외부 API · 데이터베이스 · 빌드 도구 · 프론트엔드 프레임워크를 쓰지 않습니다.
글꼴은 파일로 동봉하고, 엑셀 제출본은 맑은 고딕으로 고정합니다. **사내망 안에서 완결됩니다.**

### 기타 참고 사항
- 저장은 **단일 JSON 파일** + 원자적 쓰기(임시파일→교체→fsync) + 자동 백업 30개 + 감사 로그
- 규모 근거: 10년치(2,400건)를 쌓아도 파일 3.78MB, 저장 1회 168ms
- 사내 Flask판과 정적판(맥·아이패드)이 **같은 계산 규칙**을 쓰며, 어긋나면 테스트가 실패

---

## (6) 첨부 자료

| 파일 | 내용 |
|---|---|
| `01_before_계획등록폼.png` | **[Before]** 기존 계획 등록 폼 — 2인 기준 입력 지점 26개 |
| `02_after_누구세요.png` | 신원은 처음 한 번만 (브라우저에 기억) |
| `03_after_무엇을하시겠어요.png` | 대시보드 진입 — 계획 / 실적 두 갈래 |
| `04~08_문답1~5.png` | 문답 5문 — 어디로 / 언제(달력) / 무슨 일로 / 같이 가시는 분 / 1인당 금액 |
| `09_확인화면.png` | **사람이 확인하고 누르는 지점** (자동 저장 아님) |
| `10_등록완료.png` | 4분기 계획 등록 결과 |
| `11_실적1_내출장만.png` | 실적 — **내가 간 출장만** 보여 줌 |
| `12_실적2_계획대로버튼.png` | **[계획대로 썼습니다]** 1클릭 |
| `SPEC.md` | 시스템 기준서 (업무 규칙 · 데이터 · 품질 기준 · 운영 · 제약) |
| `AX_ROADMAP.md` | AX 로드맵 (현재 위치 · 측정 지표 · 4단계) |
| `travelbudget_flask_v10.28.zip` | 사내 서버 배포본 (스모크 테스트 19건 통과) |

**실행 결과 (재현 가능)**
```
$ python3 test_api.py                    API 통합       790 passed / 0 failed
$ node tools/e2e/e2e.mjs                 브라우저 E2E   249 passed / 0 failed
$ node tools/e2e/win_chrome.mjs          윈도우 크롬    135 passed / 0 failed
$ node tools/e2e/e2e_pages.mjs           정적판          33 passed / 0 failed
$ python3 tools/e2e/settings_keep.py     설정 보존       22 passed / 0 failed
$ node tools/e2e/design_audit.mjs        디자인 감사     OK 13
$ python3 smoke_test.py                  배포본 기동     19 passed / 0 failed
                                                       ─────────────────────
                                                        합계 1,261건
```
