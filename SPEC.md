# 소재 국내 출장비 관리 — 시스템 기준서

| | |
|---|---|
| 시스템명 | 소재 국내 출장비 관리 (Domestic Travel Expense) |
| 버전 | v10.28 (2026-09-09) |
| 담당 | 소재전략 이정훈 TL |
| 적용 범위 | 소재 조직 국내 출장의 계획 수립 · 예산 관리 · 실적 정산 이관 |
| 배포 형태 | 사내 Flask 서버 (블루프린트 1개) · 외부 통신 없음 |

---

## 1. 목적

분기 예산 안에서 국내 출장을 **계획 → 확정 → 실적 → 이관 → 처리 완료** 로 관리하고,
센터 제출 양식과 비용 이관 인폼을 시스템이 생성한다.
엑셀 파일이 사람마다 흩어지고 합계가 어긋나던 문제를 **원장 1개**로 없앤다.

## 2. 적용 대상

| 역할 | 하는 일 |
|---|---|
| 출장 가시는 분 | 계획 등록 · 실적 입력 (두 가지뿐) |
| 예산 담당자 | 예산 리비전 · 소재 이관 · 처리 완료 · 보류 |
| 소재 담당자 | 비용 처리 (시스템 밖) |

---

## 3. 업무 규칙 (SSOT)

### 3.1 상태

```
계획 등록(잠정) → 확정 예정 → 실적 입력·인폼 → 소재 이관 → 처리 완료
                                                              ↘ 취소
                                        (개인별) 보류
```

- 상태 전이는 **`/actual` · `/status` 두 경로로만** 일어난다. 다른 API는 상태를 고정한다.
- **실적이 없으면 이관·완료로 갈 수 없다.** 단계 건너뛰기는 실적 입력 이후에만 허용한다.
- 여러 명이 간 건은 **사람마다 상태가 갈린다.** 그룹 상태는 개인 상태의 롤업이다.

### 3.2 예산

```
가용 잔여 = 총 예산 − 처리 완료 − 처리 중 − 확정 예정(확보)
실집행 잔여 = 총 예산 − 처리 완료 − 처리 중
```

| 상태 | 잡는 금액 | 가용 잔여에서 |
|---|---|---|
| 계획 등록(잠정) | 계획액 | **빠지지 않는다** (참고) |
| 확정 예정 | **계획액** | 빠진다 |
| 실적 입력·인폼 / 소재 이관 / 보류 | **실적액** | 빠진다 (처리 중) |
| 처리 완료 | 실적액 | 빠진다 (완료) |
| 취소 | — | 되돌아온다 |

**확정까지는 계획액, 실적을 넣는 순간 실적액으로 전환된다.**
예산이 모자란 상태의 확정은 **막지 않고 확인만 받는다** (업무 판단은 사람이 한다).

### 3.3 권한

- 이관 · 처리 완료 · 보류 · 예산 리비전 · 백업 복원 · 설정 변경은 **관리자 인증 필수**.
- 이관·완료·보류가 시작된 건(`locked`)의 수정·실적 재입력도 관리자만.
- 인증 여부와 무관하게 **모든 변경은 감사 로그에 남는다** (`actor` = admin / user).

### 3.4 검증 (등록·저장 시)

- 필수: 출장도시 · 기관&업체 · 목적&사유 · 출발일자 · 복귀일자 · 출장구분
- 출장자 1명 이상, 사번은 한 출장 안에서 중복 불가, CCG팀은 등록된 목록에 있어야 함
- 금액 상한 1억(오타 방어), 텍스트 길이 상한(붙여넣기 사고 방어)
- 구분은 `계획 · 변경 · 긴급` 셋뿐. 긴급 출장은 실적 입력 시 비고 필수.

---

## 4. 데이터

### 4.1 저장 방식

**단일 JSON 파일.** 데이터베이스를 쓰지 않는다.

```
$TB_DATA_DIR/
├─ data.json        정본 — settings · budget · groups · audit_log
└─ backup/          저장 직전 자동 백업, 30개 롤링
```

| 항목 | 방식 |
|---|---|
| 쓰기 | 임시 파일 → `os.replace` 원자적 교체 → `fsync` (중단돼도 반쪽 파일 없음) |
| 동시성 | 프로세스 내 락 1개. **웹 서버는 프로세스 1개 + 스레드로 운영** |
| 백업 | 저장 직전 자동, 30개 롤링. 화면에서 시점 복원(관리자) |
| 감사 로그 | 500건 롤링. **장기 지표는 감사 로그가 아니라 그룹 타임스탬프로 계산** |

### 4.2 규모 근거

월 20건 · 1건 2명 기준으로 측정한 값이다.

| 누적 | 출장 | 파일 | 저장 1회 |
|---|---|---|---|
| 1년 | 240건 | 0.38 MB | 19 ms |
| 3년 | 720건 | 1.13 MB | 51 ms |
| 10년 | 2,400건 | 3.78 MB | 168 ms |

### 4.3 DB 전환 조건

셋 중 하나가 생기면 전환한다. 문서형이라 **스키마 재설계 없이** 옮겨진다
(`groups` → 컬렉션, `group_id` → `_id`). 전환 시 손볼 곳은 저장소가 아니라
"전체를 메모리에 올려 두고 훑는" 전제의 계산부(`core.py`)다.

1. 웹 서버를 여러 프로세스로 올려야 할 때
2. 다른 시스템이 이 데이터를 직접 질의해야 할 때
3. 건수가 자릿수로 늘 때

### 4.4 스키마 불변

`data.json` 스키마는 **버전 업그레이드로 바뀌지 않는다.** 새 칸은 없으면 기본값으로 읽는다
(예: v10.24 `tags`). 구버전으로 되돌려도 원장이 깨지지 않는다.
설정(`settings`)은 **덮어쓰지 않는다** — 그래서 업그레이드가 안내 문구·수신자·CCG를 지운다.

---

## 5. 구성

| 파일 | 역할 | 줄 수 |
|---|---|---|
| `core.py` | 계산 엔진 (SSOT) — 상태·예산·검증·CSV·인폼 | 812 |
| `routes.py` | API 29개 + 화면 3개 | 840 |
| `store.py` | 원자적 저장 · 백업 · 감사 로그 | 229 |
| `static/app.js` | 화면 전체 | 2,952 |
| `templates/index.html` | 테마 토큰 · 레이아웃 | 580 |

**의존성은 `Flask` 하나.** 외부 CDN·외부 API·빌드 도구·프레임워크를 쓰지 않는다
(사내망 차단 환경 전제). 글꼴은 파일로 동봉하고, 엑셀 제출본은 맑은 고딕으로 고정한다.

### 5.1 이중 구현

사내 Flask판과 정적판(맥·아이패드·GitHub Pages)이 **같은 계산 규칙**을 쓴다.
정적판은 `tools/tb_local.js`가 서버 대신 브라우저에서 같은 로직을 돌린다.
두 판의 규칙이 어긋나면 테스트가 실패한다.

---

## 6. AX 기능

| 기능 | 방식 | AI 사용 |
|---|---|---|
| 문답으로 계획·실적 넣기 | 질문이 칸을 정하고, 고를 수 있는 값은 눌러 고름 | **없음** |
| 이름 → 사번·CCG | 지금까지 간 사람의 이력에서 매칭. 동명이인은 둘 다 띄움 | **없음** |
| 인폼 메일 생성 | 실적 저장 시 자동 | **없음** |
| 정산서 작성 도우미 | 결재 창 칸 순서대로 정리 + 탭 구분 복사 | **없음** |
| 예산·상태 계산 | 결정적 계산 | **없음** |

**현재 시스템에 LLM은 들어 있지 않다.** 질문이 칸을 정해 주면 해석할 자유 문장이
남지 않으므로, 이 업무 범위에서는 필요하지 않다.

### 6.1 AI 도입 시 지키는 선

1. **AI는 새 쓰기 경로가 되지 않는다.** 제안까지만, 저장은 기존 검증·권한 게이트를 지난다.
2. **오답 0이 가능한 곳에는 쓰지 않는다.** 상태 6개와 예산 공식은 결정적 계산이다.
3. **금액이 나가는 전환은 사람이 누른다.** 자동 승인·자동 지급 없음.
4. **사내망 안에서 끝난다.** 개인정보·비용 내역을 외부 모델에 보내지 않는다.
5. **재지 않는 자동화는 넣지 않는다.**

향후 계획은 `AX_ROADMAP.md` 참조.

---

## 7. 품질 기준

배포 전 아래를 **전부 통과**해야 한다.

| 검사 | 대상 | 건수 |
|---|---|---|
| `test_api.py` | API·계산·화면 소스 규칙 | 787 |
| `tools/e2e/e2e.mjs` | 브라우저 전 화면 시나리오 | 249 |
| `tools/e2e/win_chrome.mjs` | 윈도우 크롬 기준 (오류·겹침·잘림·포커스) | 135 |
| `tools/e2e/e2e_pages.mjs` | 정적판 | 33 |
| `tools/e2e/settings_keep.py` | 업그레이드 시 설정 보존 | 22 |
| `tools/e2e/design_audit.mjs` | 디자인 척도 (색·여백·글씨·모서리) | 13 |
| `smoke_test.py` | 배포본 기동 확인 (읽기 전용) | 19 |

### 7.1 디자인 척도

| 축 | 허용값 |
|---|---|
| 여백 | 4 · 8 · 12 · 16 · 24 · 32 (1·2px는 선, 48px는 본문 하단) |
| 글씨 | 12/18 · 13/20 · 14/22 · 16/24 · 20/28 · 24/32 · 32/38 (px, 줄높이 정수) |
| 굵기 | 400 · 700 두 종 |
| 모서리 | 컨트롤 6px · 카드 10px · 알약 999px · 막대 3px |
| 색 | `:root` 토큰 밖 하드코딩 금지 |
| 그 외 | 자간 금지(한글 가독성) · 이모지 금지 |

---

## 8. 운영

### 8.1 배포

1. `servera/travelbudget/` 폴더를 사내 서버 `servera/` 아래에 복사
2. `webmain.py` 에 블루프린트 2줄 등록
3. **프로세스 1개**로 기동
4. `TB_DATA_DIR` 로 데이터 폴더를 앱 밖에 둔다 (덮어써도 데이터 보존)
5. `smoke_test.py` 로 확인 — 운영 데이터를 변경하지 않는다

### 8.2 업그레이드

`data.json` 을 건드리지 않는다. 코드 파일만 교체하고 재기동한다.
설정은 자동으로 덮이지 않으므로, **새 기본값은 화면 [시스템 설정]에서 한 번 넣어야 한다.**

### 8.3 인증

공용 비밀번호 1개(관리자). **의도된 설계**이며 화면에 안내되어 있다.
개인 신원은 브라우저에 기억하는 편의 기능일 뿐, 권한을 대신하지 않는다.
공용 PC 비중이 높은 조직은 매번 묻도록 바꿀 수 있다.

---

## 9. 제약 (명시)

| 제약 | 이유 |
|---|---|
| 웹 서버 프로세스 1개 | 파일 1개가 정본 — 여러 프로세스가 쓰면 서로 덮어씀 |
| SAP 카드 내역 미연동 | 사내 권한·형식 미확정 |
| 결재 시스템 값 전달 미연동 | 현재는 링크 + 복사 |
| 개인 신원은 브라우저 기억 | 사내 SSO 미연동 |
| 감사 로그 500건 롤링 | 장기 지표는 그룹 타임스탬프로 계산 |
