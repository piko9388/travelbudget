# 소재 국내 출장비 — AX 로드맵

> 이 문서는 **지금 돌아가는 시스템(v10.27)** 을 기준으로 씁니다.
> 계획이 아니라 **다음에 무엇을 재고, 무엇을 잇고, 무엇을 하지 않을지**를 정합니다.

---

## 1. 진단 — 병목은 모델이 아니라 연결입니다

사람이 이 시스템에서 시간을 가장 많이 쓰는 곳은 "입력"이 아닙니다.
**끊긴 곳을 사람이 잇는 일**입니다.

| 끊긴 곳 | 지금 사람이 하는 일 |
|---|---|
| SAP 카드 사용 내역 → 실적 | 금액을 보고 손으로 옮겨 적음 |
| 사내 결재(정산서) → 출장 내용 | 같은 내용을 다시 타이핑 (v10.25 도우미로 복붙까지 줄임) |
| 조직도 → 사번·CCG | 손으로 (v10.27 문답이 전에 간 사람에서 끌어옴) |

그래서 **다음 단계의 값은 LLM이 아니라 연결에 있습니다.**
자연어 입력은 이미 필요 없어졌습니다 — 질문이 칸을 정해 주면 해석할 문장이 남지 않습니다(v10.27).

---

## 2. 지금 위치

| 단계 | 뜻 | 상태 |
|---|---|---|
| L0 디지털화 | 엑셀·메일 → 단일 원장 | **완료** — 원장 1개, 자동 백업 30개, 감사 로그 |
| L1 규칙 자동화 | 사람이 덜 치게 | **대체로 완료** — 예산 자동 계산, 인폼 자동 생성, 정산서 도우미, 문답 입력 |
| L2 증강 | 시스템이 먼저 제안, 사람이 확인 | **시작 안 함** |
| L3 예외 처리 | 규칙에 맞는 건은 흘려보내고 예외만 사람에게 | 해당 없음 |

L1 안에서도 아직 **아무도 결과를 재고 있지 않습니다.** 그것이 첫 번째 갭입니다.

---

## 3. 지금 잴 수 있는 것 (스키마 변경 없이)

`data.json` 이 이미 갖고 있는 값만으로 계산됩니다.

| 지표 | 계산 | 왜 보나 |
|---|---|---|
| 사전 신청률 | `lead_days >= 7` 비율 | 계획 없이 가는 출장이 예산을 흔든다 |
| 실적 입력 지연 | `ret_dt` → `inform_at` 중앙값 | 정산 지연의 가장 큰 원인 |
| 정산 리드타임 | `inform_at` → `settle_at` | 센터 제출 주기와 맞는지 |
| 계획·실적 오차 | `|act_tot − plan_tot| / plan_tot` | 계획 품질 |
| 보류율 | `proc.hold` / 인원 | 어디서 막히는지 |
| 문답 사용률 | 문답·JSON 등록 건 / 전체 | AX가 실제로 쓰이는지 |

**주의** — `audit_log` 는 500개 롤링입니다. 장기 지표는 감사 로그가 아니라
그룹의 `created_at` · `inform_at` · `settle_at` 로 계산해야 합니다.

---

## 4. 로드맵

단계에 번호를 붙인 이유는 **앞 단계가 뒷 단계의 조건이기 때문**입니다.
Phase 0의 기준선 없이 Phase 1의 효과를 말할 수 없습니다.

### Phase 0 — 재는 것부터 (지금 ~ 2주)

- 위 지표 6개를 계산하는 화면 하나. **기존 `data.json` 만 읽습니다.**
- **4분기 계획 수집을 그대로 기준선으로 씁니다** — 따로 만들 필요 없습니다.
- 끝났다고 말할 수 있는 조건: 4Q 수집 뒤 사전 신청률과 실적 입력 지연이 숫자로 나온다.

### Phase 1 — 끊긴 곳 잇기 (1 ~ 2개월)

- SAP 카드 사용 내역 → 실적 자동 채움 *(사내 확인 필요)*
- 조직도 → 사번·CCG 자동 *(사내 확인 필요)*
- 결재 창에 URL 파라미터로 값 넘기기 — 복붙에서 링크 한 번으로
- 끝났다고 말할 수 있는 조건: 실적 입력 지연 중앙값이 Phase 0 기준선보다 줄어든다.

### Phase 2 — 증강 (2 ~ 4개월)

- **계획 제안** — 작년 같은 분기, 같은 BP의 실적으로 금액 초안을 채운다
- **이상 감지** — 계획 대비 실적 이상치, 같은 날 중복 출장, 분기 예산 초과 예측
- 사내 LLM API가 열리면: 메일·메신저 문장 → 문답 자동 채움
- 끝났다고 말할 수 있는 조건: 제안 수락률이 나오고, 수정 없이 등록되는 비율이 오른다.

### Phase 3 — 예외만 사람에게 (4 ~ 6개월)

- 규칙에 맞는 건(사전 신청, 계획 대비 오차 작음, 예산 여유)은 **자동 확정 제안**
- 사람은 예외만 본다
- **금액이 나가는 전환은 끝까지 사람이 누릅니다** — 자동 승인·자동 지급은 하지 않습니다.

---

## 5. 지키는 원칙

1. **AI는 새 쓰기 경로가 되지 않는다.** 제안까지만, 저장은 기존 검증·권한 게이트를 지나간다.
2. **되돌릴 수 있다.** 백업 30개 + 감사 로그, `data.json` 스키마 무변경.
3. **오답 0이 가능한 곳에는 AI를 쓰지 않는다.** 상태 6개와 예산 공식은 결정적 계산이다.
4. **재지 않는 자동화는 넣지 않는다.** 효과를 말할 수 없는 기능은 유지비만 남는다.
5. **사내망 안에서 끝난다.** 외부 CDN·외부 API 없음.

## 6. 하지 않을 것

- 자율 승인 / 자동 지급
- 개인정보·비용 내역을 외부 모델에 보내는 것
- 예산 계산을 LLM에 맡기는 것
- 측정 화면 없이 새 자동화를 먼저 얹는 것

---

## 7. 지금 결정이 필요한 것

| 질문 | 없으면 막히는 곳 |
|---|---|
| 사내 LLM이 **HTTP API** 로 열려 있는가, 채팅 창만 있는가 | Phase 2의 자연어 입력 형태 |
| SAP 카드 사용 내역을 **읽을 수 있는가** (권한·형식) | Phase 1의 가장 큰 항목 |
| 조직도/사번을 조회할 수 있는가 | Phase 1 |
| 공용 PC 사용 비중 | v10.27이 신원을 브라우저에 기억함 — 공용이 많으면 매번 묻도록 바꿔야 함 |
