# v10.0 변경 내역 (2026-07-27)

`data.json` **스키마·저장 로직 무변경**. 폴더만 덮어쓰고 재기동하면 기존 원장을 그대로 씁니다.

---

## 1. 글꼴 — 윈도우 데스크톱에서 깨지던 원인 제거

| 전 | 후 |
|---|---|
| `Pretendard, "Malgun Gothic", …` | `"Malgun Gothic","맑은 고딕","Segoe UI",system-ui,sans-serif` |
| 버튼 43개가 브라우저 기본 Arial 로 렌더 | `button,.btn{font-family:inherit}` |
| `letter-spacing:-.01em` | 제거 (한글 가독성) |
| 글씨 크기 12종 | 11 / 12 / 13 / 14 / 16 / 20 / 32px — 7종 |

사내 윈도우에는 Pretendard 가 없어서 **라틴=Segoe UI · 한글=맑은고딕** 두 가족이 섞여 렌더되고 있었습니다.
버튼은 `font-family` 를 상속하지 않아 별도 선언이 없으면 무조건 Arial 로 떨어집니다 — 이것이 "글씨체가 깔끔하지 않다"의 실제 원인이었습니다.

## 2. 대시보드 — 숫자 중복 제거 + 도식화

- **hero 1장 + 구성 막대**로 통합. 이전에는 hero 공식 + KPI 카드 4장이 같은 네 숫자를 두 번 찍었습니다.
- **CCG팀별 가로 막대** (처리 완료·처리 중·확정 예정 3색 누적). 금액표는 `금액 표로 보기`로 접었습니다.
- **비목 구성 막대 신설** — 교통비 / 숙박비 / 식대&잡비 / 기타. "누가 썼나(CCG)" 다음 축인 "무엇에 썼나".
  합계는 위 예산 막대의 집행 합계(처리 완료+처리 중+확정 예정)와 항상 일치합니다.
- **잠정 계획 건수 튐 수정** — CCG 표의 건수는 '참여' 기준이라 두 팀이 함께 간 1건이 양쪽에 잡혔습니다.
  실제 건수는 중복 없는 `nTrips` 로 따로 계산하고, 열 이름을 `참여 출장`으로 바꿨습니다.
- **CCG 구성비 0% 수정** — 분자에는 확정 예정이 들어가는데 분모에서는 빠져 있어,
  확정만 있는 팀이 `310,000원 · 0%` 로 찍혔습니다. 두 축을 맞췄습니다.
- **`바로 할 일`이 실제 이동 버튼** — 건수만 보여주던 것을 `실적 입력 대기 1건 → 실적 입력` 처럼 목적지까지 눌러 가게 했습니다.

## 3. Outlook 열기 제거 → 드래그 앤 드롭

`mailto:` 는 본문 길이 제한과 인코딩 문제로 사내에서 실패하고, 열려도 표가 깨집니다.
버튼을 없애고 **인폼 카드를 통째로 끌어다 메일 본문에 놓는** 방식으로 바꿨습니다 (`text/html` + `text/plain` 동시 제공).
카드가 우측 버튼을 가리던 문제도 접기 버튼 + 폭 520px 로 해결했습니다.

## 4. 엑셀 일괄 등록 (신규)

센터 관리 시트에서 **행을 그대로 복사(Ctrl+C) → 붙여넣기(Ctrl+V)**.
- 머리글이 있어도 되고, 없으면 센터 27필드 순서로 읽습니다.
- 같은 `도시·업체·일자·목적` 행은 **동행자**로 묶어 1건으로 등록합니다.
- 복귀일자 없으면 당일, 자차·구분·비고 없으면 기본값. 금액에 콤마·"원"이 있어도 됩니다.
- 미리보기 → 확인 2단계이고, 기본은 **잠정(예산 미반영)** 입니다.
- `엑셀 양식 내려받기` 로 27필드 빈 양식(.xls)을 받을 수 있습니다.

## 5. 사용법 중복 제거

`이용 안내` 화면은 **흐름 요약**만 두고, 장문 설명은 인쇄·메일용 `출장자용 안내`(`/travelbudget/guide`) 한 곳으로 옮겼습니다.
같은 문장이 두 화면에 반복되지 않도록 테스트로 고정했습니다.

## 6. UX/UI 정리

- 색: 회색 3단계 + 브랜드 1 + 상태 4 = **8색**. 목록에 없는 색은 쓰지 않습니다.
- 여백: 2px 격자로 정리 (3·5·7·9·11·13·15·17·19px → 짝수) — 42곳.
- 사이드바를 `주 메뉴` / `도움말` 두 그룹으로 나누고 `aria-current="page"` 를 붙였습니다.
- 대비: WCAG AA 미달 2건 수정 (`--faint` 2.58:1 → 4.6:1, 공지 보조문 4.24:1 → 4.6:1).
- 용어 통일: 총 예산 / 처리 완료 / 처리 중 / 확정 예정 (띄어쓰기 포함).

---

## 고친 버그

| 증상 | 원인 |
|---|---|
| 등록 버튼 두 번 눌러 2건 생성 | 재렌더로 버튼이 교체되며 잠금이 풀림 → `BUSY` 플래그 + 버튼 비활성 |
| 인폼 카드를 닫으면 다시 못 봄 | `GET /api/groups/<gid>/mail` 추가 + 실적·내역 화면에 `인폼 다시 보기` |
| 계획 수정 시 이미 입력한 실적이 지워짐 | PUT 이 travelers 를 통째로 교체 → 사번 기준으로 `a_*` 이식 |
| 안내 HTML 을 파일로 열면 한글이 깨짐 | doctype·charset 누락 (Flask 헤더가 가리고 있었음) |
| 금액에 소수점 입력 시 10배 (`70000.4` → `700004`) | 숫자 파싱이 `.` 를 제거 → `numFrom()` 로 통일 |
| `Infinity` · `/api/audit?n=abc` 등에서 500 | `num()` NaN/∞ 방어, `_qint()` 추가 |
| travelers 가 리스트가 아니면 500 | 정규화가 검증보다 먼저 실행 → `_tlist()` 3곳 + `normalize_group` 강화 |
| 텍스트 칸에 dict/list 가 들어가면 CSV·엑셀 내보내기 전부 실패 | `_txt()` 정규화 + 정렬 키 방어 |
| 센터 제출 리포트가 표 없이 뭉개짐 | 대시보드 KPI 제거 시 리포트가 쓰던 `.kpis`/`.kpi` CSS 까지 삭제 → 복원 |
| **사이드바가 본문 위로 밀려 2열이 무너짐** | `<nav class="nav">` 를 `</div>` 로 닫아 브라우저 파서가 `aside` 와 `.app` 까지 함께 닫음 |

마지막 항목은 DOM·기능·접근성 테스트를 모두 통과했습니다 — 좌표를 직접 재는 레이아웃 테스트와
배포 HTML 태그 짝 검사를 새로 넣어 재발을 막았습니다.

### 테스트에서 운영 데이터가 지워지던 문제

`test_api.py` 가 `servera/travelbudget/data_json` 을 직접 지우고 `TB_DATA_DIR` 를 상속했습니다.
실행 즉시 임시 폴더로 격리하고 경로를 assert 로 확인하도록 고쳤으며,
**운영 서버 점검용으로는 읽기만 하는 `smoke_test.py`(19개 항목)** 를 따로 만들었습니다.

---

## 검증

| 항목 | 결과 |
|---|---|
| API 통합 | 309 / 309 |
| 브라우저 E2E | 123 / 123 |
| 정적판(Pages) E2E | 23 / 23 |
| 붙여넣기 파서 | 49 / 49 |
| 퍼징 (1,302회 호출) | 500·예외 **0건** |
| WCAG AA 대비 | 미달 **0건** |
| 글꼴 스택 | 1종 |
| 렌더된 글씨 크기 | 7종 |

---

## 바꿀 파일 (`servera/travelbudget/` 안 5개)

| 파일 | 바뀐 이유 |
|---|---|
| `templates/index.html` | 글꼴·팔레트·여백·막대 CSS·일괄 등록 메뉴·사이드바 구조 |
| `static/app.js` | 대시보드 도식화·비목 막대·드래그 인폼·일괄 등록·안내 요약 |
| `templates/traveler_guide.html` | 안내 4단계(드래그 방식)·doctype/charset |
| `core.py` | 비목 집계·구성비 분모·nTrips·엑셀 양식·입력 방어·버전 |
| `routes.py` | 인폼 재조회·양식 내려받기·입력 방어 |

`store.py` 는 **바뀌지 않았습니다** — 저장 로직·스키마 무변경.

---

# v10.1 (2026-07-30) — 단계 음영 팔레트

## 왜 바꿨나

처리 완료 = 녹색, 처리 중 = 주황, 확정 예정 = 파랑 이었습니다.
**이 셋은 서로 다른 범주가 아니라 한 파이프라인의 단계**인데, 무관한 세 색을 쓰면
"녹색이 좋은 건가? 주황이 경고인가?" 를 매번 해석해야 합니다.
순서가 있는 데이터에 범주형 팔레트를 쓴 것이라 바꿨습니다.

## 원칙

> **단계는 진하기로, 좋다/나쁘다는 색으로. 둘을 섞지 않는다.**

| 단계 | 값 | 의미 |
|---|---|---|
| 계획(잠정) | `--s4` #9FBBD8 (흰 바탕 + 테두리) | 예산 미반영 |
| 확정 예정 | `--s3` #6892BE (옅은 바탕) | 예산 선확보 |
| 처리 중 | `--s2` #35618F (중간 채움) | 실적·인폼 / 소재 이관 |
| 처리 완료 | `--s1` #12304F (진한 채움) | 정산 끝 — 이미 나간 돈 |
| 가용 잔여 | `--track` #E9EEF4 | 아직 안 쓴 돈 |

인접 단계 대비 **2.09 / 1.98 / 1.70**, 배지 글씨 대비 **최소 6.45** (전부 AA 통과).

신호색은 단계와 겹치지 않게 셋만 남겼습니다.

| 신호 | 쓰는 곳 |
|---|---|
| 녹색 `--green` | 정상 운영 램프 · 계획 대비 절감 · 예산 증액 |
| 황색 `--amber` | 대기 경과 D+3~6 |
| 적색 `--red` | 예산 초과 · 취소 · 긴급 · 보류 |

## 적용 범위

화면 전체 — 대시보드 막대·범례, 출장 내역·이관 관리의 상태 배지, 이용 안내 흐름 pill,
출장자용 안내(`traveler_guide.html`)까지 같은 램프를 씁니다.
어느 화면에서든 **처리 완료는 항상 같은 색**입니다.

## 함께 바뀐 것

- **잠정 계획 = 별도 참고 막대.** 예산 막대에 섞으면 가용 잔여에서 차감된 것처럼 읽히므로,
  같은 축·같은 폭의 옅은 막대를 아래에 따로 그립니다. 확정하면 얼마나 차지할지 눈으로 보입니다.
- **비목 구성 = 단색 4줄.** 비목은 단계가 아니라 범주입니다. 색으로 나누면 단계 음영과
  헷갈리므로 전부 같은 색으로 두고 이름과 길이로만 구분합니다.
- **역할 칩(담당자/예산 담당자/소재 담당자) = 중립 회색.** 역할은 단계가 아닙니다.
- **`소재 이관` 배지 등급 수정** — 출장자용 안내에서만 기본칩이라 진행이 뒤로 돌아간 것처럼
  보였습니다. 화면의 `stClass` 와 일치시키고 6단계 전부를 테스트로 고정했습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| E2E §20 | 배지 4단계가 실제로 점점 진해지는가 (렌더된 명도 측정) |
| E2E §20 | 4단계의 색상(hue) 편차 25도 미만 — 무관한 범주로 안 읽히게 |
| E2E §20 | 단계 배지에 신호색(녹·황·적)이 섞이지 않았는가 |
| E2E §20 | 배지 글씨 대비 전부 4.5 이상 |
| E2E §20 | 예산 막대 4칸도 같은 램프·같은 방향인가 |
| E2E §19 | 비목 막대가 단색인가 (단계 음영과 혼동 금지) |
| test_api | 단계를 그리는 CSS 선언에 `--green`/`--amber`/`#8A5A10` 등이 없는가 |
| test_api | 안내 문서의 배지 등급 6종이 화면 `stClass` 와 같은가 |

검증: **API 331 / 브라우저 E2E 132 / Pages 23 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 31조합 미달 0건 / 글씨 크기 6종 / 폰트 스택 1종.**
`data.json` 스키마·`store.py` 무변경.

---

# v10.2 (2026-07-30) — 입력 칸 겹침 수정 · 여백 척도 통일

## 고친 버그 — 계획 등록에서 칸이 겹침

출장자 표의 **첫 칸(성명)이 고정(sticky)** 이었습니다. 표가 가로로 스크롤되면
고정된 칸이 뒤 칸 **위로 올라와 사번·직책이 성명 밑으로 사라졌습니다** (최대 72px 침범).

| 화면 폭 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 1920 · 1440 | 정상 | 정상 |
| 1280 | 14px 침범 | 정상 · 가로 스크롤 없음 |
| 1152 | **48px 침범** | 정상 · 가로 스크롤 없음 |
| 1024 | **72px 침범** | 정상 · 겹침 없이 스크롤 |

세 가지를 함께 고쳤습니다.

1. **고정 칸 제거.** 행이 1~4개뿐이라 고정할 이유가 없고, 겹침의 직접 원인이었습니다.
2. **비용 4열을 그룹 머리로 묶음.** 열 폭이 `계획 식대&잡비` 같은 긴 머리글에 끌려다녔습니다.
   `계획 비용` 을 위에 한 줄 두고 아래를 `교통비 / 숙박비 / 식대&잡비 / 기타` 로 줄였습니다.
3. **`CCG No.` 전용 열 삭제.** 팀을 고르면 자동으로 채워지는 읽기 전용 값이라 입력할 것이 없는데
   열 하나를 차지했습니다. 팀 칸 안 캡션(`CCG C1202`)으로 옮겼습니다. 저장값은 그대로입니다.

표 최소 폭 **978px → 820px**. 1152px 이상에서 가로 스크롤 없이 들어갑니다.

## 금액칸 크기 통일

실적 표는 열이 8개뿐이라 금액칸이 **195px** 까지 늘어났습니다 (계획 표는 110px).
같은 성격의 칸이 화면마다 다른 크기였습니다. 상한을 걸어 두 표를 맞췄습니다.
남는 폭은 칸을 늘리지 않고 여백으로 둡니다 — 오른쪽 정렬이라 숫자 열은 그대로 맞습니다.

## 여백 척도

**4 · 8 · 12 · 16 · 24 다섯 단계만** 씁니다. (1·2px 은 선·미세보정, 48px 은 본문 하단 여유)

| 단계 | 쓰는 곳 |
|---|---|
| 4 | 라벨과 입력 사이 |
| 8 | 한 덩어리 안 (버튼 사이, 범례 항목 사이) |
| 12 | 칸과 칸 사이 |
| 16 | 블록과 블록 사이 (카드 안쪽 여백, 카드 간격) |
| 24 | 큰 구획 |

`index.html` 60곳 + `traveler_guide.html` 32곳을 스냅했습니다.
6·10·14·18·20·22·28px 이 사라져 화면마다 리듬이 어긋나던 것이 정리됐습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| E2E §21 | 1920·1440·1280·1152·1024px 에서 **행 안 칸끼리 좌표가 겹치는지 직접 측정** |
| E2E §21 | 1152px 이상에서 가로 스크롤이 생기지 않는가 |
| E2E §21 | 실적 표도 같은 기준으로 겹침 없는가 |
| E2E §21 | CCG 코드 자동 채움이 유지되는가 (열은 없애되 값은 그대로) |
| E2E §21 | 계획·실적 금액칸 폭이 서로 어긋나지 않는가 |
| test_api | 배포 HTML 의 모든 margin·padding·gap 이 척도 안에 있는가 |
| test_api | 첫 칸 고정(sticky)이 다시 들어오지 않았는가 |

겹침은 CSS 단언으로는 잡히지 않습니다 — 실제로 렌더된 좌표를 재야 잡힙니다.

검증: **API 335 / 브라우저 E2E 146 / Pages 23 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 31조합 미달 0건.** `data.json` 스키마·`store.py` 무변경.

---

# v10.3 (2026-07-30) — 무인증 취소 구멍 · 막대 축 · 큐

## 보안 — 인증 없이 실적 든 출장을 취소할 수 있었습니다

```
POST /api/groups/<gid>/status  {"status":"취소"}      ← 관리자 비밀번호 없음
→ HTTP 200 (실적 285,000원 건이 취소됨)
```

`routes.py` 의 게이트가 `locked(g)` 만 봤는데, `locked()` 는 **출장자 중 한 명이라도
이관·완료·보류일 때만** True 입니다. 실적만 넣고 아직 아무도 이관되지 않은
`실적 입력·인폼` 그룹은 게이트를 그냥 통과했습니다.

그 틈으로 취소하면 **정산금이 '처리 중' 집계에서 통째로 빠지고 가용 잔여가 그만큼 늘어난
것처럼** 보입니다. 예산 원장에서 돈이 조용히 사라지는 경로였습니다.

수정 후 (전부 실측):

| 경우 | 결과 |
|---|---|
| 무인증 · 실적 든 건 취소 | **401** |
| 무인증 · 소재 이관 건 취소 | 401 (기존과 동일) |
| 무인증 · 잠정 계획 취소 | **200** (출장자 본인의 정상 동작 — 유지) |
| 무인증 · 확정 예정 취소 | **200** (유지) |
| 관리자 · 실적 든 건 취소 | **200** |

`tools/tb_local.js` 정적 미러도 같은 규칙으로 동기화했습니다.

## 막대가 라벨과 반대 말을 하고 있었습니다

```js
const peak = Math.max(...);            // 최댓값 정규화
<i style="width:${v / peak * 100}%">
```

막대는 **최댓값 기준**인데 옆의 라벨은 **합계 대비 구성비**였습니다.
그래서 `Chemical 659,000 34%` 옆의 막대가 가로를 꽉 채웠습니다. 비목도 `교통비 35%` 인데 100%.

"교통비가 예산을 다 썼나"로 읽힙니다. 분모를 표 합계로 바꿔 라벨과 일치시켰습니다.
(총예산 기준은 쓰지 않습니다 — 최대 팀이 6%라 막대가 전부 슬라이버가 됩니다)

## '바로 할 일'을 건수에서 큐로

서버는 `group_id` 배열을 정확히 보내는데(`core.dash().todo`) 화면은 `.length` 만 쓰고 버렸습니다.
그래서 `실적 입력 대기 1건` 을 눌러 가면 드롭다운엔 4건이 있고, **어느 1건인지 화면 어디에도
없었습니다.** 3일 지난 건과 40일 지난 건도 똑같이 "1건"이었습니다.

```
이관·처리   화성 동진쎄미켐 이민호        D+23   [처리 관리 →]
이관·처리   성남 케이씨텍 정다은          D+22   [처리 관리 →]
실적 대기   구미 SK실트론 한지우, 오세훈    D+4   [실적 입력 →]
```

복귀일 기준 오래 묵은 순, 상위 6건. 버튼을 누르면 **그 건이 선택된 채로** 도착합니다
(`openActual` / `openProcess`). 배지 숫자와 도착 화면 숫자가 어긋날 여지가 없어졌습니다.

## 목록에서 상태 배지가 머리행과 중복됐습니다

`프로세스별 묶기` 를 켜면 `처리 완료 2건` 머리행 바로 아래 행마다 같은 배지가 또 찍혔습니다.
음영 팔레트로 바꾼 뒤엔 그 배지가 **행에서 가장 무거운 요소**가 되어, 정작 눈으로 찾아야 하는
출장 이름을 눌러버렸습니다. 묶기 모드에서는 행 배지를 빼고, 묶기를 끄면 그대로 표시합니다.

## 램프 재조정

실무에서 제일 헷갈리는 쌍이 "이미 쓴 돈(처리 중)"과 "쓰기로 한 돈(확정 예정)"인데
하필 그 둘의 대비가 가장 좁았습니다.

| 인접 쌍 | 전 | 후 |
|---|---|---|
| 처리 완료 ↔ 처리 중 | 2.09 | 2.05 |
| **처리 중 ↔ 확정 예정** | **1.98** | **2.51** |
| 확정 예정 ↔ 가용 잔여 | 2.79 | 2.24 |
| **사슬 최소** | **1.98** | **2.05** |

`--s3` 를 `#6892BE → #7BA4CE` 로 올렸습니다. 브랜드 네이비(`--s1`)는 그대로입니다.
세그먼트 경계선도 1px → 2px — 같은 색상 음영이라 경계선이 구분의 절반을 담당합니다.

> 한계도 적어둡니다. 한 색상 안에서 3단계 + 트랙을 나누는 이상 인접 대비 상한은 약 2.5입니다.
> 더 벌리려면 가장 진한 칸을 거의 검정으로 내려야 하는데 브랜드색을 잃습니다.
> 스택 막대 안에서 세 단계를 갈라 보는 건 녹·황·청보다 여전히 어렵고, 이건 감수한 대가입니다.
> 대신 흑백 인쇄와 색각 이상에서는 명백히 낫습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api 13-2b | 실적 든 건 무인증 취소 401 · 처리 중 금액 보존 · 관리자는 가능 |
| test_api 13-2b | 잠정·확정 취소는 계속 무인증 200 (막아서는 안 되는 것) |
| test_api 13-2b | 정적 미러도 같은 게이트 보유 |
| E2E §22 | 막대 폭과 % 라벨이 1.2%p 이내로 일치 · 100% 초과 없음 |
| E2E §22 | 큐 각 행이 특정 group_id 로 이동 · 경과일 표기 · 오래된 순 정렬 |
| E2E §22 | 큐 → 실적 입력 딥링크로 그 건이 실제로 선택되는가 |
| E2E §22 | 묶기 모드 행 배지 중복 0 · 묶기 해제 시 행마다 배지 |
| E2E §22 | 사슬 인접 대비 2.0 이상 · 처리중↔확정이 가장 넓음 |

검증: **API 342 / 브라우저 E2E 160 / Pages 23 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 33조합 미달 0건.** `data.json` 스키마·`store.py` 무변경.

---

# v10.4 (2026-07-30) — 조용히 틀리는 값 · 막다른 길 · 거짓 표시

워크플로가 올린 후보 28건을 하나씩 재현해 **진짜인 것만** 고쳤습니다.
(그중 하나는 이미 v10.2에서 해결된 것을 미적용으로 잘못 본 것이었습니다)

## 조용히 틀리는 값 — 아무도 못 잡는 종류

| 증상 | 원인 |
|---|---|
| 동행자 입력 중 Enter 한 번에 **덜 채운 계획이 등록**되거나 실적이 저장되고 인폼 메일까지 발행 | Enter 제출 핸들러가 모달·검색칸만 제외. 표는 칸이 여러 개라 '지금까지 친 것' ≠ '다 친 것' |
| 붙여넣은 20건이 **전부 `정기 Audit`** 으로 센터 CSV 에 나감 | 빈 출장구분의 기본값이 `kinds[2]` = 정기 Audit. 그럴듯해서 검출 불가 → `기타` 로 |
| 열 이름을 `목적` 으로 줄여 쓴 시트는 **전 행이 '목적&사유 없음'** | 별칭 표가 `'목적':'목적'` 로 자기 자신을 가리켜 그 열이 통째로 무시됨 |
| 머리글 없이 일부 열만 붙이면 **날짜를 CCG명으로 읽음** | 27필드 순서로 가정. 열 수가 모자라면 멈추고 머리글을 요청하도록 |

## 막다른 길 · 백지

- **data.json 이 깨지면 화면 전체가 백지** 였습니다. `document.body.innerHTML` 을 통째로
  덮어써 사이드바·연락처까지 사라지고 빨간 한 줄만 남았습니다. 화면이 안 뜨는 상황일수록
  연락처가 필요합니다 — 셸을 유지하고 **Ctrl+F5 → 서버 확인 → 백업 복원** 순서를 안내합니다.
- **실적 입력 대상이 0건일 때** 0개짜리 목록을 검색하라는 검색창과 빈 드롭다운만 남았습니다.
  → 왜 없는지와 다음 행동(계획 등록 / 내역 확인)을 제시합니다.
- **목록이 비면 항상 "조건에 맞는 출장이 없습니다"** — 필터를 건 적도 없는데 필터 탓으로
  돌렸습니다. 이제 `이번 분기에 없음` 과 `필터 때문에 0건` 을 구분하고 각각 다음 행동을 줍니다.
- **처리 관리가 비면** 회색 한 줄. → 실적이 입력된 건만 온다는 설명 + 이동 버튼.
- **인폼 카드(position:fixed)가 화면 아래 버튼을 영구히 덮었습니다.** 접기 버튼이 있어도
  카드가 열린 동안은 그 아래를 스크롤로 빼낼 수 없습니다. 카드 높이만큼 본문 아래 여유를
  줘서 어떤 버튼도 손이 닿지 않는 상태가 되지 않게 했습니다.
  (Pages 테스트가 클릭 차단으로 먼저 잡아냈습니다 — 테스트가 아니라 화면을 고쳤습니다)

## 거짓 · 중복 표시

| 항목 | 전 | 후 |
|---|---|---|
| 대시보드 부제 공식 | `잔여 = 총 예산 − 처리 완료 − 처리 중` | `가용 잔여 = … − 확정 예정` (hero·안내와 일치) |
| 예산 미배정 분기 | 초록 램프 + **"정상 운영 중"** | 중립 회색 + `예산 배정하기 →` |
| 빈 분기 안내문 | `잠정 계획 0원 (0건) 은 …` | 숨김 |
| 빈 CCG 카드 | "없다" 3번 + 0 여섯 칸 | 한 줄 |
| 취소 확인창 | "되돌리려면 예산 담당자 모드가 필요합니다" (실제로 불가) | "화면에서 되돌리는 방법은 없습니다 — 백업 복원이 필요합니다" |
| 검색 안내문구 | `…·목적·전표번호` (검색 대상에 없음) | `…·목적` |
| 부분처리 태그 | 1인 인폼 건에 `(인폼 1)` — 같은 행 배지의 축어 반복 | 상태가 섞였거나 보류가 있을 때만 |
| 정렬 화살표 | 상태를 눌렀는데 뒤집히는 건 기간 | ▲▼ 는 실제로 뒤집히는 열에, 상태에는 `⋯`(순서 고정) |
| 컬럼 검색칸 안내문구 | 대비 1.87:1 | 5.67:1 |
| 묶기 체크박스 | `.filter-row input{min-width:200px}` 이 걸려 라벨이 190px 떨어짐 | 체크박스는 제외 |
| CCG·비목 금액 열 | 오른쪽 끝에 맞는 건 `%` 라 금액 끝이 최대 15px 밀림 | 구성비를 고정폭 칸에 |
| 처리 관리 순서 | 정렬 없음 (방치 일수를 계산해 색까지 칠하고 순서엔 안 씀) | 보류 → 오래 묵은 순, 완료는 아래 |
| 실적 드롭다운 | `대상 출장 선택 (4건)` — 대시보드 '1건'과 어긋남 | `복귀함 · 실적 대기 (N건)` / `예정 · 작성 중 (N건)` 두 묶음 |
| 첫 화면 | 부제가 없다가 메뉴를 누르는 순간 생겨 헤더가 밀림 | `load()` 끝에 `nav(VIEW)` |

## 재발 방지 (E2E §23 · test_api)

표 안 Enter 무제출 · 일괄 `목적` 별칭 · 빈 출장구분 `기타` · 머리글 없는 일부 열 정지 ·
예산 미배정 중립(초록 램프 없음·CTA 있음·0원 안내문 없음) · 목록 빈 화면 구분 ·
정렬 화살표 위치 · 체크박스 폭 · 금액 열 정렬(픽셀) · 처리 완료 맨 아래 ·
인폼 카드 여유 확보/원복 · 취소 안내 문구 · 부제 공식 일치 · 부팅 실패 시 셸 유지.

검증: **API 350 / 브라우저 E2E 181 / Pages 23 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 33조합 미달 0건.** `data.json` 스키마·`store.py` 무변경.

---

# v10.5 (2026-07-30) — 글자가 우그러지던 원인 · 문장 다이어트

"글씨체가 안 이쁘고 글자가 약간 우그러진다"를 실측으로 쪼갰습니다. 원인이 셋이었습니다.

## 1. 줄높이가 전부 소수점이었습니다 — 이게 주범

`line-height:1.55` 를 배수로 쓰고 있었습니다. 크기를 곱하면 전부 소수가 됩니다.

| 크기 | 배수 계산 결과 | 고친 값 |
|---|---|---|
| 12px | **18.6px** | 18px |
| 13px | **20.15px** | 20px |
| 14px | **21.7px** | 22px |
| 16px | **24.8px** | 24px |
| 20px | **31px** | 28px |

소수 줄높이는 베이스라인을 서브픽셀에 걸고, 브라우저가 줄마다 다르게 반올림합니다.
그래서 같은 문단 안에서도 줄마다 글자가 다르게 뭉개집니다.

**측정: 소수점 크기·줄높이 98건 → 0건.**

## 2. 굵기를 5종 선언했는데 화면엔 2종만 나왔습니다

맑은 고딕은 **Regular(400)·Bold(700) 둘뿐**입니다. 실측하면:

```
300 · 400 · 500  →  전부 같은 폭 (400 으로 렌더)
600 · 700 · 800 · 900  →  전부 같은 폭 (700 으로 렌더)
```

CSS 는 400/500/600/700/800 다섯 단계로 위계를 만들려 했지만 화면에는 두 단계뿐이었습니다.
그래서 위계가 흐릿했고, 부족한 만큼을 색과 곁가지 문구로 메우고 있었습니다.

**선언을 실제 렌더와 일치시켰습니다 — 400 / 700 두 종만.**
보조 요소(범례·메뉴 배지)는 600→700 으로 올라가며 오히려 무거워져서 400 으로 내렸습니다.

## 3. 11px 한글

맑은 고딕은 11px 에서 한글 획이 붙습니다. **28곳을 12px 로 올렸습니다.**
한글에 주던 양수 자간(`letter-spacing:.04em`)도 제거했습니다 — 한글은 기본 자간이 이미 맞습니다.

## 문장 다이어트

| 화면 | 45자 넘는 줄 | 괄호 | 줄표 |
|---|---|---|---|
| 대시보드 | 2 → **1** | 4 → 2 | 1 → 1 |
| 계획 등록 | 1 → **0** | 2 → 2 | 1 → **0** |
| 출장 내역 | 3 → **2** | 6 → 4 | 1 → **0** |

가장 심했던 106자 한 줄:

> ‘계획(잠정)’은 참고용 리스트 — 실제로 안 가면 삭제(흔적 없이 사라짐). 실제로 갈 건
> ‘출장 확정’ 하면 계획 금액만큼 예산이 미리 확보됩니다. (확정 이후 취소는 기록으로 남습니다)

→

> 계획(잠정)은 참고용이라 예산에 잡히지 않습니다.
> 실제로 가는 건만 출장 확정을 누르면 계획 금액만큼 예산이 확보됩니다.

대시보드에 남은 72자 한 줄은 **담당자가 직접 쓴 공지 문구**라 건드리지 않았습니다.
(화면에서 언제든 고칠 수 있습니다)

## 글꼴을 바꾸고 싶을 때 — 파일만 넣으면 됩니다

맑은 고딕의 한계(굵기 2종, 작은 크기에서 뭉개짐)는 규율로 줄일 수는 있어도 없앨 수는 없습니다.
근본 해결은 본문용으로 설계된 글꼴을 쓰는 것인데, **외부 CDN 없이** 가능합니다.

```
servera/travelbudget/static/fonts/ui-400.woff2
servera/travelbudget/static/fonts/ui-700.woff2
```

이 두 파일을 넣으면 자동으로 적용됩니다. 없으면 맑은 고딕으로 그대로 돌아갑니다.
파일이 **있을 때만** `@font-face` 를 내보내므로, 없는데 404 가 매 로드마다 찍히는 일도 없습니다.
자세한 건 `static/fonts/README.txt` — 라이선스와 사내 폰트 정책은 반드시 먼저 확인하세요.

## 재발 방지 — 선언이 아니라 렌더 결과를 잽니다

| 테스트 | 검사 |
|---|---|
| E2E §22b | 렌더된 크기·줄높이에 소수점 0건 |
| E2E §22b | 11px 이하 한글 없음 |
| E2E §22b | 실제로 렌더되는 굵기 2종 이하 |
| test_api | 배수 줄높이 선언 없음 · 크기↔줄높이 짝 고정 |
| test_api | `font-weight` 는 400·700 만 · 최소 12px · `letter-spacing` 미사용 |
| test_api | 글꼴 스택에 외부 URL 없음 · 맑은 고딕이 시스템 글꼴 중 첫째 |

검증: **API 370 / 브라우저 E2E 185 / Pages 23 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 27조합 미달 0건 / 렌더 크기 5종 / 폰트 스택 1종.** `data.json` 스키마·`store.py` 무변경.

---

# v10.6 (2026-07-31) — 실집행 기준 집계 · 비목별 내역 열람

## 1. 부서별 구분을 실집행(처리 중 + 처리 완료) 기준으로

확정 예정은 **아직 안 쓴 돈**인데 합계·구성비·정렬에 섞여 있었습니다.
인원은 더 심해서 **잠정 계획 단계 사람까지** 세고 있었습니다.

| | 전 | 후 |
|---|---|---|
| 부서별 합계·구성비 | 완료+처리중+**확정** | 완료+처리중 |
| 부서별 인원 | 잠정·확정 포함 **9명** | 실집행 **6명** |
| 카드 하단 건수 | 전체 **7건** | 실집행 **5건** |
| 비목 구성 | 확정의 계획비 섞임 | 실집행만 (합계가 위 막대와 일치) |

확정 예정은 **사선 흐린 막대**로 뒤에 덧붙여 "앞으로 들어올 것"으로만 읽히게 했습니다.
실집행이 없고 확정만 있는 팀은 금액·구성비 대신 `– 예정` 으로 표시하고 아래로 내립니다.
범례에도 `확정 예정 · 집계 제외` 를 명시했습니다.

## 2. 출장 내역에서 비목별 실적을 바로 볼 수 있습니다 — 가장 중요한 변경

이관·정산 때 사내 출장 정산서를 쓰려면 **출장자별 교통비·숙박비·식대·기타**가 필요한데,
그 정보가 `출장 실적 입력` 화면에만 있었습니다. 그래서 담당자가 확인하려면
**되돌리기 → 수정** 으로 들어가야 했습니다 — 보려고 상태를 건드려야 했던 겁니다.

이제 `출장 내역` 각 행의 **내역 ▼** 버튼으로 바로 펼칩니다.

```
성명    사번      CCG팀              교통비  숙박비  식대&잡비  기타   실적 합계  계획 대비  처리 상태
이수진  20180915  Chemical 소재팀 C1101  88,000  90,000    75,000  18,000   271,000   −14,000   실적 입력·인폼
합계 1명                              88,000  90,000    75,000  18,000   271,000
```

- **읽기 전용** — 상태도 금액도 바뀌지 않습니다 (테스트로 고정)
- `표 복사` — 탭 구분 텍스트라 엑셀 정산서에 그대로 붙습니다
- 실적이 없는 건은 계획값을, 있으면 실적값을 보여줍니다
- 비고도 함께 표시 (정산 사유를 적어둔 경우가 많습니다)
- 펼친 행은 필터·정렬·검색을 해도 유지됩니다

**이관·처리 관리** 화면에도 같은 표를 넣었습니다 — `비목별 내역 보기`.
이관 결재를 올리는 그 화면에서 바로 정산서를 쓸 수 있습니다.
(전에는 개인별 **총액**만 보여줘서 비목을 알 수 없었습니다)

## 재발 방지

| 테스트 | 검사 |
|---|---|
| E2E §24 | 부서별 인원 = 실집행 인원 · 실집행 ≤ 전체 |
| E2E §24 | CCG 구성비 합 100% · 비목 합계 = 실집행 합계 |
| E2E §24 | 확정만 있는 팀은 `예정` 표기 · 구성비 없음 · 사선 세그먼트 |
| E2E §24 | 내역에 비목 4종 + 성명·사번·CCG팀 + 합계 행 + 표 복사 |
| E2E §24 | **내역을 봐도 상태·금액이 그대로** (되돌리기 없이 열람) |
| E2E §24 | 이관·처리 관리에도 비목별 내역·표 복사 |
| test_api | 부서별 인원 = `nUsedPeople` · 참여 건수 중복 허용 · 구성비 100% |
| Pages E2E | 정적 미러도 같은 축 (실집행 건수·인원 제공) |

검증: **API 374 / 브라우저 E2E 199 / Pages 25 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
대비 27조합 미달 0건.** `data.json` 스키마·`store.py` 무변경.

---

# v10.7 (2026-07-31) — 안내서에 실제 화면 넣기

## 글로만 설명하면 어느 칸인지 못 찾습니다

출장자용 안내서에 **단계별 실제 화면 캡처 5장**을 넣었습니다.

| 단계 | 넣은 화면 |
|---|---|
| ① 출장 계획 등록 | 실제로 채운 등록 폼 (공통 정보 + 출장자 표 + 계획 비용) |
| ② 출장 확정 | 등록 화면 맨 아래 체크박스 + 등록 버튼 |
| ③ 실적 입력 | 계획액이 흐리게 보이고 차액이 뜨는 실적 표 |
| ④ 표를 메일로 | 화면 오른쪽 아래 인폼 카드 |
| 상태 보는 법 | 출장 내역 — 상태별로 묶인 목록과 배지 |

맨 위에 **4단계 목차**를 넣어 몇 단계인지부터 보이게 하고, 각 단계는 번호 원 + 박스로
구획을 나눴습니다. 설명과 화면 사이에는 점선을 두어 "여기부터 실제 화면"이 구분됩니다.

## 파일 하나로 끝납니다

캡처는 **HTML 안에 data: URI 로 embed** 했습니다.

- 외부 이미지 파일을 부르지 않으므로 **메일로 HTML 하나만 보내도** 그대로 보입니다
- 사내망 차단·외부 CDN 과 무관합니다
- 인쇄할 때 캡처가 단계 중간에서 잘리지 않게 `break-inside:avoid`

용량은 팔레트 축소로 **원본 317KB → 76KB**(base64 포함 안내서 전체 119KB)까지 줄였습니다.
품질 손실은 눈으로 확인되지 않는 수준입니다.

## 화면이 바뀌면 캡처도 바뀌어야 합니다

낡은 캡처는 없느니만 못합니다. 한 명령으로 다시 만듭니다.

```bash
pip install pillow                       # 최초 1회
NODE_BIN=node PLAYWRIGHT_PATH=<경로> python3 tools/build_guide.py
```

- 임시 원장으로 화면을 띄우므로 **운영 데이터를 쓰지 않습니다**
- 화면이 안 바뀌었으면 **결과 파일도 안 바뀝니다** (md5 동일 확인)
- `<figure data-shot="키">` 자리를 찾아 이미지만 갈아끼우므로 설명·구조는 보존됩니다
- 300KB 를 넘으면 경고 (메일 첨부가 불편해지는 선)

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 캡처 5장 · 전부 data: URI · figcaption · alt · 재생성 키(data-shot) |
| test_api | 맨 위 4단계 목차 · 목차 이름이 본문 단계와 일치 |
| test_api | 인쇄 시 분할 방지 · 안내서 300KB 이하 |
| test_api | 외부 참조 0건 — base64 안의 우연한 문자열이 아니라 **실제 src/href** 만 검사 |

검증: **API 387 / 브라우저 E2E 199 / Pages 25 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건.**
`data.json` 스키마·`store.py` 무변경.

---

# v10.8 (2026-07-31) — 시스템 설정: 코드 고치지 않고 화면에서

> "추가로 우리가 세팅해야하는 걸 관리자 메뉴 내 만들고 조정할 수 있게끔 해보는 건 어때?
> data json 말고 다른 곳에 있어서 불가능할 수도 있어서 물어봐. CCG, 메일 수신인 등등"

## 먼저, 물어보신 것에 대한 답

**대부분은 이미 `data.json` 안에 있었습니다. 딱 하나 CCG만 코드에 박혀 있었습니다.**

| 항목 | v10.7 까지 있던 곳 | v10.8 |
|---|---|---|
| 인폼 수신자 | `data.json` → `settings.mail_recipients` | 시스템 설정에서 편집 |
| 시스템 이름 | `data.json` → `settings.system_name` | 시스템 설정에서 편집 |
| 참조 주소 | `data.json` → `settings.reference_url` | 시스템 설정에서 편집 |
| 예산 담당자 비밀번호 | `data.json` → `settings.admin_pw` | 시스템 설정에서 편집 |
| 공지 문구 · 부연 | `data.json` → `settings.notice` / `notice_sub` | 이미 대시보드에서 편집 중 (그대로 둠) |
| **CCG 팀 ↔ 코드** | **`core.py` 코드에 박힘** | **`settings.ccg_teams` 로 이동 — 시스템 설정에서 편집** |

즉 지금까지는 담당자가 바뀌거나 CCG가 개편될 때마다 **저에게 요청 → 파일 교체 → 재기동**이
필요했는데, 이제 화면에서 끝납니다. CCG만 코드에 있던 것이라 그것만 옮겼습니다.

## 좌측 메뉴 `시스템 설정` (예산 담당자 인증)

한 화면에서 네 묶음을 고칩니다.

| 묶음 | 내용 |
|---|---|
| 인폼 기본 수신자 | 메일 주소 행 추가/삭제 (최대 10명) |
| CCG 팀 ↔ 코드 | 팀명·코드 행 추가/삭제 (최대 40행) |
| 화면 문구 | 시스템 이름 · 참조 주소 (인폼 하단 `** 참고 :` 줄) |
| 예산 담당자 비밀번호 | 4자 이상. 바꾸면 **모든 담당자가 새 번호를 써야 한다**고 화면에 경고 |

대시보드 공지는 지금도 **`안내 문구 수정`** 버튼으로 그 자리에서 고칩니다 — 보이는 곳에서
바로 고치는 편이 나아 설정 화면으로 옮기지 않았습니다.

## 과거 데이터를 건드리지 않는 설계

CCG를 설정으로 옮길 때 가장 위험한 건 **과거 원장이 흔들리는 것**입니다. 세 겹으로 막았습니다.

1. **원장은 등록 시점 코드를 그대로 보관합니다.** 설정에서 코드를 바꿔도 과거 출장의
   `ccg` 값은 그대로입니다 — 지난 분기 집계가 소급해서 바뀌지 않습니다.
2. **쓰고 있는 코드는 삭제 불가.** 각 행에 `사용 중 N건` 을 표시하고, 1건이라도 쓰였으면
   삭제 버튼이 잠깁니다. 화면에서만이 아니라 **서버가 거부**합니다(400).
3. **설정이 비어 있거나 깨져 있으면 코드 기본값으로 되돌아갑니다.** `settings.ccg_teams` 가
   없거나 형식이 잘못됐으면 `core.CCG_TEAMS` 를 씁니다 — 설정 사고로 화면이 죽지 않습니다.

## 서버가 거부하는 것

화면 검증만으로는 부족합니다. 아래는 전부 **서버**에서 막습니다.

| 입력 | 결과 |
|---|---|
| 메일 형식 아님 · 중복 · 11명 이상 | 400 |
| 팀명 중복 · 코드 중복 · 41행 이상 · 빈 값 | 400 |
| 원장에서 쓰고 있는 CCG 코드 삭제 | 400 (몇 건이 쓰고 있는지 함께 알려줌) |
| 비밀번호 4자 미만 · 공백 포함 | 400 |
| 인증 없이 설정 **읽기** | 401 — 비밀번호가 들어 있으므로 조회도 막습니다 |
| 인증 없이 설정 **쓰기** | 401 |

**안 보낸 항목은 "지우라"가 아니라 "그대로 두라"** 입니다. 화면은 항상 전부 보내지만,
CCG만 고치는 부분 저장이 메일 수신자를 날려서는 안 됩니다. 반대로 **빈 배열을 명시적으로**
보내면 그건 지우겠다는 뜻이므로 거부합니다 — 생략과 빈 값을 구분합니다.
(`reference_url` 이 부분 저장에서 날아가던 문제도 같은 규칙으로 고쳤습니다.)
저장은 감사 로그에 남습니다.

## 되돌리기

`settings.ccg_teams` 는 **CCG를 실제로 고쳤을 때만** 생기는 칸입니다. 출장·예산 데이터에는
필드가 하나도 늘지 않았습니다. 이전 버전으로 되돌리면 그 칸을 무시하고 코드 기본값을 쓰므로
오류는 나지 않고, 되돌린 동안만 고친 목록이 반영되지 않습니다.

## 출시 전 점검에서 잡은 버그 2건

설정으로 CCG를 옮기고 나서, **팀 이름을 실제로 바꿔 보는** 시나리오를 돌리다 나왔습니다.
둘 다 v10.8이 새로 만든 경로라 배포 전에 고쳤습니다.

**① 팀 이름을 바꾼 뒤 기존 건을 수정하면 팀 이름이 빈 값으로 저장됨** (심각)

계획 화면의 CCG 드롭다운이 **팀 이름으로** 선택지를 맞추고 있었습니다.
이름을 바꾸면 예전 건을 열었을 때 아무것도 선택되지 않고(`선택`), 그대로 저장하면
`ccg_nm` 이 빈 문자열로 덮였습니다. 코드(`ccg`)는 남아 있어 **검증은 통과** —
즉 오류 없이 조용히 팀 이름만 사라지고, 센터 제출본의 `CCG명` 칸이 비었습니다.

→ 드롭다운을 **CCG 코드로** 맞춥니다. 목록에 없는 코드(폐지된 팀의 과거 건)는
`폐지된 소재팀 (목록에 없음)` 처럼 저장된 값을 선택지로 남겨 지워지지 않게 했습니다.

**② 같은 팀이 화면마다 다른 이름으로 보임**

원장에는 등록 시점의 `ccg_nm` 이 박혀 있어서, 이름을 바꾸면
**대시보드는 새 이름 · 출장 내역과 센터 제출 CSV와 인폼은 옛 이름**이 나왔습니다.

→ 표시용 팀 이름을 **코드에서 파생**하도록 통일했습니다(`normalize_group`).
저장된 코드는 그대로라 **집계는 1원도 안 바뀌고**, 목록에 없는 코드는 저장된 이름을 그대로 씁니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 이름 변경 후 내역·CSV·대시보드·인폼이 **모두 같은 팀 이름** |
| test_api | 이름 변경 후 수정 저장해도 팀 이름이 비지 않음 · 코드 불변 |
| test_api | 목록에 없는 코드는 저장된 이름 유지 |
| test_api | 드롭다운이 이름이 아니라 코드로 선택 |
| test_api | 인증 없이 설정 읽기·쓰기 401 |
| test_api | 메일 형식·중복·개수, 팀명·코드 중복·개수·길이 거부 |
| test_api | 사용 중인 CCG 삭제 거부 · 사용 건수 계산 |
| test_api | 새 팀 등록 후 그 팀으로 출장 등록이 실제로 통과 |
| test_api | 비밀번호 변경 후 `pw_default` 가 꺼져 화면에서 기본 비밀번호 안내가 사라짐 |
| test_api | `settings.ccg_teams` 가 쓰레기 값일 때 기본값 폴백 |
| test_api | 부분 저장 — 한쪽만 보내도 나머지가 유지되고, 빈 배열 명시는 거부 |
| test_api | **저장되는 출장·예산 필드가 v9.4와 동일**(추가 0 · 삭제 0) |
| e2e | 설정 화면은 메뉴 클릭 시 인증을 요구 · 인증 후 다시 그려짐 |
| e2e | 외부 요청 0건 — 콘솔 문자열이 아니라 **실제 네트워크 요청 URL** 로 검사 |

검증: **API 444 / 브라우저 E2E 211 / Pages 25 / 붙여넣기 49 / 퍼징 1,302회 500오류 0건 /
디자인 감사 OK 13 · INFO 2.** `store.py` 무변경.

---

## 아직 받아야 할 것

- **CCG팀 ↔ 코드 새 목록** — 이제 파일 교체 없이 **화면 `시스템 설정` 에서 직접** 넣으시면 됩니다.
  주시면 코드 기본값(`core.CCG_TEAMS`)·예시·문서도 함께 맞추겠습니다.
- **인폼 수신자 메일 주소** (김은정 동명이인) — 마찬가지로 화면에서 바로 고칠 수 있습니다.
  다만 이 주소는 **안내서 4단계 캡처에도 찍혀 있으므로**, 최종 주소가 정해지면
  `tools/build_guide.py` 를 한 번 돌려 캡처를 다시 만들어야 합니다.

---

# v10.9 (2026-07-31) — 실제 조직 CCG·수신인 반영 + 연 단위 개편 도구

## 받은 값을 시스템 전체에 반영했습니다

가안으로 넣어 두었던 CCG 7팀(`Photo 소재팀`/`C1303` 등)과 수신인을 **실제 값**으로 바꿨습니다.
예시 원장·테스트·문서·붙여넣기 샘플·정적판·안내서 캡처까지 **174곳**입니다.

| 팀 이름 | CCG No. |
|---|---|
| 소재전략 | 50110502 |
| P&C소재 | 50128121 |
| Patterning소재기술 | 50119135 |
| Patterning소재개발 | 50077468 |
| C&C소재기술 | 50139632 |
| C&C소재개발 | 50134405 |
| EDTW소재기술 | 50119134 |
| EDTW소재개발 | 50103536 |

인폼 기본 수신인은 **소재 담당자 2명 + 센터 담당자 2명**입니다.

| 구분 | 이름 | 주소 |
|---|---|---|
| 소재 | 이정훈 | junghoon12.lee@sk.com |
| 소재 | 김은정 | eunjeong5.kim@sk.com (동명이인 오류 정정) |
| 센터 | 천지웅 | Jeewoung.Chun@sk.com |
| 센터 | 김건영 | geonyoung.kim@sk.com |

안내서 4단계 캡처도 **다시 찍어** 새 수신인 4명과 새 팀명이 그림 안에 보입니다.

## 그냥 목록만 바꾸면 막힙니다 — 그래서 옮기는 길을 냈습니다

운영 중인 원장에는 **옛 코드가 이미 박혀 있습니다.** 그 코드는 "사용 중"이라
목록에서 지울 수 없습니다(과거 건의 소속이 미아가 되는 것을 막는 안전장치).
즉 **새 목록으로 교체하는 것 자체가 거부됩니다.**

`시스템 설정` 맨 아래에 **`원장 CCG 정리`** 를 넣었습니다.

- 원장에는 있는데 목록에 없는 코드를 **건수와 함께** 보여줍니다
- 각 줄에서 **옮길 팀을 고르고 [옮기기]** — 해당 출장자의 코드·팀 이름이 새 값으로 바뀝니다
- **금액·날짜·상태는 건드리지 않습니다.** 실행 직전 자동 백업 + 감사 로그
- 다 옮기면 옛 팀 행을 지울 수 있습니다

리허설(출장 15건·출장자 28명)에서 7개 코드를 전부 옮겨 봤고,
**처리 완료·처리 중·확정 예정·가용 잔여 네 숫자가 1원도 바뀌지 않았습니다.**

## 앞이 같은 팀 이름 — 붙여넣기가 조용히 틀리던 경로

새 이름에는 `Patterning소재기술`/`Patterning소재개발`, `C&C소재기술`/`C&C소재개발` 처럼
**앞부분이 같은 쌍이 4개** 있습니다. 엑셀 붙여넣기의 팀 인식은 앞글자 매칭을 쓰는데,
먼저 찾은 것을 그냥 쓰고 있었습니다 — `Patterning소재` 만 적힌 행이 **개발 건인데 기술 팀으로**
조용히 들어갈 수 있었습니다.

앞글자 매칭은 **한 팀만 걸릴 때만** 쓰도록 고쳤습니다. 애매하면 인식 실패로 두고
`'Patterning소재' 인식 불가` 로 알려줍니다 — 조용히 틀리는 것보다 낫습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 인증 없이 CCG 이동 401 · 목록에 없는 팀으로 이동 400 · 같은 코드 400 · 원장에 없는 코드 400 |
| test_api | 이동 후 코드·이름이 새 팀 값 · **금액 5항목 불변** · 감사 로그 기록 |
| test_api | 이동 전에는 못 지우던 코드가 이동 후에는 지워짐 |
| test_api | 저장된 설정이 코드 기본값보다 우선 (기존 원장을 조용히 덮지 않음) |
| test_api | **옛 코드가 원장에 남아 있어도 메일만 바꾸는 저장이 통과** (아래 참고) |
| test_api | 그래도 목록에 있는 코드를 빼는 것은 여전히 거부 |
| bulk_debug | 앞이 같은 팀 이름은 단독으로 확정되지 않음 |

검증: **API 468 / 브라우저 E2E 211 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 500오류 0건 /
디자인 감사 OK 13.** `store.py` 의 **저장 로직**은 무변경(신규 설치 기본값만 갱신) —
v9.4 원장에 v9.4 의 store.py 를 그대로 둔 채 올려 원장 md5 가 안 바뀌는 것을 확인했습니다.

## 리허설이 문서를 반증해서 하나 더 고쳤습니다

문서에 적은 순서(수신인 먼저 → CCG 정리)를 **실제 옛 원장으로 밟아 보니 첫 단계부터 400** 이었습니다.

원인은 "쓰는 중인 CCG 는 지울 수 없다" 검사였습니다. 이 검사가 **원장에 있는 모든 코드**를
대상으로 돌아서, 개편 직후처럼 *원장에만 있고 목록에는 없는* 옛 코드가 있으면
**CCG 와 무관한 저장(메일 주소 한 줄)까지 통째로 거부**됐습니다.

→ **지금 목록에 있던 것을 빼는 경우만** 막도록 좁혔습니다.
목록 밖에 있던 옛 코드는 애초에 '빼는' 것이 아니므로 저장을 막지 않습니다.
목록에 있는 코드를 지우려는 시도는 **그대로 거부**됩니다(안전장치는 유지).

고친 뒤 같은 절차를 다시 밟아 ①수신인 4명 저장 → ②새 목록 확인 → ③옛 코드 7개 이동까지
**전부 200**, 금액 네 숫자 불변을 확인했습니다.

## 배포 후 화면에서 해야 하는 일 (중요)

기존 원장에는 **이미 저장된 설정이 있어서 새 기본값이 자동 적용되지 않습니다.**
(설정이 코드보다 우선입니다 — 남이 고친 값을 배포가 조용히 덮으면 안 되니까요)

올린 뒤 `시스템 설정`에서 두 가지를 직접 하세요. → `UPGRADE.md` 8단계


---

# v10.10 (2026-07-31) — 전수 검증에서 나온 것 + 맥에서 띄우기

## 전수 검증 범위

| 축 | 방법 | 결과 |
|---|---|---|
| API 27개 × 무인증/인증 | 권한 매트릭스 자동 실행 | **경계 이상 0건** |
| 화면 9개 | 렌더·버튼·입력·대비·오버플로 계측 | 대비 미달 0 · 오버플로 0 · JS 오류 0 |
| 저장 무결성 | 백업 생성·상한·임시파일·스키마 | 이상 0건 |
| 동시 편집 | 같은 건에 8스레드 동시 PUT | 섞임 없음 · JSON 깨짐 없음 |
| 이상 입력 | 5,000자 · 동행 200명 · 거대 데이터 내보내기 | 500 오류 0건 |
| 백업 복원 | 실제 복원 후 화면 확인 | 정상 |
| 내보내기 | 27/31필드 · BOM · 표 서식 | 정상 |

## 고친 것 1 — 텍스트에 상한이 없었다

금액에는 1억 상한이 있는데 **텍스트에는 아무 제한이 없어서**, 5,000자를 붙여넣으면
그대로 원장에 저장됐습니다. 목록·센터 제출 CSV·인폼 표가 통째로 망가지는 경로입니다.

| 칸 | 상한 |
|---|---|
| 출장도시 | 40자 |
| 출장기관&업체 | 100자 |
| 출장목적&사유 | 300자 |
| 비고 | 500자 |
| 성명 / 사번 | 40 / 30자 |
| 한 출장의 동행 | 30명 |

업무 규칙이 아니라 **붙여넣기 사고 방어선**입니다. 넘으면 몇 자인지와 함께 알려 줍니다.

## 고친 것 2 — 화면 낭독기에 이름이 없던 칸 6개

직책·CCG팀·이전 출장 불러오기·실적 대상 고르기·상태 거르기·리비전 유형 —
전부 `<select>` 라 눈으로는 열 제목이 보이지만 낭독기에는 이름이 없었습니다.
`aria-label` 을 붙이고 테스트로 고정했습니다.

## 맥에서 띄우기

사내 서버는 윈도우지만 검토·시연은 맥에서 합니다. 윈도우 전용 코드는 원래 없었고,
맥에서 걸리는 두 가지를 처리했습니다.

- **포트** — macOS 12+ 는 5000번을 AirPlay 수신 모드가 씁니다.
  `TB_PORT` 환경변수를 추가했습니다 (`TB_PORT=5050 python3 webmain.py`).
  잘못된 값이면 5000으로 되돌아갑니다.
- **글꼴** — 맥에는 맑은 고딕이 없습니다. 윈도우 순서는 **그대로 두고** 뒤에
  `-apple-system`(라틴=SF Pro) → `Apple SD Gothic Neo`(한글) 를 받쳐 뒀습니다.
  **사내 윈도우 화면은 한 픽셀도 바뀌지 않습니다.** 코드블록도 SF Mono/Menlo 를 추가했습니다.

실행법은 `DEPLOY.md` **8-6**. 맥에서 만든 `data.json` 은 그대로 서버로 옮겨도 됩니다.

> 이 컨테이너는 리눅스라 **macOS 에서 직접 띄워 보지는 못했습니다.**
> 윈도우 전용 API·경로가 없고(전수 grep), 같은 POSIX 계열인 리눅스에서
> 동일하게 동작하는 것까지 확인한 상태입니다.

검증: **API 492 / 브라우저 E2E 211 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 500오류 0건 /
디자인 감사 OK 13.** `store.py` 저장 로직 무변경.


---

# v10.11 (2026-08-01) — 남아 있던 3개

지난 평가에서 B 로 남겨 둔 세 가지를 전부 고쳤습니다.

## 1. 여러 건 한꺼번에 확정 (19클릭 → 2클릭)

엑셀로 20건을 넣고 **하나씩 20번** 눌러야 했습니다. 이제 목록에서 골라 한 번에 확정합니다.

- 맨 앞에 **선택 열** — 고를 수 있는 것은 **잠정 계획뿐**입니다. 다른 단계는 칸이 비어 있습니다
- 머리글 체크박스로 **보이는 잠정 전체 선택** (필터를 걸면 걸린 것만)
- 고르면 띠가 나타납니다 — `4건 선택 · 계획 합계 733,000원`,
  가용 잔여를 넘으면 그 자리에서 **빨간 경고**
- 누르기 전 확인창에 **어떤 건인지 8건까지 나열**하고, 초과분이 있으면 `−N원` 을 함께 보여줍니다

서버는 `POST /api/groups/bulk_status` 하나로 처리합니다.

- **한 번의 락 · 한 번의 저장 · 백업 1개** — 단건을 20번 부르면 백업 20개가 쌓여
  30개 한도가 하루에 소진됩니다 (테스트로 고정)
- **허용 전환은 `계획 등록 → 확정 예정` 하나뿐**입니다.
  이관·처리 완료·취소처럼 금액이 나가는 전환은 일괄로 열지 않았습니다
- 일부만 조건에 안 맞으면 **되는 것만 처리하고 실패는 건별 사유**로 돌려줍니다
  (`청주 협력사 — 계획(잠정) 단계가 아닙니다`)

## 2. 부서별 현황을 첫 화면으로

`y=938px` — 1920·1440·1366 **어느 해상도에서도 스크롤해야** 보였습니다.
직접 요청하신 지표인데 매번 스크롤이 필요했던 자리입니다.

**`y=471px`** 로 올렸습니다. `공지 → 예산 → 부서별 → 바로 할 일 → 비목` 순서입니다.
큐는 목록·처리 화면에도 진입로가 있지만, 부서별 집계는 여기뿐이라 위로 올렸습니다.

## 3. 삭제를 ⋯ 메뉴로

행마다 버튼이 4개라 한 화면에 **124개**였고, **되돌릴 수 없는 삭제가 주요 동작과 같은 크기**로
`출장 확정` 바로 옆에 붙어 있었습니다.

이제 행에는 **`내역 ▼` + 주요 동작 1개 + `⋯`** 셋만 둡니다.

| 상태 | 눈에 보이는 주요 동작 | ⋯ 안 |
|---|---|---|
| 계획(잠정) | 출장 확정 | 수정 · **삭제** |
| 확정 예정 | 실적 입력 | 수정 · 확정 해제 · **출장 취소** |
| 실적 이후 | 인폼 보기 | 수정 · 이관·처리 관리로 |

- 파괴적 항목은 메뉴 **맨 아래에 선을 긋고 빨강**으로 분리했습니다
- 메뉴는 **한 번에 하나만** 열리고, 바깥을 누르거나 **Esc** 로 닫힙니다
- `<details>` 라 **키보드로도** 열립니다

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 일괄 확정 후 확정액 증가 = 가용 감소 = 계획 합계 |
| test_api | 섞여 있으면 되는 것만 처리 · 실패는 건별 사유 |
| test_api | 이관·완료·취소·인폼은 일괄로 불가 · 빈 목록 · 201건 거부 |
| test_api | **5건 일괄 확정에 백업은 1개만** |
| e2e | 삭제·취소가 행에 노출되지 않음 (렌더된 가시성으로 판정) |
| e2e | 메뉴 하나만 열림 · Esc 로 닫힘 · 위험 항목 구분 |
| e2e | 전체 선택이 잠정만 고름 · 확정 후 선택 비워짐 |
| e2e | **부서별 현황이 1366×768 첫 화면 안** · 큐보다 위 |

검증: **API 517 / 브라우저 E2E 223 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 500오류 0건 /
디자인 감사 OK 13.** `data.json` 스키마·저장 로직 무변경.


---

# v10.12 (2026-08-01) — 아이패드판에서 수신인·CCG 가 빠져 있던 것

## 무엇이 잘못됐나

아이패드용(정적판)을 열어 보니 **인폼 수신인이 2명**, **CCG 가 7팀**이었습니다.
서버는 4명·8팀입니다.

정적판은 같은 화면(app.js)을 쓰지만 **백엔드를 따로 흉내낸 파일**(`tools/tb_local.js`)이 있습니다.
v10.9 에서 실제 값을 넣을 때, 옛 7팀을 1:1 로 바꾸는 일괄 치환을 썼습니다. 그래서

- **새로 생긴 8번째 팀** `Patterning소재개발(50077468)` 은 치환 대상이 없어 **추가되지 않았고**
- 센터 담당자 2명은 `store.py` 에만 손으로 넣어 **정적판에 반영되지 않았습니다**

윈도우 사내 서버는 처음부터 4명·8팀이라 **아이패드로 열었을 때만** 보이는 차이였습니다.

## 고친 것

정적판의 기본 수신인을 4명으로, CCG 를 **8팀·같은 순서**로 맞췄습니다.

## 재발 방지 — 값을 맞춘 게 아니라 어긋남을 잡게 했습니다

같은 실수가 다시 나지 않도록, 서버와 정적판의 **기본값 전체를 대조**하는 검사를 넣었습니다.

| 테스트 | 검사 |
|---|---|
| test_api §31 | `store._settings()` 6개 항목이 정적판과 글자까지 같은가 |
| test_api §31 | 수신인 **수**가 같은가 |
| test_api §31 | CCG 목록이 **이름·코드·순서까지** 같은가 |
| test_api §31 | CCG 팀 **수**가 같은가 |

일부러 수신인 1명과 팀 1개를 빼서 **4건이 실제로 실패하는 것**까지 확인했습니다.

검증: **API 541 / 브라우저 E2E 223 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건.**
아이패드 화면에서 수신인 4명·CCG 8팀·등록 드롭다운 8팀을 눈으로 확인했습니다.


---

# v10.13 (2026-08-01) — 역할별 사용성 검증에서 나온 것 2건

세 역할(출장 예정자·다녀온 사람·운영 담당자)로 **실제로 클릭해 보며** 재고, 그 과정에서
v10.11 에 새로 넣은 선택·메뉴가 만든 결함 2건을 잡았습니다.

## ① 인쇄물에 조작용 UI 가 찍혔다

출장 내역을 결재 첨부용으로 인쇄하면 **선택 체크박스 16개와 `⋯` 메뉴 15개**가 그대로
나왔습니다. 인쇄 스타일은 메뉴·버튼·인폼카드를 숨기고 있었는데, v10.11 에서 새로 생긴
선택 열·행 메뉴·선택 띠가 그 목록에 없었습니다.

→ `@media print` 에 넣고, 선택 행 음영도 지웠습니다. 인쇄 미리보기에서 **0개** 확인.

## ② 일괄 확정 확인창에서 어떤 건인지 구별이 안 됐다

같은 곳에 두 번 가면 확인창이 이렇게 보였습니다.

```
 · 이천 SK하이닉스 이천 07/01
 · 이천 SK하이닉스 이천 07/01     ← 같은 줄. 무엇을 확정하는지 알 수 없다
```

→ **날짜를 앞에 두고 목적·인원·금액**을 붙였습니다.

```
 · 07/03 용인 용인 소재센터 — 용인 3차 협의 (3명 246,000원)
 · 07/02 청주 청주 협력사 — 청주 2차 협의 (2명 337,000원)
```

## 확인해서 정상이던 것 (같이 공격했음)

- 선택해 둔 채 검색해서 결과가 0건이 되면 → 띠가 **사라짐**, 필터를 풀면 선택 **복원**
- 일부만 보이게 필터한 뒤 일괄 확정 → **보이는 것만** 처리(필터 밖 2건은 잠정으로 남음)
- `⋯` 메뉴를 연 채 내역을 펼치면 → 메뉴 **자동으로 닫힘**
- `⋯` 메뉴는 **키보드만으로** 열림 (`summary` tabindex 0 · Enter)

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 인쇄 스타일에 선택칸·⋯메뉴·선택 띠·선택 행 음영이 모두 들어 있는가 |
| test_api | 일괄 확정 확인창이 목적·인원·금액을 함께 보여주는가 |

검증: **API 548 / 브라우저 E2E 223 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건.**


---

# v10.14 (2026-08-01) — 인폼을 화면 안으로, 끄는 곳을 눈에 보이게

## 1. 떠 있던 인폼 카드를 화면 흐름 안으로

인폼은 오른쪽 아래에 **떠 있는 카드**였습니다. 그래서 아래쪽 버튼을 가렸고,
그걸 피하려고 **접기 버튼**과 **본문 아래 여백 자동 보정**(`--mailh`)을 붙여 뒀습니다.
문제를 만들고 그 문제를 가리는 장치를 얹은 구조였습니다.

이제 **지금 보고 있는 화면의 맨 아래**에 그냥 놓입니다.

- 가릴 일이 없으니 **접기 버튼도, 여백 보정도 필요 없습니다** (둘 다 삭제)
- 카드가 생기면 그 자리로 부드럽게 스크롤합니다
- 실적 저장·인폼 다시 보기·이관 인폼·센터 제출 리포트 **네 곳 모두** 같은 방식

**함께 고친 순서 문제** — 이관 인폼은 `showMail()` 을 부른 뒤에 화면을 다시 그리고
있었습니다. 떠 있는 카드는 `body` 에 붙어 살아남았지만, 화면 안에 놓이면 그 재렌더에
지워집니다. 재렌더가 끝난 뒤에 붙이도록 순서를 바꿨습니다.

## 2. 누가 봐도 "여기를 끄는구나"

표 위에 **점선 구역**을 두르고 손잡이와 문장을 넣었습니다.

```
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
  ⠿  여기를 마우스로 끌어 메일 본문에 놓으세요
 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
   (실비 이관 요청 표)
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```

- 마우스를 올리면 **테두리가 진해지고 바탕이 밝아지며 살짝 떠오릅니다**
- 커서는 `grab`, 누르면 `grabbing`
- 끌기 시작하면 구역이 더 진해져 **끌고 가는 중**임을 알립니다
- 색은 기존 단계 음영(s1~s3)만 씁니다 — 새 색을 만들지 않았습니다

## 재발 방지

'접기가 동작하는가' 같은 **장치의 테스트**를 지우고, 그 장치가 있던 **이유**를 직접 잽니다.

| 테스트 | 검사 |
|---|---|
| e2e | 인폼이 `position:static` 이고 화면 섹션 안에 있는가 |
| e2e | **카드 밖 버튼 중 카드와 겹치는 것이 0개인가** (원래 접기의 목적) |
| e2e | 끄는 구역이 점선인가 · 커서가 `grab` 인가 · 문장에 "끌어"가 있는가 |
| test_api | 여백 척도(4·8·12·16·24) 준수 · `letter-spacing` 미사용 |

검증: **API 548 / 브라우저 E2E 227 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건 /
디자인 감사 OK 13.**


---

# v10.15 (2026-08-01) — 모서리·그림자·테두리 척도

## 재던 중에 나온 것

라운딩이 **2px · 3px · 4px 세 종류**로 섞여 있었고, 그림자는 **5종**,
테두리 회색은 토큰(`--line`) 밖에 `#CDD4DF` · `#CFD5DF` · `#E3E7ED` 가 하드코딩돼
있었습니다. 화면마다 미묘하게 다른 제품처럼 보이던 원인입니다.

## 모서리 — 네 단계

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--r-ctl` | **3px** | 버튼 · 입력 · 메뉴 항목 |
| `--r-box` | **4px** | 카드 · 표 · 모달 · 팝오버 |
| `--r-pill` | **알약** | 상태 배지 |
| `--r-bar` | **2px** | 막대 · 범례 조각 |

**컨테이너를 컨트롤보다 한 단계 크게** 둔 것이 핵심입니다. 카드 안에 버튼이 들어갔을 때
바깥이 더 둥글어야 겹쳐 놓인 모양이 자연스럽습니다(같으면 안쪽이 더 각져 보입니다).
막대는 조각이 서로 맞닿아 있어 크게 굴리면 이가 벌어져 보이므로 가장 작게 뒀습니다.

**상태 배지만 알약**입니다 — 상태는 값이 아니라 라벨이라, 표의 사각 셀과 즉시 구분됩니다.

## 그림자 — '얼마나 떠 있는가' 세 단계

| 토큰 | 쓰는 곳 |
|---|---|
| `--sh-1` | 바닥에 붙은 면 — 카드 · 인폼 |
| `--sh-2` | 살짝 뜬 것 — 행 메뉴 팝오버 |
| `--sh-3` | 완전히 뜬 것 — 모달 |

## 테두리로 더한 완성도 (아이콘·이모지 없음)

- **지금 보고 있는 메뉴에 왼쪽 강조선** — `inset` 그림자로 그려 글자가 밀리지 않습니다
- **표 머리글 경계를 2px** 로 — 머리글과 본문이 한눈에 갈립니다
- 하드코딩 회색 3종을 `--line` · `--line2` 로 통일

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | `border-radius` · `box-shadow` 에 토큰 밖 값이 있으면 실패 |
| test_api | 컨테이너 > 컨트롤 > 막대 순서가 깨지면 실패 |
| test_api | 하드코딩 테두리 색(`#CDD4DF` 등) 재등장 시 실패 |
| e2e | **렌더된** 모서리 값이 4종 이하 · 버튼 3px · 카드 4px · 배지 알약 |
| e2e | 표 머리글 경계가 본문보다 두꺼운가 · 메뉴 강조선이 있는가 |

검증: **API 569 / 브라우저 E2E 233 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건 /
디자인 감사 OK 13.**


---

# v10.16 (2026-08-02) — 지금 보고 있는 메뉴, 막대 대신 면으로

## 바꾼 이유

v10.15 에서 선택된 메뉴에 **왼쪽 강조선**(3px 남색 막대)을 넣었습니다.
흔한 관리자 템플릿의 기본값이라 "기성품을 그대로 쓴" 인상을 줍니다.
표시 방식만 바꾸고, 기능·데이터는 손대지 않았습니다.

## 비교한 네 가지

| 안 | 방식 | 판단 |
|---|---|---|
| A | 왼쪽 강조선 (v10.15) | 흔한 기본값 |
| **B** | **옅은 남색 면**(`--s3bg`) **+ 남색 계열 테두리**(`--s4ln`) | **채택** |
| C | 짙은 남색 면 + 흰 글자 | 좁은 사이드바에서 무겁고, 남색 = 처리 완료 배지와 겹침 |
| D | 사이드바 배경을 낮추고 선택만 흰 카드 | 보기는 좋으나 사이드바 전체가 바뀜 |

**B** 를 고른 이유:

- 이미 쓰고 있는 남색 계열 두 개만 씁니다 — 새 색을 만들지 않았습니다
- 마우스만 올린 상태는 **회색**(`--soft`), 지금 보고 있는 화면은 **남색 면** —
  두 상태가 색으로 갈립니다(v10.15 는 둘 다 회색이고 막대 유무로만 갈렸습니다)
- 항목 전체가 채워져서, 사이드바를 눈으로 훑을 때 먼저 잡힙니다
- 아이콘·이모지 없이 면과 테두리만 씁니다

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 선택 메뉴가 `--s3bg` 면 + `--s4ln` 테두리인가 |
| test_api | 마우스 올린 상태는 회색인가 (선택과 색이 달라야 함) |
| test_api | 선택 메뉴에 강조 막대(`box-shadow`)가 다시 생기면 실패 |
| e2e | **렌더된** 배경색 `rgb(211,225,240)` · 테두리 `rgb(184,204,226)` · 그림자 없음 |

## 업그레이드 리허설 재실행

버전을 올리면서 **v9.4 → v10.16 업그레이드와 되돌리기를 다시 한 번 돌렸습니다.**

| 항목 | 결과 |
|---|---|
| `data.json` md5 | `56aa703b…` → **동일 (한 글자도 안 바뀜)** |
| 출장 15건 / 출장자 28행 / 예산 리비전 6건 | **동일** |
| 계획 3,623,000 / 실적 2,049,000원 | **동일** |
| 대시보드 완료·처리중·확정·가용 | **동일** |
| 센터 제출 CSV md5 `7af279cd…` | **동일** |
| 감사 로그 52건 / 자동 백업 30개 | **동일** |
| v10.16 → v9.4 되돌리기 | 지문 전부 동일 · v9.4 스모크 19/0 |

검증: **API 571 / 브라우저 E2E 235 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건 /
디자인 감사 OK 13.**


---

# v10.17 (2026-08-02) — 테마 전수 점검: 색 · 글씨 · 모서리 · 테두리

## 훑어보니 어긋난 것

모서리는 v10.15 에서 잡았지만, **색 · 글씨 · 인라인 여백**은 훑은 적이 없었습니다.
선언 단위로 전부 세어 보니:

| 항목 | 점검 전 | 점검 후 |
|---|---|---|
| `:root` 밖 하드코딩 색 (화면) | **34종** (거의 같은 옅은 회색만 12종) | **0** |
| 테두리 굵기 | 1 · 2 · 3 · 4px 네 종 (3과 4가 같은 용도) | **1 · 2 · 4** 세 단계 |
| 안내문 글씨 크기 | **12종** (11.5 · 12.5 · 13.5 · 14.5 · 16.5px …) | **6종** (12·13·14·16·20 + 인쇄 pt) |
| 안내문 줄높이 | 1.6 · 1.25 · 1.7 · 19px 혼용 | **px 정수만** |
| 안내문 글씨 굵기 | 700 · 800 혼용 | **700** (맑은 고딕이 가진 굵기) |
| `letter-spacing` | 안내문에 3곳 | **0** |
| app.js 인라인 여백 | 척도 밖 **28곳** (10 · 14 · 18 · 9 · 6 · 5px) | **0** |
| 원(circle) 모서리 | `50%` 직접 지정 | `--r-pill` |

## 제일 큰 것 — 안내문이 화면과 다른 팔레트를 쓰고 있었습니다

`traveler_guide.html`(출장자에게 인쇄·메일로 나가는 안내문) 첫 줄에는
"화면과 같은 토큰·글꼴을 씁니다" 라고 적혀 있었지만, 실제 값은 **화면을 새로 칠하기 전의
옛 팔레트**였습니다 — `--ink:#111827`(화면은 `#1A2230`), `--line:#D9DEE7`(화면은 `#D5DBE3`),
`--bg:#F5F6F8`(화면은 `#F4F6F8`). 나란히 놓으면 회색이 서로 다르고 글씨도 반 픽셀씩 달랐습니다.

- 팔레트 · 모서리 토큰을 화면과 **같은 값**으로 맞췄습니다
- 상태 배지(`.st`)를 화면과 같이 **알약**으로 — 안내문의 배지와 화면의 배지가 같아졌습니다
- 글씨는 12 · 13 · 14 · 16 · 20px 다섯 단계 + 줄높이 px 정수로

## 새로 만든 토큰

| 토큰 | 뜻 |
|---|---|
| `--surf` | 표 행 hover · 표 머리 · 안내 상자 (거의 흰색) |
| `--tint` / `--tint2` | 선택된 행 · 메뉴 hover / 묶음 머리 — 남색 기 도는 면 두 단계 |
| `--red-bg` · `--red-ln` | 빨강 신호 한 벌 (면 · 선). 글자는 기존 `--red` |
| `--amber-bg` · `--amber-ln` · `--amber-bar` · `--amber-ink` | 호박 신호 한 벌 |
| `--s3ln` | 확정 예정 배지 테두리 |
| `--sh-cut` | 막대 조각을 자르는 선 — '떠 있는 정도'가 아니라서 이름을 따로 줬습니다 |
| `.mono` | 고정폭 — 붙여넣기 원문·미리보기. 스택을 한 곳에서만 정의 |

## 규칙 (음영 · 테두리)

- **음영은 '얼마나 떠 있는가'만** 말합니다 — 카드 `--sh-1`, 팝오버 `--sh-2`, 모달 `--sh-3`.
  버튼·입력칸·표 행에는 그림자를 쓰지 않습니다. 면을 나누는 일은 테두리 몫입니다.
- **테두리 굵기 세 단계** — 1px 기본 구분, 2px 강한 구분(표 머리글 아래),
  4px 한쪽 표식(안내·경고·근거 블록의 왼쪽 띠). 3px 같은 중간 굵기는 쓰지 않습니다.
- **신호는 면 · 선 · 글자 세 쪽이 한 벌**입니다. 한 벌에서 면만 빌려 쓰지 않습니다.

## 재발 방지 — §33 테마 전수

| 테스트 | 검사 |
|---|---|
| test_api | 화면·안내문 `:root` 밖 하드코딩 색 0 |
| test_api | 글씨 크기가 척도 안 · 줄높이 px 정수 · 크기↔줄높이 짝 |
| test_api | 굵기는 400·700 뿐 · `letter-spacing` 미사용 |
| test_api | 테두리 굵기 1·2·4 뿐 |
| test_api | 안내문 `:root` 18개 토큰 값이 화면과 동일 |
| test_api | app.js **인라인** 여백·글씨도 척도 안 (CSS 밖이라 그동안 빠져나가던 자리) |
| test_api | 고정폭 스택은 한 곳에서만 정의 |
| design_audit | 렌더된 폰트 스택 = 본문 1 + 고정폭 1 |

검증: **API 608 / 브라우저 E2E 235 / Pages 25 / 붙여넣기 53 / 퍼징 1,302회 0건 /
디자인 감사 OK 13.**
업그레이드 리허설 재실행 — v9.4 → v10.17 후 `data.json` md5 · 건수 · 금액 · 상태 ·
CSV md5 · 감사로그 · 백업 30개 전부 동일, 되돌린 뒤에도 동일(스모크 19/0).


---

# v10.18 (2026-08-02) — 안내문 문장을 사람 말로

## 고친 것

안내문 제목과 단계 설명이 **번역투**라 읽다 걸렸습니다. 뜻은 그대로 두고 문장만 바꿨습니다.

| 전 | 후 |
|---|---|
| 출장 가시는 분께 — 4단계면 끝납니다 | **출장비 처리, 네 번이면 끝납니다** |
| 출장자가 하는 일은 계획 등록과 실적 입력 둘뿐입니다 | 출장 가시는 분이 직접 하실 일은 계획 등록과 실적 입력, 두 가지입니다 |
| 메일 만들기·이관·정산은 시스템과 담당자가 **이어받습니다** | 메일 만들기와 이관·정산은 담당자와 시스템이 **맡습니다** |
| **누르는 버튼** [출장 계획 등록] | **다 적었으면** [출장 계획 등록] |
| 실제로 간다면 '확정'을 눌러야 예산이 잡힙니다 | **정말 가는 출장이면 확정을 누릅니다** |
| 다녀온 뒤 실적을 입력합니다 | **다녀와서 실제 쓴 금액을 넣습니다** |
| 실제로 쓴 금액을 넣습니다. 각 칸에 계획액이 흐리게 보이고… | **칸마다 계획액이 흐리게 보입니다.** 실제 쓴 금액을 넣으면 옆에 차액이 바로 뜹니다 |
| 카드를 닫았어도 **없어지지 않습니다** | 카드를 닫아도 **내용은 남아 있습니다** |
| 여기까지가 출장자 몫입니다 | **여기까지 하시면 됩니다** |

- 지시문은 **한 항목에 한 가지**만 담고, 문장을 짧게 끊었습니다
- 버튼 앞 라벨을 상황에 맞게 바꿨습니다 — "다 적었으면 · 나중에 확정할 때 · 다 넣었으면 · 끌어 놓기가 어려우면"
- 머리말과 차례 밑에 같은 말이 두 번 나오던 것을 하나로 정리했습니다
- 화면 안 **이용 안내**도 같은 기준으로 다듬고, 섞여 있던 말끝(`~이에요` · `~돼요`)을 `~습니다`로 통일했습니다

## 이모지 제거

화면에 남아 있던 색 이모지 다섯 개를 뺐습니다 — 검색창 `🔍` 2곳, 자동 표시 `⚙`,
예산 계산 `💡`, 확정 확인창 `⚠`. 색 이모지는 윈도우·맥·아이패드에서 서로 다르게 그려지고
인쇄물에도 그대로 찍힙니다. 화살표(→ ↗)처럼 단색으로 그려지는 글리프만 남겼습니다.

## 윈도우 크롬 점검 도구 (`tools/e2e/win_chrome.mjs`)

"데스크톱 윈도우 크롬에서 깔끔한가"를 매번 같은 방식으로 확인하려고 만들었습니다.
윈도우 UA·1366×768·1920×1080 두 크기로 **모든 화면 10개**를 돌며 봅니다.

| 검사 | 내용 |
|---|---|
| 가로 스크롤 | 화면마다 `scrollWidth > clientWidth` 인가 |
| 잘린 글자 | 넘치는데 말줄임도 스크롤도 없는 칸이 있는가 |
| 카드 겹침 | 카드가 위아래로 겹쳐 그려지는가 |
| 클릭 대상 | 버튼·메뉴 높이가 24px 이상인가 |
| 행 메뉴 | 팝오버가 화면 밖으로 나가는가 · Esc 로 닫히는가 |
| 붙여넣기 | 미리보기가 뜨는가 · 입력칸이 고정폭인가 |
| 키보드 | Tab 이동 시 포커스 테두리가 보이는가 |
| 인쇄 | 사이드바·선택칸·⋯메뉴가 빠지는가 |
| 안내문 | 가로 스크롤 · 깨진 그림 · 칸을 넘는 그림 |
| 콘솔 | JS 오류 · 예외 · 실패한 요청 |

결과 **95개 항목 전부 통과** (오류 0 · 예외 0 · 실패 요청 0).

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 화면·안내문·app.js·정적판에 색 이모지가 있으면 실패 |
| win_chrome | 위 표의 10개 화면 × 2해상도 점검 |

검증: **API 612 / 브라우저 E2E 235 / 윈도우 크롬 95 / Pages 25 / 붙여넣기 53 /
퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.19 (2026-08-02) — 올려도 설정이 흐트러지지 않는다는 것을, 확인할 수 있게

## 질문

"이미 쌓인 데이터 + 내가 고친 안내 문구 + 메일 수신인 + CCG 설정이
버전 올리면서 흐트러지지 않게 하려면?"

## 답 — 구조상 흐트러질 수 없고, 30초면 확인됩니다

| 무엇 | 어디에 있나 | 업그레이드 때 |
|---|---|---|
| 출장·예산·감사 로그 | `data.json` | 건드리지 않음 |
| 안내 문구 · 보조 문구 | `data.json` → `settings.notice` | 건드리지 않음 |
| 인폼 메일 수신인 | `data.json` → `settings.mail_recipients` | 건드리지 않음 |
| CCG 목록 (화면에서 고쳤다면) | `data.json` → `settings.ccg_teams` | 건드리지 않음 |
| 담당자 비밀번호·시스템 이름·참고 주소 | `data.json` → `settings` | 건드리지 않음 |
| 화면·계산 로직 | 파일 5개 | 이것만 교체 |

프로그램은 켜질 때 `data.json` 을 **그대로 읽기만** 합니다. 새 버전의 기본값과
합치는 로직 자체가 없습니다(`store.py` 의 기본값은 **파일이 없을 때만** 쓰입니다).

**반대로도 참입니다** — 새 기본값이 저절로 들어오지도 않습니다. 지금 수신인이 옛날 2명이면
올린 뒤에도 2명입니다. 그래서 8단계(올린 뒤 화면에서 하는 일)가 따로 있습니다.

## `check_data.py` 에 설정 지문 추가

업그레이드 전후로 이 12자리만 비교하면 됩니다.

```
  ── 내가 고쳐 둔 설정 (업그레이드 후 이 블록이 그대로여야 합니다) ──
  설정 지문    5b364e5cb109      ← 올린 뒤 다시 실행해서 이 값만 비교하면 됩니다
  안내 문구    2026-3Q 소재 예산 소진 — 센터 예산 사용 중입니다…
  인폼 수신인  4명
               · junghoon12.lee@sk.com …
  CCG 목록     8팀  (화면 [시스템 설정]에서 고쳐 둔 것 — data.json 에 있음)
```

- 비밀번호는 **값을 찍지 않고** "기본값" / "바꿔 두셨습니다" 로만 표시합니다
- CCG 가 `data.json` 에서 오는지 프로그램 기본값에서 오는지 알려줍니다
- 읽기만 합니다 — 어떤 파일도 만들거나 바꾸지 않습니다

## 설정 보존 리허설 (`tools/e2e/settings_keep.py`)

말이 아니라 실제로 확인합니다. 옛 버전 서버에서 안내 문구를 바꾸고, 수신인을 4명으로 고치고,
CCG 에 새 팀을 하나 추가한 뒤 파일 5개를 갈아끼웁니다.

| 확인 | 결과 |
|---|---|
| 안내 문구 · 보조 문구 · 수신인 4명 · CCG(추가 팀 포함) | **그대로** |
| `settings` 블록 전체 | **한 글자도 안 바뀜** |
| `data.json` 파일 자체 | **md5 동일** |
| 출장 건수 · 대시보드 4개 금액 · 센터 제출 CSV | **동일** |
| 실제 인폼 메일 수신인 | 고쳐 둔 4명 |
| 한 번도 안 고친 서버 | 옛 수신인이 그대로 남음 (새 기본값이 저절로 안 들어옴) |

**22개 항목 전부 통과.** 임시 폴더에서 예시 원장으로만 돌며, 운영 데이터는 열지도 않습니다.

## UPGRADE.md 보강

- **1-1단계** 신설 — 프로그램과 데이터가 어떻게 갈라져 있는지, CCG 가 두 곳 중 어디서 오는지
- **1단계** — 숫자뿐 아니라 **설정 지문**도 적어 두게
- **6단계** — 올린 뒤 설정 지문 대조 + 대시보드 안내 문구·설정 화면 눈으로 확인
- **8-2** — 이미 CCG 를 고쳐 두신 경우도 다루도록(고쳐 둔 목록이 그대로 보입니다)
- 자주 나오는 질문 3개 추가 — 설정이 날아가지 않는 이유 / 새 기본값은 언제 쓰이나 /
  바꿔 둔 비밀번호는 어떻게 되나

## 부수

- `check_data.py` 의 이모지(경고·체크) 제거 — 사내 콘솔에서 깨져 보이던 자리

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §34 | 설정 지문이 안내 문구·수신인·CCG·비밀번호를 포함하는가 |
| test_api §34 | 비밀번호 **값**을 찍지 않는가 |
| test_api §34 | 리허설 스크립트가 운영 데이터를 쓰지 않는가 |
| settings_keep | 위 표 22개 항목 |

검증: **API 622 / 설정 보존 22 / 브라우저 E2E 235 / 윈도우 크롬 95 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.20 (2026-08-03) — 이름표에 로고, 한 줄 안의 눈높이

## 좌측 상단 이름표

- **SK hynix 로고**를 이름 위에 넣었습니다. 회사에서 받은 `.ai` 원본에서 뽑아
  **인라인 SVG(4KB)** 로 넣었습니다 — 외부 파일도, CDN 도 부르지 않습니다.
  벡터라 어느 배율에서도 또렷하고, 아이패드 단일 파일 배포본에도 그대로 들어갑니다.
- **메뉴와 다른 척도**로 구분했습니다. 메뉴는 13px 한 줄, 이름표는
  로고 + **16px 제목** + **12px 영문(Domestic Travel Expense)** + 아래 경계선.
  같은 글씨 크기로 나열되던 때보다 "이 시스템이 무엇인가"가 먼저 읽힙니다.
- **누르면 대시보드로** 갑니다. 메뉴에도 대시보드가 있어 중복이지만,
  로고를 눌러 첫 화면으로 가는 것은 사람들이 가장 먼저 시도하는 동작입니다.
  마우스뿐 아니라 **Tab → Enter** 로도 됩니다.
- 시스템 이름을 [시스템 설정]에서 바꾸면 **이름표와 브라우저 탭 제목도 함께** 바뀝니다.

## 한 줄 안의 칸들이 우글거리던 것

출장자 표의 **CCG팀 칸만 두 줄**(선택 + 아래 CCG 번호)이라, 그 칸이 옆 칸들과
**9px 어긋나** 보였습니다. 재보니 원인이 두 겹이었습니다.

| 원인 | 내용 |
|---|---|
| 두 줄 | CCG 번호를 칸 아래 캡션으로 둬서 그 칸만 높아짐 (행 높이 65px) |
| 칸 높이 | **크롬은 select 의 line-height 를 무시**해 input(38px)보다 3px 작게(35px) 그림 |
| 날짜칸 | `input[type=date]` 는 달력 아이콘 때문에 2px 큼 |

고친 방법:

- **CCG 번호를 선택지 라벨 안으로** — `EDTW소재기술 · 50119134`. 칸이 한 줄로 돌아왔습니다.
  저장되는 값은 팀 이름 그대로입니다(라벨과 `value` 를 분리).
- **입력칸 높이를 토큰으로 못박음** — `--h-ctl:38px` · `--h-ctl-sm:28px`.
  input · select · date 가 전부 같은 높이가 됩니다. 표 안 필터 줄도 작은 척도로 통일.
- 입력 표에서는 **버튼도 같은 높이**로 — 한 줄이 통째로 반듯해집니다.

결과: 출장자 한 줄의 컨트롤 9개 눈높이 **편차 9px → 0px**, 행 높이 65px → 48px.

## 열 폭 (1366×768 기준)

번호까지 한 줄에 담으려면 CCG 칸이 넓어야 하는데, 좁은 화면에서는 표가 가로로 밀립니다.

| 화면 폭 | CCG 칸 | 가장 긴 팀 이름 + 번호 |
|---|---|---|
| 1360px 이상 (사내 표준 1366 포함) | 240px | **닫아 놓아도 다 보임** |
| 1360px 미만 | 148px | 목록을 펼치면 온전히 보임 |

금액칸 상한을 110 → 88px 로 낮춰 남는 폭을 CCG 칸에 줬습니다
(`1,250,000` 이 들어가고도 남습니다). 계획·실적 두 표가 같은 상한을 쓰므로
두 표의 금액칸 크기도 서로 같습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §35 | 로고가 인라인 SVG 인가 · 대체 텍스트 · 클릭/키보드로 대시보드 이동 |
| test_api §35 | 이름표 16px·영문 12px (메뉴 13px 과 다른 척도) |
| test_api §35 | `--h-ctl` 토큰 · input=select 높이 · 체크박스/textarea 예외 |
| test_api §35 | CCG 번호가 선택지 안에 있고 `value` 는 팀 이름인가 |
| e2e | 출장자 한 줄의 눈높이 편차 0 · 가장 긴 라벨이 잘리지 않음 · 열이 늘지 않음 |
| win_chrome | **모든 화면**에서 한 줄 안의 입력칸 눈높이가 같은가 (1366·1920) |
| win_chrome | 포트가 이미 쓰이면 실패 — 낡은 서버를 재고 통과하던 구멍을 막음 |

검증: **API 639 / 브라우저 E2E 240 / 윈도우 크롬 115 / 설정 보존 22 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.21 (2026-08-03) — 걸러 놓은 건만 센터 제출 양식으로

## 나온 문제

추가 예산을 받으려면 **확정 예정 건만** 골라 센터에 내야 하는데, 출장 내역에서 CSV 를
받으면 **필터와 상관없이 그 분기 전부**가 나왔습니다. 화면에서 1건만 걸러 놓아도
파일에는 8건이 담기니, 받는 쪽에서 다시 골라내야 했습니다.

## 고친 것

출장 내역 카드 머리에 버튼 두 개를 두고, **지금 화면에 걸러진 것만** 내보냅니다.

| 버튼 | 내용 |
|---|---|
| **센터 제출 양식 (Excel) · N건** | 센터 관리 양식 27칸 + 서식(맑은 고딕/Trebuchet). 제출본 |
| **CSV** | 같은 27칸을 CSV 로 |

- 버튼에 **나갈 건수**가 찍히고, 바로 아래에
  `지금 걸러진 3건 · 출장자 5명 · 계획 1,860,000원 이 나갑니다` 처럼 미리 알려줍니다.
- **체크박스로 고른 것이 있으면 그것만** 나갑니다(없으면 걸러진 전체).
  안내 문구도 '고른 3건' 으로 바뀝니다.
- 필터가 없으면 예전처럼 **분기 전체**가 나갑니다.
- 엑셀 제출본 제목에 **(선택 3건 · 출장자 5명)** 이 붙고 파일명에도 `선택3건` 이 들어갑니다 —
  받는 쪽이 '전체인 줄' 알 수 없게 합니다. **머리글 27칸은 그대로**라 센터 양식은 유지됩니다.

## 안전장치

- 없는 출장 id 가 섞여 들어와도 조용히 무시합니다(사이에 삭제된 건이 있어도 오류로 막지 않음).
- 한 번에 800건을 넘으면 **조용히 자르지 않고 분기 전체로 되돌리고** 화면에 그 사실을 적습니다 —
  일부만 빠진 제출본이 더 위험합니다.
- 아이패드 단일 파일 배포본도 같은 규칙으로 동작합니다(서버 없이 브라우저에서 파일 생성).

## 덤 — 잘려 있던 선택칸 두 곳

내보내기를 만들다 상태 필터가 `출장 확정 · 예…` 로 잘려 있는 것이 보여, 화면 전체의
**select 글자 잘림**을 새로 검사했더니 두 곳이 나왔습니다.

| 자리 | 전 | 후 |
|---|---|---|
| 출장 내역 상태 필터 | `출장 확정 · 예산 반영` 이 잘림 | 좁은 칸용 짧은 이름(`출장 확정` · `실적·인폼`) + 칸 폭 104px |
| 출장 계획 등록 `이전 출장 복사` | 긴 항목이 통째로 잘림 | 16자에서 끊고 `…` · 전체 문구는 마우스를 올리면 표시 |

`select` 는 글자가 넘쳐도 `scrollWidth` 가 늘지 않아 기존 '잘린 글자' 검사에 걸리지
않았습니다. **고를 수 있는 항목 중 가장 긴 것**이 들어가는지 재는 검사를 새로 넣었습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §36 | 머리글 27칸 동일 · 고른 건만 · 행 수 = 그 건의 출장자 수 |
| test_api §36 | 없는 id 무시 · 상한 초과 시 전체로 되돌림 |
| test_api §36 | 엑셀 제목·파일명에 선택 건수 표시 |
| e2e §28 | 필터 없으면 gids 없음 · 걸러진 수만큼 gids · 실제로 받은 파일 행 수 |
| e2e §28 | 체크박스로 고른 것이 있으면 그것만 |
| test_api §36 | **여러 건**(인원 3·1·2·4명)을 한 번에 — 행 수 = 각 건의 출장자 합 |
| test_api §36 | 확정이 아닌 건의 사람은 한 명도 섞이지 않는가 |
| win_chrome | 모든 화면의 select 가 **가장 긴 항목**을 담을 수 있는가 |

### 여러 건 확인

확정 건은 보통 여러 건이라, **6건(출장자 13명)** 을 걸러 실제로 받아 봤습니다 —
전체 21행 중 **13행**만 담겼고, 건별 인원(3·1·2·4·1·2명)이 그대로 맞았습니다.
엑셀 제목도 `(선택 6건 · 출장자 13명)`. 이 조건을 테스트로 고정해 뒀습니다.

검증: **API 660 / 브라우저 E2E 249 / 윈도우 크롬 135 / 설정 보존 22 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.22 (2026-08-03) — 글꼴 · JSON 입력 · 결재 링크와 작성 도우미

## 1. 화면 글꼴 Pretendard, 엑셀은 맑은 고딕

- 화면은 **Pretendard 가변 폰트** 한 개(`PretendardVariable.woff2`)로 400·700 을 다 냅니다.
  파일이 없으면 예전처럼 **맑은 고딕으로 조용히 내려갑니다** — 안 올려도 동작합니다.
- 라이선스(SIL OFL)를 폰트 옆에 함께 둡니다. 글꼴 이름은 `TB UI` 로 싣습니다
  (OFL 의 예약 이름 규정을 건드리지 않기 위해).
- **엑셀 내보내기(.xls)는 맑은 고딕 고정**입니다. 받는 PC 에는 Pretendard 가 없어서,
  제출본이 사람마다 다르게 보이면 안 됩니다. 일괄 등록 양식(.xls)도 같습니다.
  (기존 Trebuchet MS 혼용도 함께 정리했습니다)
- 아이패드 단일 파일에는 **150KB 서브셋**을 안에 심습니다(전체 2.0MB 대신).
  서브셋에 없는 드문 글자는 맑은 고딕으로 나옵니다 — 데모용이라 그대로 둡니다.

## 2. JSON 으로 출장 등록 (사내 LLM 연계)

**엑셀 일괄 등록** 칸에 **JSON 을 붙여넣어도** 같은 미리보기·같은 등록 버튼으로 들어갑니다.

- 화면 **[JSON 형식 보기]** → **[형식 복사]** 를 누르면 사내 LLM 에게 그대로 줄 사양이 복사됩니다.
  **지금 등록된 CCG팀 목록까지 채워서** 복사되므로 팀명이 틀릴 일이 줄어듭니다.
- **[JSON 예시 넣어보기]** 로 형태를 눈으로 확인할 수 있습니다.
- 관대하게 받습니다 — 객체 하나만, `{"groups":[…]}`, `2026.8.4`, `"120,000원"`, CCG 코드 8자리.
- 막습니다 — 도시·업체·목적·출발일 없는 건, 성명·사번 없는 사람, 알 수 없는 CCG팀.
- 문서: `JSON_INPUT.md` (화면과 같은 사양 + 사용 예)

> 붙여넣기만으로는 저장되지 않습니다. 미리보기를 보고 **등록** 을 눌러야 원장에 들어갑니다.

## 3. 결재 — 사내 사이트 링크 + 작성 도우미

- **시스템 설정**에 `사내 결재 사이트` 주소 칸이 생겼습니다(`http(s)://` 만 허용).
- 출장 내역 행의 **⋯ → 결재 작성 도우미** 를 누르면, 결재 창에 넣을 값이
  **항목 : 값** 표로 나옵니다. 줄마다 **복사**, 위에 **전체 복사**, 그리고
  **사내 결재 사이트 열기 ↗**.
- **실적이 입력된 건은 실적 금액**, 아직이면 계획 금액을 보여줍니다.
- 값을 대신 써 넣지 않습니다 — 결재는 사내 사이트에서 사람이 올립니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §37 | 폰트 파일·라이선스 존재 · 가변 선언 · 없으면 맑은 고딕 |
| test_api §37 | **엑셀에 화면 글꼴이 새지 않음**(Pretendard·TB UI 금지, 맑은 고딕 있음) |
| test_api §37 | 정적판은 글꼴을 파일이 아니라 안에 심는가 |
| test_api §37 | JSON 파서 — 배열/단일/`{groups}` · 필수 항목 · CCG 이름·코드 |
| test_api §37 | 결재 주소 저장 · `javascript:` 등 거부 · 거부돼도 기존 값 유지 |
| test_api §37 | 도우미가 실적/계획을 상태에 맞게 보여주는가 · 링크 `noopener` |
| test_api §31 | 정적판 기본 설정에 `approval_url` 이 함께 있는가 |

검증: **API 688 / 브라우저 E2E 249 / 윈도우 크롬 135 / 설정 보존 22 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.23 (2026-08-03) — 테마 손질: 선을 줄이고 여백을 늘렸습니다

"올드해 보인다"는 지적에 따라, 기능은 그대로 두고 **보이는 규칙만** 손봤습니다.
색·상태·계산은 한 줄도 바뀌지 않았습니다.

| 무엇 | 전 | 후 | 이유 |
|---|---|---|---|
| 모서리 | 컨트롤 3px · 카드 4px | **6px · 10px** | 각진 모서리가 오래된 인상의 큰 몫이었습니다 |
| 카드 | 진한 1px 테두리 | **실낱 테두리 + 뜬 그림자** | 상자를 선으로 가두지 않고 띄웁니다 |
| 표 머리 | 회색 띠 배경 | **배경 없이 선 하나** | 화면 위쪽이 무거워 보이던 원인 |
| 표 행 높이 | 8px | **12px** | 눈이 행을 따라가기 쉬워집니다 |
| 카드 안쪽 | 16px | **24px** | 글이 테두리에 붙어 있었습니다 |
| 본문 바깥 | 24px | **32px** (좁은 화면은 24px) | 화면이 꽉 차 보이던 것 |
| 구분선 | `#D5DBE3` | **`#E3E8EF`** (한 톤 옅게) | 선이 내용보다 먼저 보였습니다 |
| 사이드바 | 흰색 | **`#FBFCFD`** (반 톤 낮춤) | 본문의 흰 카드가 떠 보이게 |
| 화면 제목 | 20px | **24px** | 어느 화면인지 먼저 읽히게 |

- 좁은 화면(1279px 이하)에서는 바깥 여백·카드 안쪽을 예전 값으로 되돌려
  **표가 가로로 밀리지 않게** 했습니다.
- **인쇄물은 그대로** 테두리로 구분합니다(그림자는 종이에 안 나옵니다).
- 출장자용 안내문도 같은 팔레트·모서리로 맞췄습니다.

## 척도 갱신

| 척도 | 값 |
|---|---|
| 모서리 | `--r-ctl 6` · `--r-box 10` · `--r-pill 알약` · `--r-bar 3` |
| 여백 | 4 · 8 · 12 · 16 · 24 · **32** (32 = 본문 바깥) |
| 글씨 | 12 · 13 · 14 · 16 · 20 · **24** · 32 (24 = 화면 제목) |

테스트의 척도 상수도 함께 올렸습니다 — 다음에 누가 28px 을 쓰면 여전히 실패합니다.

검증: **API 688 / 브라우저 E2E 249 / 윈도우 크롬 135 / 설정 보존 22 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.24 (2026-08-03) — 표 잘림 · 태그(#) · 국내 출장 정산서 도우미

## 1. 출장 내역 — 뒤 칸이 밀려 나가던 것

출장 이름이 길면 오른쪽 칸(계획·실적·관리)이 밀려 나갔습니다. 가로 스크롤은 있었지만
한눈에 보이는 편이 낫습니다.

- **출장 칸에서만 말줄임**(…)합니다. 나머지 칸은 항상 제자리에 보입니다.
- **행을 누르면** 잘린 이름 전부와 출장자별 내역이 열립니다.
- **`내역 ▼` 버튼을 없앴습니다** — 행 클릭과 같은 일을 하는 중복이었습니다.
  버튼·체크박스·⋯ 메뉴 위에서 누른 것은 그쪽 동작이라 열리지 않습니다.
- 표 위에 무엇을 누르면 되는지 한 줄 적어 뒀습니다.

## 2. 태그(#) — 선택 입력, 분류·검색용

- **출장 계획 등록**에 `태그` 칸이 생겼습니다(선택). `CMP, 정기 Audit` 처럼 쉼표로 여러 개.
  **이미 쓰인 태그는 눌러서** 넣을 수 있고(오타로 갈리지 않게), **새 태그는 그냥 쳐서** 만듭니다.
- **출장 내역**에서 태그가 칩으로 보이고, **누르면 그 태그만** 걸러집니다. 전체 검색어에도 걸립니다.
- **⋯ → 태그 편집** 으로 언제든 고치거나 지웁니다. 분류용이라
  **이관·완료된 건도** 고칠 수 있습니다(금액·상태는 건드리지 않습니다).
- 저장은 `#` 없이 하고 화면에서 `#`를 붙입니다. 8개·각 20자까지.
- 감사 로그에 `태그 변경 … → …` 로 남습니다.

> `groups[].tags` 는 새 칸입니다. 옛 버전으로 되돌려도 원장은 그대로 읽히고,
> 그 버전에서 저장한 건의 태그만 사라집니다 — UPGRADE.md 에 적어 뒀습니다.

## 3. 국내 출장 정산서 작성 도우미

사내 결재 주소를 기본값으로 넣었습니다(시스템 설정에서 수정 가능).

```
http://apv.skhynix.com/Website/Approval/Forms/Form_SRC.aspx?fmid=…&mode=DRAFT&…
```

**⋯ → 정산서 작성 도우미** 를 누르면 **결재 창의 칸 순서 그대로** 값을 꺼내 줍니다.

| 정산서 칸 | 우리가 채우는 값 |
|---|---|
| 출장 목적 | 출장 목적&사유 |
| 세부일정 | 일자 · 도시 · 업체 · 목적 한 줄 |
| 방문회사 | 출장 기관&업체 |
| 방문자 | 우리 쪽 출장자 (상대측 담당자는 결재 창에서) |
| 목적지 주소 및 전화번호 | **시스템에 없는 값** — 그렇다고 표시합니다 |
| 사번 · 성명 | 고른 출장자 |
| 출장일시 | 출발 ~ 복귀 |
| 교통편 | 자차 여부로 미리 골라 둠 (선택지 10종 함께 표시) |
| 출발지 | 결재 창에서 선택 (사업장 4곳·거주지 목록 표시) |
| 출장지 | 도시 · 업체 (주소는 결재 창에서 [추가]) |
| 비용 4항목 + 합계 | 실적이 있으면 실적, 없으면 계획 |

- ~~정산서는 사번이 한 개라 사람별로 올립니다~~ → **v10.25 에서 정정**: 정산서는 인원을 추가할 수 있어
  **한 출장을 한 장**으로 올립니다.
- 줄마다 **복사**, 위에 **전체 복사**, 그리고 **정산서 열기 ↗**.
- 증빙(카드 지불 정보)은 결재 창이 직접 불러오므로, **합계와 대조**하라고 안내합니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §38 | 출장 칸만 말줄임 · 행 클릭으로 열림 · 버튼 위에서는 안 열림 · 토글 버튼 제거 |
| test_api §38 | 태그 정리 규칙(`#` 제거·중복·8개·20자) · 등록/수정/삭제 · 감사 로그 |
| test_api §38 | 완료된 건도 태그는 붙는가 · 정적판도 같은 규칙 |
| test_api §38 | 정산서 10개 칸이 다 있는가 · 교통편 10종 · 출발지 목록 · 사람별 |
| test_api | `data.json` 새 칸은 `tags` 하나뿐인가(되돌리기 안전 목록에 명시) |

검증: **API 723 / 브라우저 E2E 249 / 윈도우 크롬 135 / 설정 보존 22 / Pages 25 /
붙여넣기 53 / 퍼징 1,302회 0건 / 디자인 감사 OK 13.**


---

# v10.25 (2026-08-03) — 정산서는 한 장에 인원을 추가한다 (v10.24 정정)

v10.24 에서 "정산서는 사번이 한 개라 사람별로 올린다"고 만들었는데, **사실이 아니었습니다.**
정산서는 **인원을 [추가]** 할 수 있어 한 출장을 한 장으로 올립니다. 화면을 그 구조로 고쳤습니다.

| 전 (v10.24) | 후 (v10.25) |
|---|---|
| 위에서 사람을 고르면 그 사람 기준 17줄 | **공통 10줄 + 인원 표** 한 화면 |
| 사번·성명이 표 안의 한 줄 | 인원 표에 **성명·사번·소속 CCG·비용 4항목·합계** |
| 합계 = 고른 사람 것 | **총합계 = 출장 전체** (증빙 카드 총액과 대조할 값) |
| 전체 복사 = 한 사람 분 | **전체 복사 = 공통 + 인원 표 전부** |

- 사람 줄마다 **복사** 가 있어 그 한 명 값만 탭으로 나눠 담습니다
  (정산서에서 인원을 추가하며 한 줄씩 붙일 때).
- 전체 복사는 **탭으로 나눈 텍스트**라 결재 창에도, 엑셀에도 칸이 맞게 붙습니다.

```
출장 목적	Wafer 표면 결함 정기 Audit
세부일정	07/25–07/26 구미 SK실트론 · Wafer 표면 결함 정기 Audit
…
인원 2명
성명	사번	소속 CCG	계획 교통비	숙박비	식대&잡비	기타	합계
한지우	20210302	P&C소재 (50128121)	120,000	100,000	70,000	20,000	310,000
오세훈	20160828	소재전략 (50110502)	120,000	100,000	70,000	20,000	310,000
계획 총합계	620,000원
```

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §38 | 한 장에 인원을 담는가 · 공통/인원을 나눠 보여주는가 |
| test_api §38 | **사람별로 쪼개는 코드가 다시 생기면 실패** |
| test_api §38 | 인원 표에 성명·사번·소속·금액이 있는가 · 탭으로 나눠 복사하는가 |

검증: **API 726 / 브라우저 E2E 249 / 윈도우 크롬 135 / Pages 25 / 디자인 감사 OK 13.**


---

# v10.26 (2026-08-06) — 더미 데이터 제거 · 일괄 등록도 태그를 싣는다

## 1. 정적판(맥·아이패드·Pages)이 가짜 출장 8건을 깔고 시작하던 것

사내 Flask 는 v9.4 부터 **빈 원장**으로 시작했는데, 정적판만 첫 실행에
예시 8건 + 예산 리비전 2건을 `localStorage` 에 심고 있었습니다.
첫 화면의 `9,049,000원` 이 실데이터처럼 보이던 원인입니다.

| 전 | 후 |
|---|---|
| `Store.load()` → `seedData()` (출장 8건·예산 2건) | `defaultData()` — **출장 0건·예산 0건** |
| 예시가 첫 실행 경로에 있음 | `exampleData()` 로 분리, 첫 실행에서 호출 안 함 |

설정(안내 문구·메일 수신자·CCG·결재 주소)은 그대로 들어갑니다 — 지운 것은 **원장**뿐입니다.
예시가 필요하면 `window.__tbExample()` 또는 `data.example.json` 을 쓰면 됩니다.

**사내 Flask 서버는 이 변경의 영향을 받지 않습니다** — 원래도 빈 원장이었고,
이미 원장이 있는 서버에서는 첫 실행 경로 자체가 돌지 않습니다.

## 2. JSON 일괄 등록이 태그를 버리던 것

v10.24 에서 태그(`#`)를 넣었는데 `bParseJson` 이 `tags` 를 옮기지 않아,
**화면에서 한 건씩 넣으면 태그가 붙고 JSON 으로 대량 적재하면 안 붙었습니다.**
서버(`normalize_group` → `clean_tags`)는 원래 받고 있었으므로 화면만 고쳤습니다.

- `tags` 를 계획 폼과 같은 경로로 넘깁니다(정리는 서버가 합니다).
- 화면의 **[JSON 형식 보기] → [형식 복사]** 와 `JSON_INPUT.md` 에 `tags` 칸을 넣었습니다.
- `["CMP","정기 Audit"]` · `"CMP, 정기 Audit"` · `"#CMP"` 를 모두 같게 읽습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api | 정적판도 첫 실행이 빈 원장인가 · 예시를 자동으로 깔지 않는가 |
| test_api §38 | JSON 일괄 등록이 태그를 흘려보내는가 |
| e2e_pages | **첫 실행 빈 원장** — 그 뒤 검사는 픽스처를 직접 깔고 시작 |

검증: **API 730 / 브라우저 E2E 249 / 윈도우 크롬 135 / Pages 26 / 설정 보존 22 /
디자인 감사 OK 13 / 패키지 스모크 19.**


---

# v10.27 (2026-08-06) — 문답으로 끝내기

**대시보드 한 화면**에서 묻는 대로 고르면 등록까지 끝납니다.
한 화면에 질문 하나, **고르면 바로 다음**으로 넘어갑니다.

## 처음 한 번만 "누구세요?"

원장에 이미 있는 분은 이름을 누르면 끝이고, 없으면 직접 넣습니다.
이 브라우저에 기억해 두고 **다음부터는 묻지 않습니다.**

신원을 알게 되니 흐름이 짧아집니다.

- **계획**: 본인은 출장자에 자동으로 들어갑니다 — 같이 가는 분만 고르면 됩니다.
- **실적**: **내가 간 출장만** 보여 줍니다. 사번을 칠 필요가 없습니다.

## 계획 다섯 문 · 실적 두 문

| 계획 | 무엇이 채워지나 |
|---|---|
| 어디로 가시나요? | 전에 다녀온 곳을 누르면 도시·업체가 함께 (직접 입력도) |
| 언제 다녀오시나요? | **달력** 두 칸 + 자차 사용 — 자차를 누르면 바로 다음 |
| 무슨 일로 가시나요? | 목적을 적고 **구분을 누르면 바로 다음** |
| 같이 가시는 분이 있나요? | 전에 간 분을 누르면 사번·직책·CCG까지 |
| 1인당 얼마쯤 드나요? | 금액 넷 + 1인당 합계 |

실적은 **어느 출장**(누르면 바로 다음)과 **얼마**(계획대로 썼습니다 한 번이면 끝) 둘뿐입니다.

구분·태그·비고는 마지막 확인 화면에서 **필요할 때만 펼쳐** 넣습니다 — 늘 묻지 않습니다.

## 고를 수 있는 것은 고르게 합니다

주관식으로 받으면 그 글을 해석해야 하고, 해석하는 곳마다 틀릴 자리가 생깁니다.
그래서 구분·자차·직책·CCG·태그는 전부 눌러서 고르고, 날짜는 달력을 씁니다.
자유롭게 적는 칸은 **목적·비고**뿐이고 그건 해석하지 않습니다.

## 지킨 선

**문답이 스스로 저장하지 않습니다.** 지나온 답이 위에 한 줄씩 쌓이고(눌러서 고칠 수 있습니다),
마지막 확인 화면에서 사람이 누릅니다. 저장은 지금까지 쓰던
`POST /groups` · `POST /groups/<gid>/actual` 을 그대로 지나가므로 검증·권한·감사 로그가
전부 살아 있습니다. 예산 부족 확인창, 이관 시작된 건의 관리자 인증도 그대로입니다.

실적은 **내 줄만 보냅니다** — 같이 가신 분 실적은 건드리지 않고, 아직 비어 있으면 알려 줍니다.

## 만들며 고친 것

- 처음 화면의 사람 목록이 길어 **부서별 현황이 첫 화면 밖으로 밀렸습니다**(1366×768에서 1384px).
  이름은 칩으로 눕히고, 직접 넣는 칸은 접고, 두 갈래를 나란히 두어 되돌렸습니다(719px).
- 칩을 고를 때 카드를 통째로 다시 그려 **같은 화면에 이미 친 글이 지워졌습니다.**
- 화면 두 곳에 상자를 두면서 **같은 id 를 써서** 실적 화면에서 계획 쪽 상자를 그렸습니다.
- 목적에 이미 든 말이면 출장 구분을 또 찍지 않고, 0원인 비목은 빼고 합계만 덧붙입니다.
- 체크박스가 입력칸 폭(200px)으로 늘어나던 것.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §39 | 신원을 한 번만 묻고 기억하는가 · 본인이 출장자에 자동으로 들어가는가 |
| test_api §39 | 고르게 하는가(구분·자차·직책·CCG·태그) · 날짜가 달력인가 · 고르면 바로 다음인가 |
| test_api §39 | **주관식 해석기가 다시 생기면 실패** · **기존 라우트로만 저장하는가** |
| e2e | 부서별 현황이 1366×768 첫 화면 안에 남아 있는가 |
| e2e_pages | 눌러 넣어 등록·실적 저장까지 · **내 출장만 보이는가** · 내 줄만 저장되는가 |

검증: **API 780 / 브라우저 E2E 249 / 윈도우 크롬 135 / Pages 32 / 설정 보존 22 /
디자인 감사 OK 13.**


---

# v10.28 (2026-09-09) — 이름을 치면 사번이 따라옵니다

조직도를 붙일 수 없어서, **지금까지 간 사람의 이력을 명부로 씁니다.**
계획을 새로 넣을 때 이름만 치면 사번·직책·CCG팀이 이력에서 따라옵니다.

| 친 이름 | 화면이 하는 일 |
|---|---|
| 이력에 한 명 | 사번·직책·CCG를 **바로 채우고** `이력에서 채웠습니다 — 2071478 · TL · C&C소재기술` |
| **같은 이름이 둘 이상** | `같은 이름이 2명입니다 — 고르세요` 와 함께 **둘 다 사번·CCG를 붙여 띄웁니다** |
| 앞글자만 (`이`) | 그 글자로 시작하는 사람을 후보로 (최대 6명) |
| 이력에 없음 | `이력에 없는 이름입니다 — 사번과 CCG팀을 넣어 주세요` |

**소재에 아직 동명이인은 없습니다.** 생겼을 때 조용히 엉뚱한 사번이 들어가지 않도록
미리 갈라 두는 것이 이 기능의 목적입니다.

붙은 곳은 두 군데 — **누구세요?** 의 직접 입력, **같이 가시는 분** 의 직접 입력.

## 왜 조직도 대신 이력인가

조직도·SAP 연동은 아직 사내에서 열 수 없습니다(AX 로드맵 Phase 1).
그런데 **출장을 한 번이라도 간 사람은 이미 원장에 있습니다.** 새 명부를 만들 필요가 없습니다.
데이터가 쌓일수록 정확해지고, 외부 연동이 하나도 늘지 않습니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §39 | 이름 → 사번·직책·CCG 를 이력에서 채우는가 · 두 입력칸 모두에 붙었는가 |
| test_api §39 | **동명이인이면 둘 다 띄우고 고르게 하는가** · 앞글자 후보 · 이력에 없을 때 안내 |
| test_api §39 | 힌트 칸만 갈아 끼우는가 (다시 그리면 이미 친 글이 지워진다) |
| e2e_pages | 실제로 이름만 쳐서 사번·CCG·직책이 따라오는가 |

검증: **API 787 / 브라우저 E2E 249 / 윈도우 크롬 135 / Pages 33 / 설정 보존 22 /
디자인 감사 OK 13.**


---

# v10.29 (2026-09-09) — 조직명 설정 · 다른 부서가 그대로 쓸 수 있게

시스템 이름·관리자·CCG·수신자·주소는 이미 설정이었는데, **"소재"라는 말이 코드에 박혀 있었습니다.**
이름만 바꾼 부서의 센터 제출본에 `소재`가 찍히고, 이관 인폼에 `소재 담당자`가 나갔습니다.

## 조직명 한 칸

설정 화면에 **조직명**(`org_name`, 기본 `소재`, 20자)을 두고 아래가 전부 따라갑니다.

| 자리 | 전 | 후 |
|---|---|---|
| 센터 양식 LV2 열 | `"소재"` 하드코딩 | 저장된 `lv2`, 없으면 조직명 |
| 엑셀 제목 | `소재 국내 출장비 정산 대장` | `{조직명} 국내 출장비 정산 대장` |
| 이관 인폼 수신자 힌트 | `소재 담당자` | `{조직명} 담당자` |
| 상태 표시 · 안내 · 처리 버튼 | `소재 이관` · `소재 담당자` | `{조직명} 이관` · `{조직명} 담당자` |
| 새 건의 `lv2` | `소재` | 조직명 |

**저장된 상태 문자열(`소재 이관`)은 바꾸지 않습니다** — 스키마입니다. 표시만 바꿉니다.
옛 원장은 `org_name`이 없어도 `소재`로 읽히므로 마이그레이션이 없습니다.

## 고친 버그

`ledger_rows` 가 저장된 `lv2` 를 읽지 않고 `"소재"` 를 찍고 있었습니다. 이제 저장값을 씁니다.

## 재발 방지

| 테스트 | 검사 |
|---|---|
| test_api §40 | LV2 열이 저장값→조직명 순으로 · 엑셀 제목 · 인폼 힌트 · 새 건 `lv2` |
| test_api §40 | 설정 API 저장·조회·state 노출 · 비우면 `소재` · 20자 상한 |
| test_api §40 | 화면에 `소재 담당자` 하드코딩이 남지 않는가 · 정적판 동일 규칙 |
