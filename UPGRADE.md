# 데이터를 지키면서 버전 올리기 (초보자용)

이미 출장 데이터가 쌓여 있는 서버를 **v10.28 로 올리는 방법**입니다.
지금 쓰는 버전이 v9.4든 v10.x든 절차는 같습니다.

> **결론부터.** 데이터는 `data.json` **파일 하나**에 들어 있고, 이번 업그레이드는
> **프로그램 파일 5개만 갈아끼웁니다.** 그 5개 안에 데이터는 없습니다.
> 아래 순서대로만 하면 데이터는 그대로입니다.

---

## 0. 시작 전에 — 딱 하나만 기억하세요

### ❌ 절대 하지 말 것

```
servera\travelbudget 폴더를 지우고 → 새 폴더를 붙여넣기
```

**데이터가 이 폴더 안에 들어 있습니다.** 폴더를 지우면 원장과 자동 백업 30개가 함께 사라집니다.
윈도우 탐색기의 **"덮어쓰기"는 안전**합니다(파일만 바뀌고 나머지는 남음).
**"폴더 삭제 후 붙여넣기"만 위험**합니다.

```
servera\travelbudget\
├─ core.py              ← 프로그램 (바꿈)
├─ routes.py            ← 프로그램 (바꿈)
├─ store.py             ← 프로그램 (안 바꿈)
├─ __init__.py          ← 프로그램 (안 바꿈)
├─ static\app.js        ← 프로그램 (바꿈)
├─ templates\           ← 프로그램 (바꿈)
└─ data_json\           ★ 여기가 데이터. 절대 건드리지 않습니다
   ├─ data.json            ← 출장·예산·사람 전부 이 파일 하나
   └─ backup\              ← 자동 백업 30개
```

---

## 1단계. 지금 숫자를 적어 둡니다 (2분)

올린 뒤에 **같은 숫자인지 대조**하기 위해서입니다. 다르면 되돌리면 됩니다.

**화면에서 확인하는 방법 (가장 쉬움)**

1. 브라우저로 `material.skhynix.com/travelbudget` 접속
2. **출장 내역** 메뉴 → 목록 맨 위의 **전체 건수**
3. **대시보드** → **총 예산 / 처리 완료 / 처리 중 / 확정 예정 / 가용 잔여**

종이나 메모장에 이렇게 적어 두세요.

```
날짜: 2026-__-__
출장 건수: ______건
총 예산:  ____________원
처리 완료: ____________원
처리 중:  ____________원
확정 예정: ____________원
가용 잔여: ____________원
```

**명령어로 확인하는 방법 (더 정확 · 이쪽을 권합니다)**

서버에서 아래를 실행하면 데이터 위치와 숫자, 그리고 **내가 고쳐 둔 설정**을 한 번에 찍어 줍니다.
**읽기만 하고 아무것도 바꾸지 않습니다.**

```
python check_data.py
```

출력 아래쪽에 이런 블록이 나옵니다. **이 화면을 그대로 캡처해 두세요.**

```
  ── 내가 고쳐 둔 설정 (업그레이드 후 이 블록이 그대로여야 합니다) ──
  설정 지문    5b364e5cb109      ← 올린 뒤 다시 실행해서 이 값만 비교하면 됩니다
  시스템 이름  소재 국내 출장비 관리
  안내 문구    2026-3Q 소재 예산 소진 — 센터 예산 사용 중입니다. 식비 15,000원 이내로…
  안내 보조    (센터 검토 시 반려될 수 있음)
  참고 주소    material.skhynix.com/travelbudget
  담당자 암호  기본값(2071478)
  인폼 수신인  4명
               · junghoon12.lee@sk.com
               · eunjeong5.kim@sk.com
               · Jeewoung.Chun@sk.com
               · geonyoung.kim@sk.com
  CCG 목록     8팀  (화면 [시스템 설정]에서 고쳐 둔 것 — data.json 에 있음)
               · 소재전략 50110502
               · P&C소재 50128121
               ...
```

**설정 지문(12자리)** 하나만 적어 두셔도 됩니다. 올린 뒤 같은 명령을 다시 실행해서
**이 12자리가 같으면 설정이 한 글자도 안 바뀐 것**입니다.

---

## 1-1단계. 왜 설정이 안 흐트러지는가 (읽어두면 마음이 놓입니다)

이 시스템은 **프로그램**과 **데이터**가 완전히 갈라져 있습니다.

| 무엇 | 어디에 있나 | 업그레이드 때 |
|---|---|---|
| 출장·예산·감사 로그 | `data_json/data.json` | **건드리지 않습니다** |
| 안내 문구 · 안내 보조 문구 | `data.json` 안 `settings.notice` | **건드리지 않습니다** |
| 인폼 메일 수신인 | `data.json` 안 `settings.mail_recipients` | **건드리지 않습니다** |
| CCG 목록 (화면에서 고친 경우) | `data.json` 안 `settings.ccg_teams` | **건드리지 않습니다** |
| 담당자 비밀번호 · 시스템 이름 · 참고 주소 | `data.json` 안 `settings` | **건드리지 않습니다** |
| 화면·계산 로직 | `core.py` · `routes.py` · `app.js` · `index.html` · `traveler_guide.html` | 이 5개만 갈아끼웁니다 |

프로그램은 켜질 때 `data.json` 을 **그대로 읽기만** 합니다.
새 버전의 기본값을 덮어쓰거나 섞지 않습니다(코드에 합치는 로직 자체가 없습니다).
그래서 **파일 5개만 바꾸면 설정은 손댈 수 없습니다.**

### 다만 이건 반대로도 참입니다 — 새 기본값이 자동으로 들어오지도 않습니다

지금 서버의 수신인이 **옛날 2명**이면, 새 버전을 올려도 **그대로 2명**입니다.
새 버전에 적어 둔 4명은 **처음 설치하는 서버에만** 쓰이는 값입니다.
그래서 **8단계(올린 뒤 화면에서 해야 하는 일)** 가 따로 있습니다.

### CCG 목록은 두 곳 중 한 곳에서 옵니다

| 상황 | 어디서 오나 | 업그레이드 영향 |
|---|---|---|
| 화면 **[시스템 설정]** 에서 **한 번이라도 저장**했다 | `data.json` (여기가 이깁니다) | 없음 — 그대로 |
| 저장한 적이 한 번도 없다 | 프로그램 기본값 (`core.py`) | 새 버전의 8팀 목록으로 보입니다 |

`check_data.py` 가 둘 중 어느 쪽인지 알려줍니다
("화면에서 고쳐 둔 것 — data.json 에 있음" / "프로그램 기본값(core.py)을 씁니다").

> **알아두면 좋은 것** — [시스템 설정]에서 **메일 주소만 고쳐 저장해도**,
> 그 순간의 CCG 목록이 `data.json` 에 함께 저장됩니다. 그 뒤로는 프로그램을
> 아무리 올려도 CCG 목록은 화면에서 고치지 않는 한 바뀌지 않습니다.

---

## 2단계. 데이터가 어디 있는지 확인합니다 (1분)

두 경우뿐입니다. `python check_data.py` 가 알려주지만, 눈으로 확인해도 됩니다.

| 경우 | 확인 방법 | 이후 절차 |
|---|---|---|
| **A. 앱 폴더 안** (대부분 여기) | `servera\travelbudget\data_json\data.json` 이 있음 | 3단계부터 그대로 |
| **B. 앱 폴더 밖** | 서비스 환경변수에 `TB_DATA_DIR` 이 잡혀 있음 | 3단계부터 그대로 (더 안전) |

---

## 3단계. 백업합니다 ★ 가장 중요 (2분)

**앱 폴더 바깥에** 복사해 둡니다. 안에 두면 나중에 같이 지워질 수 있습니다.

윈도우 탐색기에서:

1. `servera\travelbudget\data_json` 폴더를 마우스 우클릭 → **복사**
2. 바탕화면(또는 `D:\백업`)에 **붙여넣기**
3. 폴더 이름을 `data_json_백업_20260731` 처럼 **날짜를 붙여** 바꿔 둡니다

명령 프롬프트를 쓴다면:

```
xcopy /E /I servera\travelbudget\data_json D:\백업\data_json_20260731
```

> 이 백업 하나만 있으면 **무슨 일이 있어도 원래대로 되돌릴 수 있습니다.**
> 복사가 끝나면 백업 폴더 안에 `data.json` 이 실제로 보이는지 눈으로 확인하세요.

---

## 4단계. 서버를 멈춥니다 (1분)

어떻게 띄웠느냐에 따라 다릅니다.

| 띄운 방법 | 멈추는 법 |
|---|---|
| 검은 창(명령 프롬프트)에서 `python webmain.py` | 그 창을 클릭하고 **Ctrl + C** |
| 윈도우 서비스로 등록 | **서비스** 앱 → 해당 서비스 → **중지** |
| 잘 모르겠음 | 작업 관리자 → **세부 정보** 탭 → `python.exe` → **작업 끝내기** |

브라우저에서 새로고침했을 때 **접속이 안 되면** 멈춘 것입니다.

---

## 5단계. 파일 5개만 덮어씁니다 (3분)

받은 `travelbudget_flask_v10.28.zip` 의 압축을 풀면 이런 구조입니다.

```
travelbudget_flask_v10.28\
└─ servera\
   └─ travelbudget\
      ├─ core.py
      ├─ routes.py
      ├─ static\app.js
      └─ templates\index.html, traveler_guide.html
```

**이 5개만** 서버의 같은 자리에 덮어씁니다.

```
servera\travelbudget\core.py
servera\travelbudget\routes.py
servera\travelbudget\static\app.js
servera\travelbudget\templates\index.html
servera\travelbudget\templates\traveler_guide.html
```

탐색기에서 하는 법:

1. 압축 푼 폴더에서 `core.py` 와 `routes.py` 를 선택 → **복사**
2. 서버의 `servera\travelbudget\` 에서 **붙여넣기** → **"파일 바꾸기"** 선택
3. `static\app.js` 도 같은 방식으로
4. `templates\index.html`, `templates\traveler_guide.html` 도 같은 방식으로

> **v10.28 부터 글꼴 파일이 하나 늘었습니다.** 아래 5개와 함께
> `servera\travelbudget\static\fonts\` 폴더를 통째로 복사하세요
> (`PretendardVariable.woff2` · `Pretendard-LICENSE.txt` · `ui-subset.woff2`).
> 복사하지 않아도 동작합니다 — 그때는 예전처럼 맑은 고딕으로 보입니다.
>
> **`store.py` 와 `__init__.py` 는 건드리지 않습니다.**
> 데이터를 **저장하는 로직**은 v9.4 이후 한 줄도 바뀌지 않았습니다 — 저장 방식이 달라질 일이 없습니다.
>
> (v10.28 에서 `store.py` 의 **신규 설치용 기본값**만 바뀌었습니다 — 처음 설치할 때 넣어 줄
> CCG 목록과 수신인. 이미 데이터가 있는 서버는 원장에 저장된 설정을 쓰므로 이 값이
> 쓰이지 않습니다. 실제로 v9.4 의 `store.py` 그대로 v10.28 를 올려 원장이 한 글자도
> 바뀌지 않는 것을 확인했습니다. 트리를 맞추고 싶으면 같이 복사해도 안전합니다.)

---

## 6단계. 다시 켜고 확인합니다 (3분)

멈출 때와 반대로 켭니다(`python webmain.py` 또는 서비스 **시작**).

확인 순서:

1. **좌측 아래에 `v10.28 · 2026-08-06`** 이 보이는지 → 새 버전이 올라간 것
2. **1단계에 적어 둔 숫자와 같은지** → 출장 건수, 총 예산, 처리 완료, 가용 잔여
3. 출장 내역에서 **아무 건이나 열어** 사람·금액이 그대로인지
4. **설정이 그대로인지** — `python check_data.py` 를 다시 실행해
   **설정 지문 12자리가 1단계와 같은지** 봅니다. 같으면 안내 문구·수신인·CCG·비밀번호가
   전부 그대로라는 뜻입니다.
5. 눈으로도 한 번 — **대시보드 맨 위 안내 문구**가 내가 쓴 문장 그대로인지,
   **시스템 설정** 화면의 수신인·CCG 목록이 그대로인지
6. (가능하면) `python smoke_test.py` — 19개 항목을 읽기만 하며 점검합니다

**숫자나 설정 지문이 다르면 7단계로 되돌리고 알려 주세요.**

---

## 7단계. 되돌리는 법 (문제가 생겼을 때만)

되돌리기는 **두 종류**입니다. 대부분은 ①만으로 충분합니다.

### ① 프로그램만 되돌리기 (데이터는 그대로)

이전 버전 zip 에서 **같은 5개 파일**을 덮어쓰고 재기동합니다. 데이터는 손대지 않습니다.
실제로 v10.28 → v9.4 로 되돌려 봤고, **출장 건수·금액·상태·CSV 결과가 전부 원래와 같았습니다.**

### ② 데이터까지 되돌리기 (실수로 데이터를 건드렸을 때)

3단계에서 만든 백업 폴더를 `servera\travelbudget\data_json` 자리에 덮어씁니다.
(백업을 안 만들었다면 `data_json\backup\` 안에 **자동 백업 30개**가 있습니다 —
화면 **데이터 관리 → 백업 복원** 에서도 고를 수 있습니다.)

---

## 8단계. 올린 뒤 화면에서 해야 하는 일 ★ v10.28 전용 (5분)

**이번 버전은 여기까지 해야 끝납니다.** 새 CCG 목록과 새 수신인은 **자동으로 반영되지 않습니다.**

이유: 이미 쓰고 있는 서버에는 `data.json` 안에 **예전 설정이 저장돼 있고**, 프로그램은
저장된 설정을 코드 기본값보다 **우선**합니다. (담당자가 화면에서 고쳐 둔 값을
새 배포가 조용히 덮어쓰면 안 되기 때문입니다.)

좌측 메뉴 **시스템 설정** → 담당자 인증(2071478) 후 아래 순서로 하세요.

### 8-1. 인폼 수신인 4명으로 (1분)

기존에 2명만 들어 있고, **김은정 주소가 동명이인이라 틀려 있습니다.**
`+ 수신인 추가` 로 채우고 `설정 저장`.

```
junghoon12.lee@sk.com      이정훈 (소재)
eunjeong5.kim@sk.com       김은정 (소재)   ← 주소가 바뀌었습니다
Jeewoung.Chun@sk.com       천지웅 (센터)
geonyoung.kim@sk.com       김건영 (센터)
```

### 8-2. CCG 8팀은 이미 들어 있습니다 (확인만, 30초)

같은 화면의 **CCG팀** 표를 확인합니다. 두 경우가 있습니다.

- **CCG를 화면에서 고친 적이 없다면** — 아래 8줄이 **이미 보입니다**(프로그램 기본값). 확인만 하세요.
- **이미 화면에서 고쳐 두셨다면** — **고쳐 둔 목록이 그대로 보입니다.** 새 버전이 덮어쓰지 않습니다.
  바꿀 것이 없으면 아무것도 하지 않아도 됩니다.

| 팀 이름 | CCG 코드 |
|---|---|
| 소재전략 | 50110502 |
| P&C소재 | 50128121 |
| Patterning소재기술 | 50119135 |
| Patterning소재개발 | 50077468 |
| C&C소재기술 | 50139632 |
| C&C소재개발 | 50134405 |
| EDTW소재기술 | 50119134 |
| EDTW소재개발 | 50103536 |

### 8-3. 옛 코드를 새 팀으로 옮기기 ★ 이게 핵심 (2분)

화면 맨 아래 **`원장 CCG 정리`** 에 **지금까지 쌓인 출장의 옛 코드**가 건수와 함께 나옵니다.
줄마다 **옮길 팀을 고르고 [옮기기]** 를 누르세요. 옛 팀 수만큼 반복하면 끝입니다.

- 해당 출장자의 **CCG 코드·팀 이름만** 새 값으로 바뀝니다
- **금액·날짜·상태는 그대로입니다** — 리허설에서 처리 완료·처리 중·확정 예정·가용 잔여
  **네 숫자가 1원도 바뀌지 않는 것**을 확인했습니다
- 누르기 직전 **자동 백업**이 만들어지고 감사 로그에 남습니다 — 잘못 옮겼으면 복원할 수 있습니다

다 옮기면 `원장 CCG 정리` 카드가 사라집니다. **그러면 끝입니다.**

> **다음 해에 또 바뀌면**: CCG팀 표에서 이름·코드를 고치고 저장 → `원장 CCG 정리`에서 옮기기.
> 파일 교체도, 저에게 요청도 필요 없습니다.

---

## 자주 나오는 질문

**Q. 사람들이 쓰는 중인데 올려도 되나요?**
점심시간처럼 아무도 안 쓰는 때가 좋습니다. 올리는 동안(약 5분) 화면이 안 열립니다.
저장 중에 끄면 안 되니, 방금 저장한 사람이 없는지 확인하고 멈추세요.

**Q. 올리면 사람들이 다시 입력해야 하나요?**
아닙니다. 이미 입력된 출장·실적·예산·감사 기록은 그대로 있습니다.

**Q. 지금 버전이 v9.4 인지 v10.5 인지 모르겠습니다.**
화면 **좌측 아래**에 버전이 찍혀 있습니다. 어느 버전이든 절차는 같습니다.

**Q. CCG 팀이나 메일 수신자가 바뀌면 또 파일을 바꿔야 하나요?**
아닙니다. v10.28부터는 **좌측 메뉴 `시스템 설정`** 에서 화면으로 바꿉니다(예산 담당자 인증 필요).
이미 원장에서 쓰고 있는 CCG 코드는 실수로 지울 수 없게 막아 두었습니다.

**Q. 제가 고쳐 둔 안내 문구·수신인·CCG가 업그레이드로 날아가지 않나요?**
날아가지 않습니다. 그 값들은 **프로그램이 아니라 `data.json` 안**에 있고, 업그레이드는
`data.json` 을 열지도 않습니다. 새 버전의 기본값을 덮어쓰거나 섞는 로직 자체가 없습니다.
확인은 `python check_data.py` 의 **설정 지문 12자리**를 전·후로 비교하면 30초면 끝납니다.

**Q. 그럼 새 버전에 들어 있는 4명 수신인·8팀 CCG는 언제 쓰이나요?**
**처음 설치하는 빈 서버**에만 쓰입니다. 이미 데이터가 있는 서버는 자기 `data.json` 값을 씁니다.
그래서 지금 수신인이 옛날 2명이면 올린 뒤에도 2명입니다 — **8단계에서 화면으로 한 번 고쳐야** 합니다.

**Q. 담당자 비밀번호를 바꿔 뒀는데 기본값으로 돌아가나요?**
돌아가지 않습니다. 비밀번호도 `data.json` 안 설정입니다. `check_data.py` 가
"바꿔 두셨습니다"로 표시해 줍니다(값은 찍지 않습니다).

**Q. v10.28 의 태그(#)를 쓰다가 옛 버전으로 되돌리면 어떻게 되나요?**
원장은 그대로 읽힙니다. 태그는 `groups[].tags` 라는 **새 칸**이라, 옛 버전은 그 칸을 모른 채
지나갑니다. 다만 옛 버전에서 그 출장을 **저장하면 그 건의 태그만** 사라집니다
(금액·상태·예산에는 관여하지 않습니다). 되돌릴 계획이 있으면 태그를 잠시 쓰지 마세요.

**Q. 설정을 바꾸면 지난 분기 집계가 달라지나요?**
아닙니다. 원장에는 **출장 등록 시점의 CCG 코드**가 그대로 남습니다.
설정을 바꿔도 과거 숫자는 소급해서 바뀌지 않습니다.

---

## 이 절차를 실제로 리허설한 결과

문서만 쓰지 않고, **v9.4 서버를 띄워 데이터를 쌓은 뒤 위 절차 그대로 v10.28 로 올려** 봤습니다.

| 항목 | 리허설에 쓴 원장 | 업그레이드 후 |
|---|---|---|
| 출장 건수 | 15건 | **15건 (동일)** |
| 출장자 행 | 28명 | **28명 (동일)** |
| 예산 리비전 | 6건 · 배정 67,600,000원 | **동일** |
| 계획 합계 / 실적 합계 | 3,623,000 / 2,049,000원 | **동일** |
| 상태별 건수 | 계획4·확정3·실적3·이관3·완료2 | **동일** |
| 감사 로그 | 52건 | **52건 (동일)** |
| 대시보드 | 완료 451,000 / 처리중 1,598,000 / 확정 856,000 / 가용 64,695,000 | **동일** |
| 센터 제출 CSV | md5 `7af279cd…` | **md5 동일** |
| 자동 백업 | 30개 | **30개 (동일)** |
| **`data.json` 파일 자체** | md5 `56aa703b…` | **md5 동일 — 한 글자도 안 바뀜** |

그다음 **v9.4 로 되돌리는 것까지** 해 봤습니다 — 출장 건수·금액·상태·CSV 결과가 전부 원래와 같았고,
점검 스크립트도 19개 항목 전부 통과했습니다.

### 설정(안내 문구·수신인·CCG)도 따로 리허설했습니다

`python3 tools/e2e/settings_keep.py` — 옛 버전 서버에서 **안내 문구를 바꾸고, 수신인을 4명으로 고치고,
CCG에 새 팀을 하나 추가한 뒤** 파일 5개를 갈아끼웠습니다.

| 확인한 것 | 결과 |
|---|---|
| 안내 문구 · 안내 보조 문구 | **그대로** |
| 인폼 메일 수신인 4명 | **그대로** |
| CCG 목록 (새로 추가한 팀 포함) | **그대로** |
| `settings` 블록 전체 | **한 글자도 안 바뀜** |
| `data.json` 파일 자체 | **md5 동일** |
| 출장 건수 · 대시보드 4개 금액 · 센터 제출 CSV | **동일** |
| 실제 인폼 메일의 수신인 | 고쳐 둔 **4명** 그대로 |
| 한 번도 안 고친 서버 | 옛 수신인 2명이 **그대로 남음** (새 기본값이 저절로 들어오지 않음) |

**22개 항목 전부 통과.** 이 스크립트는 임시 폴더에서 예시 원장으로만 돌아가며,
운영 데이터는 열지도 않습니다.

이 리허설은 **v9.4 의 `store.py` 를 그대로 둔 채** 5개 파일만 갈아끼워 돌렸습니다 —
저장 로직을 건드리지 않아도 v10.28 가 온전히 동작한다는 뜻입니다.

---

## 참고 — v10.28 이후 새로 생기는 칸 하나

`시스템 설정` 화면에서 **CCG 목록을 실제로 고쳤을 때만** `data.json` 안에
`settings.ccg_teams` 칸이 하나 생깁니다. 출장·예산 데이터에는 칸이 하나도 늘지 않습니다.

이전 버전으로 되돌려도 그 칸을 **무시하고** 넘어가므로 오류는 나지 않습니다
(되돌린 동안에는 고친 CCG 목록 대신 예전 목록이 보입니다 — 데이터는 그대로입니다).
이것도 리허설에서 실제로 확인했습니다.
