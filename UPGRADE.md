# 데이터를 지키면서 버전 올리기 (초보자용)

이미 출장 데이터가 쌓여 있는 서버를 **v10.13 로 올리는 방법**입니다.
지금 쓰는 버전이 v9.4든 v10.x든 절차는 같습니다.

> **결론부터.** 데이터는 `data.json` **파일 하나**에 들어 있고, 이번 업그레이드는
> **프로그램 파일 5개만 갈아끼웁니다.** 그 5개 안에 데이터는 없습니다.
> 아래 순서대로만 하면 데이터는 그대로입니다.

---

## 0. 시작 전에 — 딱 하나만 기억하세요

### ❌ 절대 하지 말 것

```
servera\travelbudget 폴더를 지우고 → 새 폴더를 붙여넣기
```

**데이터가 이 폴더 안에 들어 있습니다.** 폴더를 지우면 원장과 자동 백업 30개가 함께 사라집니다.
윈도우 탐색기의 **"덮어쓰기"는 안전**합니다(파일만 바뀌고 나머지는 남음).
**"폴더 삭제 후 붙여넣기"만 위험**합니다.

```
servera\travelbudget\
├─ core.py              ← 프로그램 (바꿈)
├─ routes.py            ← 프로그램 (바꿈)
├─ store.py             ← 프로그램 (안 바꿈)
├─ __init__.py          ← 프로그램 (안 바꿈)
├─ static\app.js        ← 프로그램 (바꿈)
├─ templates\           ← 프로그램 (바꿈)
└─ data_json\           ★ 여기가 데이터. 절대 건드리지 않습니다
   ├─ data.json            ← 출장·예산·사람 전부 이 파일 하나
   └─ backup\              ← 자동 백업 30개
```

---

## 1단계. 지금 숫자를 적어 둡니다 (2분)

올린 뒤에 **같은 숫자인지 대조**하기 위해서입니다. 다르면 되돌리면 됩니다.

**화면에서 확인하는 방법 (가장 쉬움)**

1. 브라우저로 `material.skhynix.com/travelbudget` 접속
2. **출장 내역** 메뉴 → 목록 맨 위의 **전체 건수**
3. **대시보드** → **총 예산 / 처리 완료 / 처리 중 / 확정 예정 / 가용 잔여**

종이나 메모장에 이렇게 적어 두세요.

```
날짜: 2026-__-__
출장 건수: ______건
총 예산:  ____________원
처리 완료: ____________원
처리 중:  ____________원
확정 예정: ____________원
가용 잔여: ____________원
```

**명령어로 확인하는 방법 (더 정확)**

서버에서 아래를 실행하면 데이터 위치와 숫자를 한 번에 찍어 줍니다. **읽기만 하고 아무것도 바꾸지 않습니다.**

```
python check_data.py
```

---

## 2단계. 데이터가 어디 있는지 확인합니다 (1분)

두 경우뿐입니다. `python check_data.py` 가 알려주지만, 눈으로 확인해도 됩니다.

| 경우 | 확인 방법 | 이후 절차 |
|---|---|---|
| **A. 앱 폴더 안** (대부분 여기) | `servera\travelbudget\data_json\data.json` 이 있음 | 3단계부터 그대로 |
| **B. 앱 폴더 밖** | 서비스 환경변수에 `TB_DATA_DIR` 이 잡혀 있음 | 3단계부터 그대로 (더 안전) |

---

## 3단계. 백업합니다 ★ 가장 중요 (2분)

**앱 폴더 바깥에** 복사해 둡니다. 안에 두면 나중에 같이 지워질 수 있습니다.

윈도우 탐색기에서:

1. `servera\travelbudget\data_json` 폴더를 마우스 우클릭 → **복사**
2. 바탕화면(또는 `D:\백업`)에 **붙여넣기**
3. 폴더 이름을 `data_json_백업_20260731` 처럼 **날짜를 붙여** 바꿔 둡니다

명령 프롬프트를 쓴다면:

```
xcopy /E /I servera\travelbudget\data_json D:\백업\data_json_20260731
```

> 이 백업 하나만 있으면 **무슨 일이 있어도 원래대로 되돌릴 수 있습니다.**
> 복사가 끝나면 백업 폴더 안에 `data.json` 이 실제로 보이는지 눈으로 확인하세요.

---

## 4단계. 서버를 멈춥니다 (1분)

어떻게 띄웠느냐에 따라 다릅니다.

| 띄운 방법 | 멈추는 법 |
|---|---|
| 검은 창(명령 프롬프트)에서 `python webmain.py` | 그 창을 클릭하고 **Ctrl + C** |
| 윈도우 서비스로 등록 | **서비스** 앱 → 해당 서비스 → **중지** |
| 잘 모르겠음 | 작업 관리자 → **세부 정보** 탭 → `python.exe` → **작업 끝내기** |

브라우저에서 새로고침했을 때 **접속이 안 되면** 멈춘 것입니다.

---

## 5단계. 파일 5개만 덮어씁니다 (3분)

받은 `travelbudget_flask_v10.13.zip` 의 압축을 풀면 이런 구조입니다.

```
travelbudget_flask_v10.13\
└─ servera\
   └─ travelbudget\
      ├─ core.py
      ├─ routes.py
      ├─ static\app.js
      └─ templates\index.html, traveler_guide.html
```

**이 5개만** 서버의 같은 자리에 덮어씁니다.

```
servera\travelbudget\core.py
servera\travelbudget\routes.py
servera\travelbudget\static\app.js
servera\travelbudget\templates\index.html
servera\travelbudget\templates\traveler_guide.html
```

탐색기에서 하는 법:

1. 압축 푼 폴더에서 `core.py` 와 `routes.py` 를 선택 → **복사**
2. 서버의 `servera\travelbudget\` 에서 **붙여넣기** → **"파일 바꾸기"** 선택
3. `static\app.js` 도 같은 방식으로
4. `templates\index.html`, `templates\traveler_guide.html` 도 같은 방식으로

> **`store.py` 와 `__init__.py` 는 건드리지 않습니다.**
> 데이터를 **저장하는 로직**은 v9.4 이후 한 줄도 바뀌지 않았습니다 — 저장 방식이 달라질 일이 없습니다.
>
> (v10.13 에서 `store.py` 의 **신규 설치용 기본값**만 바뀌었습니다 — 처음 설치할 때 넣어 줄
> CCG 목록과 수신인. 이미 데이터가 있는 서버는 원장에 저장된 설정을 쓰므로 이 값이
> 쓰이지 않습니다. 실제로 v9.4 의 `store.py` 그대로 v10.13 를 올려 원장이 한 글자도
> 바뀌지 않는 것을 확인했습니다. 트리를 맞추고 싶으면 같이 복사해도 안전합니다.)

---

## 6단계. 다시 켜고 확인합니다 (3분)

멈출 때와 반대로 켭니다(`python webmain.py` 또는 서비스 **시작**).

확인 순서:

1. **좌측 아래에 `v10.13 · 2026-08-01`** 이 보이는지 → 새 버전이 올라간 것
2. **1단계에 적어 둔 숫자와 같은지** → 출장 건수, 총 예산, 처리 완료, 가용 잔여
3. 출장 내역에서 **아무 건이나 열어** 사람·금액이 그대로인지
4. (가능하면) `python smoke_test.py` — 19개 항목을 읽기만 하며 점검합니다

**숫자가 하나라도 다르면 7단계로 되돌리고 알려 주세요.**

---

## 7단계. 되돌리는 법 (문제가 생겼을 때만)

되돌리기는 **두 종류**입니다. 대부분은 ①만으로 충분합니다.

### ① 프로그램만 되돌리기 (데이터는 그대로)

이전 버전 zip 에서 **같은 5개 파일**을 덮어쓰고 재기동합니다. 데이터는 손대지 않습니다.
실제로 v10.13 → v9.4 로 되돌려 봤고, **출장 건수·금액·상태·CSV 결과가 전부 원래와 같았습니다.**

### ② 데이터까지 되돌리기 (실수로 데이터를 건드렸을 때)

3단계에서 만든 백업 폴더를 `servera\travelbudget\data_json` 자리에 덮어씁니다.
(백업을 안 만들었다면 `data_json\backup\` 안에 **자동 백업 30개**가 있습니다 —
화면 **데이터 관리 → 백업 복원** 에서도 고를 수 있습니다.)

---

## 8단계. 올린 뒤 화면에서 해야 하는 일 ★ v10.13 전용 (5분)

**이번 버전은 여기까지 해야 끝납니다.** 새 CCG 목록과 새 수신인은 **자동으로 반영되지 않습니다.**

이유: 이미 쓰고 있는 서버에는 `data.json` 안에 **예전 설정이 저장돼 있고**, 프로그램은
저장된 설정을 코드 기본값보다 **우선**합니다. (담당자가 화면에서 고쳐 둔 값을
새 배포가 조용히 덮어쓰면 안 되기 때문입니다.)

좌측 메뉴 **시스템 설정** → 담당자 인증(2071478) 후 아래 순서로 하세요.

### 8-1. 인폼 수신인 4명으로 (1분)

기존에 2명만 들어 있고, **김은정 주소가 동명이인이라 틀려 있습니다.**
`+ 수신인 추가` 로 채우고 `설정 저장`.

```
junghoon12.lee@sk.com      이정훈 (소재)
eunjeong5.kim@sk.com       김은정 (소재)   ← 주소가 바뀌었습니다
Jeewoung.Chun@sk.com       천지웅 (센터)
geonyoung.kim@sk.com       김건영 (센터)
```

### 8-2. CCG 8팀은 이미 들어 있습니다 (확인만, 30초)

같은 화면의 **CCG팀** 표에 아래 8줄이 **이미 보입니다.** 새 목록이 프로그램 기본값이고,
지금까지 CCG를 화면에서 고친 적이 없어 기본값이 그대로 쓰이기 때문입니다. 확인만 하세요.

| 팀 이름 | CCG 코드 |
|---|---|
| 소재전략 | 50110502 |
| P&C소재 | 50128121 |
| Patterning소재기술 | 50119135 |
| Patterning소재개발 | 50077468 |
| C&C소재기술 | 50139632 |
| C&C소재개발 | 50134405 |
| EDTW소재기술 | 50119134 |
| EDTW소재개발 | 50103536 |

### 8-3. 옛 코드를 새 팀으로 옮기기 ★ 이게 핵심 (2분)

화면 맨 아래 **`원장 CCG 정리`** 에 **지금까지 쌓인 출장의 옛 코드**가 건수와 함께 나옵니다.
줄마다 **옮길 팀을 고르고 [옮기기]** 를 누르세요. 옛 팀 수만큼 반복하면 끝입니다.

- 해당 출장자의 **CCG 코드·팀 이름만** 새 값으로 바뀝니다
- **금액·날짜·상태는 그대로입니다** — 리허설에서 처리 완료·처리 중·확정 예정·가용 잔여
  **네 숫자가 1원도 바뀌지 않는 것**을 확인했습니다
- 누르기 직전 **자동 백업**이 만들어지고 감사 로그에 남습니다 — 잘못 옮겼으면 복원할 수 있습니다

다 옮기면 `원장 CCG 정리` 카드가 사라집니다. **그러면 끝입니다.**

> **다음 해에 또 바뀌면**: CCG팀 표에서 이름·코드를 고치고 저장 → `원장 CCG 정리`에서 옮기기.
> 파일 교체도, 저에게 요청도 필요 없습니다.

---

## 자주 나오는 질문

**Q. 사람들이 쓰는 중인데 올려도 되나요?**
점심시간처럼 아무도 안 쓰는 때가 좋습니다. 올리는 동안(약 5분) 화면이 안 열립니다.
저장 중에 끄면 안 되니, 방금 저장한 사람이 없는지 확인하고 멈추세요.

**Q. 올리면 사람들이 다시 입력해야 하나요?**
아닙니다. 이미 입력된 출장·실적·예산·감사 기록은 그대로 있습니다.

**Q. 지금 버전이 v9.4 인지 v10.5 인지 모르겠습니다.**
화면 **좌측 아래**에 버전이 찍혀 있습니다. 어느 버전이든 절차는 같습니다.

**Q. CCG 팀이나 메일 수신자가 바뀌면 또 파일을 바꿔야 하나요?**
아닙니다. v10.13부터는 **좌측 메뉴 `시스템 설정`** 에서 화면으로 바꿉니다(예산 담당자 인증 필요).
이미 원장에서 쓰고 있는 CCG 코드는 실수로 지울 수 없게 막아 두었습니다.

**Q. 설정을 바꾸면 지난 분기 집계가 달라지나요?**
아닙니다. 원장에는 **출장 등록 시점의 CCG 코드**가 그대로 남습니다.
설정을 바꿔도 과거 숫자는 소급해서 바뀌지 않습니다.

---

## 이 절차를 실제로 리허설한 결과

문서만 쓰지 않고, **v9.4 서버를 띄워 데이터를 쌓은 뒤 위 절차 그대로 v10.13 로 올려** 봤습니다.

| 항목 | 리허설에 쓴 원장 | 업그레이드 후 |
|---|---|---|
| 출장 건수 | 15건 | **15건 (동일)** |
| 출장자 행 | 28명 | **28명 (동일)** |
| 예산 리비전 | 6건 · 배정 67,600,000원 | **동일** |
| 계획 합계 / 실적 합계 | 3,623,000 / 2,049,000원 | **동일** |
| 상태별 건수 | 계획4·확정3·실적3·이관3·완료2 | **동일** |
| 감사 로그 | 52건 | **52건 (동일)** |
| 대시보드 | 완료 451,000 / 처리중 1,598,000 / 확정 856,000 / 가용 64,695,000 | **동일** |
| 센터 제출 CSV | md5 `7af279cd…` | **md5 동일** |
| 자동 백업 | 30개 | **30개 (동일)** |
| **`data.json` 파일 자체** | md5 `56aa703b…` | **md5 동일 — 한 글자도 안 바뀜** |

그다음 **v9.4 로 되돌리는 것까지** 해 봤습니다 — 출장 건수·금액·상태·CSV 결과가 전부 원래와 같았고,
점검 스크립트도 19개 항목 전부 통과했습니다.

이 리허설은 **v9.4 의 `store.py` 를 그대로 둔 채** 5개 파일만 갈아끼워 돌렸습니다 —
저장 로직을 건드리지 않아도 v10.13 가 온전히 동작한다는 뜻입니다.

---

## 참고 — v10.13 이후 새로 생기는 칸 하나

`시스템 설정` 화면에서 **CCG 목록을 실제로 고쳤을 때만** `data.json` 안에
`settings.ccg_teams` 칸이 하나 생깁니다. 출장·예산 데이터에는 칸이 하나도 늘지 않습니다.

이전 버전으로 되돌려도 그 칸을 **무시하고** 넘어가므로 오류는 나지 않습니다
(되돌린 동안에는 고친 CCG 목록 대신 예전 목록이 보입니다 — 데이터는 그대로입니다).
이것도 리허설에서 실제로 확인했습니다.
