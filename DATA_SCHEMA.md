# 데이터 포맷 명세 (data.json) — 소재 국내 출장비 관리

내부 Qwen으로 기존 데이터를 이 포맷으로 변환해 넣기 위한 스펙입니다.
**정본 파일 1개**로 동작합니다.

- 위치: `servera/travelbudget/data_json/data.json` (UTF-8, JSON)
- 백업: `servera/travelbudget/data_json/backup/` (저장 직전 자동 30개)
- 첫 실행 시 파일이 없으면 예시 시드가 자동 생성됩니다. **본인 데이터로 교체하려면**
  서버를 한 번 띄워 폴더가 생성된 뒤 `data.json`을 본인 파일로 덮어쓰고 재시작하거나,
  폴더를 미리 만들어 파일을 넣어 두면 됩니다.
- 서버는 로드 시 **최상위 구조만 검증**(`settings`=객체, `budget`/`groups`=객체 배열)하고,
  각 레코드의 **파생 필드는 자동 계산**합니다(아래 "자동 계산" 참고). 즉 파생 필드는 넣지 않아도 됩니다.

---

## 1. 최상위 구조

```json
{
  "schema_version": "2.0",
  "updated_at": "2026-07-22T09:00:00",
  "settings": { ... },
  "budget":   [ ... ],
  "groups":   [ ... ],
  "audit_log": []
}
```

| 키 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `schema_version` | string | 권장 | 정보용. `"2.0"` |
| `updated_at` | string(ISO) | 아니오 | 저장 시 서버가 갱신. 생략 가능 |
| `settings` | object | **예** | 아래 §2 |
| `budget` | array\<object\> | **예** | 분기 예산 리비전. 아래 §3 |
| `groups` | array\<object\> | **예** | 출장 1건=1객체. 아래 §4 |
| `audit_log` | array | 아니오 | 감사 로그. 서버가 append. `[]`로 시작 |

---

## 2. settings (object)

```json
{
  "system_name": "소재 국내 출장비 관리",
  "admin_id": "2071478",
  "admin_pw": "2071478",
  "mail_recipients": ["junghoon12.lee@sk.com", "eunjeong5.kim@sk.com"],
  "reference_url": "material.skhynix.com/travelbudget"
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `system_name` | string | 시스템 표시명 |
| `admin_id` / `admin_pw` | string | 관리자 계정(공개 계정). 이관·처리·예산·복원 게이트에 사용 |
| `mail_recipients` | string[] | 실비 이관 인폼 **수신(To)**. 이메일 배열 |
| `reference_url` | string | 인폼 하단 "참고" 링크 |

> `admin_pw`는 응답(`/api/state`)에 **절대 포함되지 않습니다**. 파일에는 평문으로 둡니다(공개 계정).

---

## 3. budget[] (분기 예산 리비전, 한 줄 = 한 변경)

```json
{ "rev_id": "B-0001", "yq": "2026-3Q", "rev_dt": "2026-07-01",
  "rev_type": "최초배정", "amt": 9000000, "reason": "분기 정기 배정" }
```

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `rev_id` | string | 권장 | 고유 ID. 예 `B-0001` (삭제 시 사용) |
| `yq` | string | **예** | 분기. **정확히** `"YYYY-NQ"` 형식 (예 `2026-3Q`). 예산은 이 값 **그대로** 필터되므로 오타 금지 |
| `rev_dt` | string(date) | 권장 | 반영일 `YYYY-MM-DD` |
| `rev_type` | enum | **예** | `최초배정` / `추가증액` / `감액` / `이월` |
| `amt` | integer | **예** | 금액(원). **`감액`은 음수**로 기입 (예 `-1000000`) |
| `reason` | string | 아니오 | 사유 |

> 분기 총예산 = 해당 `yq`의 모든 `amt` 합계.

---

## 4. groups[] (출장 1건 = 1 그룹, 동행자는 travelers 배열)

```json
{
  "group_id": "TB-0002",
  "plan_type": "계획",
  "status": "처리 완료",
  "city": "청주",
  "org": "원익머트리얼즈",
  "purpose": "NF3 순도 관리 정기 Audit",
  "kind": "정기 Audit",
  "dep_dt": "2026-08-04",
  "ret_dt": "2026-08-05",
  "car": "자차사용",
  "remark": "",
  "travelers": [
    { "name": "박영희", "emp_no": "20140508", "rank": "팀장", "ccg_nm": "EDTW소재기술",
      "p_trans": 70000, "p_lodg": 95000, "p_meal": 65000, "p_etc": 10000,
      "a_trans": 65000, "a_lodg": 90000, "a_meal": 60000, "a_etc": 10000 },
    { "name": "이정훈", "emp_no": "2071478", "rank": "TL", "ccg_nm": "C&C소재기술",
      "p_trans": 70000, "p_lodg": 95000, "p_meal": 65000, "p_etc": 10000,
      "a_trans": 68000, "a_lodg": 90000, "a_meal": 62000, "a_etc": 8000 }
  ]
}
```

### 그룹 필드

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `group_id` | string | **예** | **고유 키**. 예 `TB-0001`. 본인 체계 가능(유일하기만 하면 됨) |
| `plan_type` | enum | **예** | `계획` / `변경` / `긴급` |
| `status` | enum | **예** | §5 상태값 중 하나 (잠정=`계획 등록`, 실제로 감=`확정 예정`) |
| `confirmed` | bool | 아니오 | **생성 시에만** — `true`면 바로 `확정 예정`(예산 선확보), 기본 `false`(잠정). 저장 후엔 `status`로 관리 |
| `city` | string | **예** | 출장도시 |
| `org` | string | **예** | 출장기관&업체(BP사) |
| `purpose` | string | **예** | 출장목적&사유 (소재명 포함 권장) |
| `kind` | enum | **예** | §5 출장구분 5종 |
| `dep_dt` | string(date) | **예** | 출발일 `YYYY-MM-DD` |
| `ret_dt` | string(date) | **예** | 복귀일 `YYYY-MM-DD` (dep 이상) |
| `car` | enum | **예** | `미사용` / `자차사용` |
| `remark` | string | 조건부 | 비고. **긴급 출장 실적 입력 시 필수** |
| `travelers` | array | **예** | 1명 이상 |
| `tags` | string[] | 아니오 | **분류용 태그**(선택). `#` 없이 저장하며 화면에서 `#`로 보입니다. 8개·각 20자 상한. 금액·상태에 관여하지 않습니다 (v10.25) |
| `sap_doc` | string | 아니오 | 소재팀 **SAP 전표번호**(비용 처리 근거). 관리자가 화면에서 입력하며 CSV에도 나갑니다 |
| `created_at`/`updated_at`/`inform_at`/`settle_at` | string(ISO) | 아니오 | 서버 관리. 생략 가능 |

### traveler 필드 (개인별)

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `name` | string | **예** | 성명 |
| `emp_no` | string | **예** | 사번. **그룹 내 유일** (문자열 권장) |
| `rank` | enum | **예** | `TL` / `팀장` |
| `ccg_nm` | string | **예** | CCG팀명. §6의 **정확한 팀명** 사용 |
| `ccg` | string | 아니오 | CCG 코드. `ccg_nm`이 정확하면 **자동 채움**(생략 권장) |
| `status` | enum | 아니오 | **개인 처리 상태**(이관/처리 단계). 생략 시 그룹 상태를 따름. §5 참고 — `실적 입력·인폼`/`소재 이관`/`처리 완료`/`보류` |
| `email` | string | 아니오 | 소재 담당자 이메일. 있으면 **이관 인폼**(비용 처리 요청)의 수신(To)에 자동 채워집니다. 없으면 담당자가 직접 수신자를 넣습니다 |
| `p_trans` `p_lodg` `p_meal` `p_etc` | integer | 아니오 | **계획** 비용(원): 교통/숙박/식대&잡비/기타. 없으면 0 |
| `a_trans` `a_lodg` `a_meal` `a_etc` | integer | 아니오 | **실적** 비용(원): 동일 4항목. 실적 전이면 0 |

> **개인별 처리(같은 출장, 사람마다 따로):** 5명이 함께 다녀와도 3명은 처리 완료, 1명은 예산 부족으로 `보류`처럼
> **출장자마다 처리 상태가 다를 수 있습니다.** 그때 각 traveler의 `status`를 개별로 두면 예산·대시보드가 인당으로 정확히 반영됩니다
> (완료 인원 금액=처리완료, 이관·인폼·보류 인원 금액=처리중). 모두 같은 상태면 traveler `status`는 생략하고 그룹 `status`만 두면 됩니다.
> 그룹 `status`는 개인 상태들의 **롤업**(전원 완료→처리 완료, 일부 진행→소재 이관 등)이라 두 값이 어긋나도 서버가 맞춥니다.

> 금액은 **정수(원)**. 문자열의 콤마(`"1,000"`)는 서버가 제거해 정수화하고, 숫자가 아니면 0 처리합니다.

---

## 5. 열거값 (정확히 일치해야 함)

| 구분 | 허용값 |
|---|---|
| `plan_type` | `계획` · `변경` · `긴급` |
| `status` (그룹) | `계획 등록`(잠정) · `확정 예정`(예산 확보) · `실적 입력·인폼` · `소재 이관` · `처리 완료` · `취소` |
| `status` (출장자 개인) | `실적 입력·인폼` · `소재 이관` · `처리 완료` · `보류` |
| `rank` | `TL` · `팀장` |
| `kind` | `기술교류(Live Demo, Data 분석)` · `실사&사양 개선,협의` · `정기 Audit` · `비정기 Audit(Issue/Theme)` · `기타` |
| `car` | `미사용` · `자차사용` |
| `rev_type` | `최초배정` · `추가증액` · `감액` · `이월` |

> 주의: `실적 입력·인폼`의 가운뎃점은 **U+00B7(·)**, `식대&잡비`의 `&`는 그대로 사용.

### 상태 흐름 & 예산 반영
```
계획 등록(잠정) → 확정 예정(예산 확보) → 실적 입력·인폼 → 소재 이관 → 처리 완료   (+ 취소)
```
- **가용 잔여 = 총예산 − 처리완료 − 처리중 − 확정예정(확보)**, 처리중 = `실적 입력·인폼` + `소재 이관`(+ 개인 `보류`)
- `계획 등록`(잠정)의 계획비는 **참고만**(예산 미반영) — 실제로 안 가면 **삭제**(흔적 없이 사라짐, DELETE)
- `확정 예정`(실제로 감)은 계획비만큼 **예산 선확보**(가용에서 차감). 생성 시 `confirmed:true` 또는 이후 ‘출장 확정’
- 과거 완결 건은 `status="처리 완료"` + `a_*` 채움 / 실제로 갈 계획은 `status="확정 예정"` + `p_*` 채움

---

## 6. CCG팀 ↔ 코드 (7팀)

| 팀명 (`ccg_nm`) | 코드 (`ccg`, 자동) |
|---|---|
| `Patterning소재기술` | 50119135 |
| `C&C소재기술` | 50139632 |
| `C&C소재개발` | 50134405 |
| `EDTW소재기술` | 50119134 |
| `EDTW소재개발` | 50103536 |
| `P&C소재` | 50128121 |
| `소재전략` | 50110502 |

---

## 7. 자동 계산 (넣지 않아도 됨 / 넣어도 서버가 재계산)

| 필드 | 계산식 |
|---|---|
| `days` | 복귀일 − 출발일 + 1 |
| `quarter` | 출발일의 분기 (`1Q`~`4Q`) |
| `yq` | **출발일 기준** `YYYY-NQ` — 그룹엔 생략 가능(출발일에서 파생). ※ budget의 `yq`는 파생 안 되니 필수 |
| `lv2` | `"소재"` 고정 |
| `ccg` | `ccg_nm` → 코드 매핑 |
| `plan_tot` / `act_tot` | 각 traveler `p_*` / `a_*` 합계 |
| `stage` | 프로세스 정렬 가중치(계획0·확정1·인폼2·이관3·완료4·취소5) |
| `lead_days` | 등록일 → 출발일 리드타임(사전 신청 기준 D-7 판단용) |

---

## 8. Qwen 변환 지침 (요약)

1. **출장 1건 = groups[] 1객체**, 동행자는 그 안 `travelers[]`에 행 추가.
2. 날짜는 `YYYY-MM-DD`. 그룹 `yq`는 생략 가능(출발일 파생). **budget의 `yq`는 필수**(`YYYY-NQ`).
3. `ccg_nm`은 §6 팀명과 **글자 그대로 일치**. `ccg` 코드는 생략(자동).
4. 금액은 **정수(원)**. `감액` 예산은 `amt` **음수**.
5. `status`는 실제 진행에 맞게: 완결=`처리 완료`(+`a_*`), 계획만=`계획 등록`(+`p_*`).
6. `group_id`/`rev_id`는 **유일**하게.
7. 열거값·가운뎃점(`·`)·`&`는 §5 표와 정확히 동일하게.

전체 예시는 같은 폴더의 **`data.example.json`** 참고 (그대로 `data.json`으로 넣으면 동작).

---

## 9. CSV 내보내기 (2종)

| 엔드포인트 | 내용 | 화면 위치 |
|---|---|---|
| `GET /api/export.csv?yq=` | **센터 관리 양식(정산 대장) 27필드** — 최초 엑셀표 순서, 출장자 개인별 행 | 출장 내역 · 데이터 관리 |
| `GET /api/export.csv?yq=&mode=internal` | 위 27필드 + 상태·개인처리상태·SAP전표번호·리드타임 (내부 관리용) | 데이터 관리 |
| `GET /api/export.xls?yq=` | 센터 양식 + **글꼴 서식**(한글 맑은 고딕 / 영문·숫자 Trebuchet MS) | 데이터 관리 |
| `GET /api/export_budget.csv?yq=` | **예산 리비전** 8필드(분기·REV·반영일·유형·증감액·**누적액**·사유) | 예산 관리 · 데이터 관리 · 센터 리포트 |

`yq`를 빼면 전체 분기. 누적액은 분기가 바뀌면 다시 0부터 누적합니다.
두 CSV 모두 UTF-8 BOM(엑셀 한글 호환)이고, 자유입력 셀은 수식 인젝션(`=`,`+`,`-`,`@`)을 방어합니다.
